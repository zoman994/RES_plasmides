/**
 * feature-flags.js — простые компилируемые фиче-флаги для обратимых UX-правок.
 *
 * Назначение: новую функциональность из UX_DIRECTION (фазы 1+) монтируем за
 * флагом, чтобы откат был мгновенным — флип значения в `false` возвращает
 * прежний интерфейс, без правки самих компонентов и без git-операций.
 *
 * Phase 1 (19.06.2026, по «погнали фазу 1, но оставь возможность откатить»):
 *   projectContextBar — полоса/карточка активного проекта в Sidebar (всегда
 *   видно, какой `.bodge` открыт). Откат: поставить false.
 */
export const FEATURE_FLAGS = {
  projectContextBar: true,
  // Phase 2 (логика окон): хлебные крошки «где я сейчас» (Инструменты/Проект →
  // окно). Проектная крошка КЛИКАБЕЛЬНА → popFullscreen (выход из оверлея). Этот
  // же флаг СЛИМИТ шапку канваса (`SkeletonHeader`): убирает дубль «← Назад» +
  // имя проекта, т.к. крошки их покрывают (back-job у канваса = собственный
  // «← Назад» в `EditorWindowShell`). Связка намеренная: off → крошек нет И
  // шапка снова с «← Назад» + именем (нет «безвыходного» состояния). Откат: false.
  breadcrumb: true,
  // Phase 2 (логика окон): двухуровневый рельс в Sidebar — группы «Инструменты»
  // (Главная/Библиотека/Выравнивание) и «Проект · {имя}» (окна активного проекта).
  // Откат: false (заголовки секций + проектный блок исчезают).
  twoLevelRail: true,
  // Phase 3 (настройки): ⚙ «Вид» всегда в шапке инспектора (disabled вне вкладки
  // «Последовательность») — стабильная раскладка + находимость. Откат: false →
  // gear показывается только на вкладке Sequence (как было).
  viewGearAlwaysVisible: true,
  // Phase 4 (контракт кликов): правый клик по записи в дереве библиотеки →
  // контекстное меню действий. Откат: false (правый клик = браузерное меню).
  libraryTreeContextMenu: true,
  // Кольцевая карта плазмиды V2 (редизайн Claude Designer, принят 20.06): feature-
  // centric с внешними лидер-подписями + стрелки по цепи + радиальные RE + тултип.
  // Применяется к ПРОСМОТРУ плазмиды (нет junctions/primers); сборочная карта —
  // legacy. PlasmidMap делегирует в PlasmidMapV2. Откат: false → старый рендер везде.
  plasmidMapV2: true,
  // Чистка (Игорь «дохера кнопок создать проект»): единственная точка создания
  // проекта — сайдбар «+ Создать проект» (всегда видна, ⌃N). Скрывает дубль
  // «+ Проект» в тулбаре дерева библиотеки. Откат: false → дубль возвращается.
  dedupeCreateProject: true,
};

export function isFeatureOn(name) {
  return !!FEATURE_FLAGS[name];
}
