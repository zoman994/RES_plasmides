# bodge-v1 reference fixtures

These are frozen v1 `.bodge` files used to verify the v1→v2 migration
chain stays stable across future refactors.

Generated by `make-fixtures.js` (re-run with `node make-fixtures.js`
from this directory).

| File | Description |
|------|-------------|
| `v1-empty.bodge` | Manifest + empty project, no library, no extras. |
| `v1-with-library.bodge` | Manifest + project + library/entries.json |
| `v1-with-extras.bodge` | Manifest + project + containers/ stub (warned) |

Do NOT edit by hand — re-generate with `node make-fixtures.js`.
