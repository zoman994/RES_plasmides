# BUGS.md — BodgeGene v0.8.4-alpha

Баги → сюда. Починены → `[x]`. Claude Code читает при старте сессии. Единственный трекер багов (CLAUDE.md §4).

---

## Сброс 27.05.2026 — трекер начат с чистого листа

Трекер вычищен полностью. Причина: после переработки структуры assembly/skeleton
(Звено-сессии, 23.05+) диагнозы накопленных OPEN-багов ссылались на файлы,
которых в дереве больше нет (`assembly-realise-suggest.js`, `skeleton-state.js`,
`selectors-assembly.js` и др.) — следовать им вслепую нельзя. Решение Игоря:
не пере-диагностировать стек устаревших записей, а прогнать реальные пайплайны
заново и репортить свежие баги от живого симптома.

**Где старое:**
- `docs/archive/BUGS_PRE_RESET_2026_05_27.md` — полный до-сбросовый снимок
  (13 OPEN со стале-диагнозами V51/V98–V110 + весь FIXED-блок v0.8.3 V52–V117).
- `docs/archive/BUGS_HISTORY.md` — закрытые баги v0.6–0.8 (V49–V57 и др.).
- `docs/archive/BUGS_v05.md` — v0.5 legacy (V1–V48).

**Нумерация:** новые баги — с **V118**. Номера V1–V117 не переиспользовать —
они живут в `DECISIONS.md` / `ZVENO_LOG.md` / commit-сообщениях, коллизия
сломает обратные ссылки.

**Carry-over (известны, не репортить как новые).**
- **Лаг ПЕЧАТИ нуклеотидов в editable SequenceView (режим сборки + правка референса в align)** (Игорь, 18.06) —
  **✅ ИСПРАВЛЕНО 18.06 (PERF-6, DEC-SQV-MEMO-01).** Корень: каждое нажатие меняет `fullSeq` (и все производные:
  `features`/`orfRanges`/`reSites`/`framesResolution`/`line`/`seqLength`) → `SequenceLine.React.memo` ломался у ВСЕХ
  строк → ре-рендер всего вьюера на нажатие. Фикс: custom `areEqual` (`lib/sequence-line-equal.js`) — `Object.is` по
  всем пропам, КРОМЕ relaxed-набора, который сравнивается по line-relevant контенту (`fullSeq` окно ±3 для кодонов на
  стыке; `features`/`orfRanges` overlap; `reSites` overlap+24; `framesResolution` по {strategy,dominant}; `line`
  shallow; `seqLength` только для wrap-bridge). Неизменные строки теперь bail'ят → ре-рендерится только правленая
  область. **Безопасность:** equivalence-тест (`sequence-line-memo-equivalence.test.jsx`, 7 видов правок) — memo-рендер
  ДОЛЖЕН совпасть со свежим рендером, ловит любой стейл-рендер эмпирически; + 11 юнитов компаратора. Полный прогон
  **5188 pass / 0 fail**, общий вьюер (Library/Annotator/canvas/importer/align) не сломан. Copy/paste в сборке: paste
  починен (V152), copy работает (V152-верификация).
- **«Выравниватель всё ещё медленный» — появление + печать на референсе >5 кб** (Игорь, 19.06) —
  **✅ ИСПРАВЛЕНО 19.06 (PERF-9, DEC-SQV-VIRTUAL-01).** Замеры развели корень: O(seqLen)-производные дёшевы
  (`detectORFRanges` 0.77мс, `scanAllSites` 0.41мс на 8 кб) — узкое место был **рендер ВСЕХ N тяжёлых строк**
  (`SequenceView` монтировал каждую строку плазмиды; >5 кб = ~100+ строк × ruler/strands/annotations/AA/RE/read/
  chromatogram = тысячи SVG). «Появление» = синхронный монтаж всех строк; «печать» = прямой ввод (вставка) сдвигает
  все нижележащие позиции → каждая строка ниже каретки реально перерендеривается. **Фикс: виртуализация строк**
  (`lib/line-window.js` + `SequenceLine active={false}` placeholder). Видимое окно (±overscan) рендерится полностью;
  остальные индексы — placeholder фикс. высоты с теми же `data-*`-атрибутами (overlays / scrollToPosition /
  computeVisible продолжают находить слоты) без тяжёлых треков. Gated по размеру (>30 строк) и **выключена в
  тест-env** → 487+ тестов вьюера идут eager-путём, ноль регресса. Браузер (7904 bp, ancestor-scroller): 53 строки →
  21 real + 36 placeholder, DOM 4.9k вместо ~13k; скролл сдвигает окно ([0,16]→[9,38]) без пустот; высота прокрутки
  сохранена (6755px); выделение выровнено (rect.top совпал с offsetTop строки); 0 ошибок. Полный прогон **5212 pass /
  0 fail** (+24: 19 line-window + 5 placeholder).
- **Быстрый скролл сиквенса: (1) не успевает отрисовываться, (2) в конце пути дёргается вверх-вниз (особ. drag средней кнопкой вниз)** (Игорь, 20.06) —
  **✅ ИСПРАВЛЕНО 20.06 (PERF-9 follow-up, DEC-SQV-VIRTUAL-02).** Два независимых корня в виртуализации:
  (1) *пустоты* — `overscan` был фикс. 10 строк симметрично; быстрый wheel/drag сдвигает >10 строк за rAF-тик и обгоняет
  окно → мелькают placeholder'ы. Фикс: **velocity-aware directional overscan** (`directionalOverscan` в `line-window.js`)
  — ведущая по ходу движения кромка расширяется ∝ скорости (base + ⌈1.5·Δстрок⌉, кап `base*3`=30), `computeDesiredWindow`
  получил асимметричные `overscanBefore/After`. (2) *дёрганье* — `placeholderHeight` был ЕДИНОЙ оценкой (`mainLineHeight`)
  на все строки, но высоты разные (аннотации/AA/read-треки делают часть строк выше) → при флипе placeholder↔real менялся
  `scrollHeight`, а `overflowAnchor:none` не компенсирует → прыжок, хуже у дна где `scrollTop` упирается. Фикс:
  **per-line запомненные высоты** (`lineHeightsRef`: idx→border-box px, пишется для real-строк в `recomputeWindow`,
  placeholder той же строки берёт ЕЁ высоту) → `scrollHeight` инвариантен при флипе. Браузер (7904 bp): свип сверху-вниз
  сходит 15991→**14597** (по мере замера строк), затем revisit bottom→mid→bottom **стабилен** (14597=14597=14597) —
  раньше осциллировал. TDD: `line-window.test.js` +7 (directionalOverscan + asymmetric window), полный прогон
  **5297 pass / 0 fail**, build clean.
- Drag-selection микролаги в SequenceView на legacy-железе (бывш. V51) — **адресовано 18.06.2026 (PERF-4):**
  `posFromPointerEvent` под pointer-capture делал O(N) `getBoundingClientRect` по всем строкам на каждый
  pointermove (доминантный layout-thrash драга); заменено на единичный `elementFromPoint` hit-test (fallback
  сохранён). Полный прогон зелёный, поведение идентично. **Финальное подтверждение на ThinkPad 2013 — за Игорем.**
- ~~Рассинхрон Dexie: браузер `bodgegene-db` v50 vs код `DB_VERSION = 5`~~ — **ФАНТОМ, снято 31.05.2026.** Dexie кодирует IDB-версию ×10: `IDB v50 = Dexie v5` — норма. Реального downgrade нет (иначе приложение не открылось бы с VersionError, а оно работает). Не репортить.

---

## QA Fix-pack #1 — 25-агентный воркфлоу-прогон пайплайнов (16.06.2026)

Прогон 9 пайплайнов × ≥3 датасета + адверсариальная верификация (`/workflows`, 25 агентов). 30 подтверждённых находок. Fix-pack #1 закрыл живые high + системный лейтмотив «молчаливые сбои». Тесты: `src/__tests__/qa-fixpack.test.js` (C8/C1·C5/C18/Tm) + `store/__tests__/library-edit-log.test.js` (C2/C3). Остальные находки (medium/low — C10/C13/C14/C15/C16/C19/C28 и UX-трения) — в дайджесте воркфлоу, открыты как follow-up.

- [x] **V139 (C6) — Tm: Mg²⁺-коррекция была мертва в ПЦР-диапазоне (ошибка единиц M/mM).** Owczarzy-ветка включалась только при R = √[Mg]/[Mon] > ~4.5 (≈ ≥52 mM Mg); при 1.5–4 mM Mg (R≈0.7) коррекция не применялась → каждый Tm считался monovalent-only. **✅ ИСПРАВЛЕНО** `tm-calculator.js`: корректный режим Owczarzy 2008 (competing 0.22≤R≤6 с monovalent-поправкой a/d/g, divalent-dominant R>6). **Дефолт `mgConc` 1.5→0** — дефолтный Tm остаётся monovalent-only (не ломает калибровку праймеров/golden-pydna-тестов); Mg применяется при явной передаче. «Mg в дефолте» — отдельное калибровочное решение.
- [x] **V140 (C4) — Tm: naConc=0 → −273.1 °C (`log(0)=−Inf`).** **✅ ИСПРАВЛЕНО** `tm-calculator.js`: monovalent-ветка только при monoM>0; при naConc=0 без Mg — фолбэк на 1 M-baseline; финальный `Number.isFinite` guard.
- [x] **V141 (C17) — Tm: вырожденные/IUPAC-базы молча вырезались** (Tm для укороченного олиго; fallback NN — мёртвый код). **✅ ИСПРАВЛЕНО**: strip сохраняет IUPAC-коды (`[^ACGTURYSWKMBDHVN]`), длина честная, fallback NN достижим.
- [x] **V142 (C2/C3) — Library undo переживал version-bumping commit (state drift + висячий parentEntryHash).** Ctrl+Z после «Перезаписать»/«Сохранить как версию» откатывал контент ниже сохранённой версии (`version` оставался bumped), undo на родителе после fork делал `child.parentEntryHash` висячим. **✅ ИСПРАВЛЕНО** `librarySlice.js`: `overwriteLibraryEntryAnnotations` и `saveLibraryEntryAsVersion` чистят `libraryUndo[id]`/`[parentId]` (commit = save-point, как в align).
- [x] **V143 (C8) — Golden Gate: `checkInternalSites` находил только ПЕРВЫЙ сайт (`indexOf`).** Второй внутренний Type IIS сайт выживал, `suggestBestEnzyme` объявлял фермент чистым → дайджест ломал сборку. **✅ ИСПРАВЛЕНО** `golden-gate.js`: цикл по всем вхождениям на обеих цепях.
- [x] **V144 (C1/C5) — ORF-детекция strand-agnostic: forward-аннотация маскировала reverse-ORF.** Дедуп/фильтр existingCDS по `[start,end]` без strand → противоцепочечный ген терялся. **✅ ИСПРАВЛЕНО** `orf-detection.js`: overlap-дедуп учитывает strand (разные цепи на одном локусе = разные гены).
- [x] **V145 (C18) — RE-лигирование без фермента: пустой tail без warning.** Праймеры выглядели валидными, но RE-сайта нет. **✅ ИСПРАВЛЕНО** `local-primer-design.js`: `designPrimersLocal` валидирует стыки и пушит warning для ligation/re_ligation без enzyme.
- [x] **V147 — «Анализ интронов» игнорировал выделение, сканировал всю последовательность** (Игорь, 17.06, на приёмке Фазы 2). Кнопка 🧬 читала только `annotator.scope` (ставится при ОТКРЫТИИ аннатора из выделения), а живое выделение во встроенном вьювере жило локально в `PreviewTab` → не доходило. **✅ ИСПРАВЛЕНО**: `useSequenceSelection` поднят в `Annotator/index.jsx`, проброшен в `PreviewTab` (одно общее состояние); анализ скоупится по приоритету `живое выделение → scope.region → вся последовательность`; шапка scope-info показывает реальный диапазон. Тест `annotator-intron-scope.test.jsx` (два гена, выделен один → найден только его интрон).
- [x] **V148 — детекция интронов: лавина ложноположительных (PWM ~31/80 на 3k/6k случайной ДНК; CNN 3 вместо 2 на реальном CBHI)** (Игорь, 17–18.06: «PWM куча ложноположительных», «нейронка НЕ идеально — на CBHI находит три»). Причины (измерены через движок): `scoreGate=4` ниже медианы случайных пар; objective награждал АБСОЛЮТНУЮ длину ORF (у случайной ДНК ORF длинные и без сплайсинга) → любая пара набирала «бесплатный» ORF-бонус; декодер выбирал акцептор по splice-скору → на CBHI брал преждевременный внутренний AG. **✅ ИСПРАВЛЕНО** (DEC-SPLICE-03, ultracode-воркфлоу: 4 стратегии + синтез + adversarial-recall-ревью, затем эмпирический подбор на CBHI/random/демо): `gene-model.js` — (1) **ORF-ведомый выбор** (DP по `splice + ORF-gain` → чинит границы акцептора), (2) **combined-site gate=14** (сильные канонические сайты — чего нет у случайной ДНК), (3) **относительный null-гейт** (сплайсинг обязан удлинить рамку: `gain<1 → []`; относительный, не абсолютный — не рубит мелкие гены/Phase-1 юнит с gain=4). `maxIntron` оставлен 50000 (не режу крупные интроны — претензия adversary). Annotator: **CNN — дефолт** с авто-фолбэком на PWM для коротких <220 п.н.; анализ всегда по ПОЛНОЙ последовательности (полный ORF/CNN-контекст), выделение лишь скоупит вывод. Результат (боевой код): CBHI CNN **3→ровно 2** верных границ; random 31/80 (PWM) и 6/18 (CNN) **→0**; демо-ген 2/2 цел. Тест `splice-precision.test.js` (CBHI-эталон + random→0).
- [x] **V149 — детекция: плотные мульти-интронные гены сливались в один (A. niger glaA: 4 интрона → находило 3, границы съедены)** (Игорь, 18.06). Жадный per-intron декодер не собирает плотные короткие интроны (связная рамка только всем набором), а vertebrate-CNN не видит грибные акцепторы. **✅ ИСПРАВЛЕНО** (DEC-SPLICE-04): построен **frame-aware gene-parser** (`lib/splice/gene-parser.js`, DP по рамке, content-first — максимум чистой кодирующей CDS через GT-AG интроны) + `ORGANISM_PRESETS` (простые группы) + дропдаун организма в Annotator. glaA **4/4 интрона, 3/4 границ точно**; CBHI 2/2. Запуск на ВЫДЕЛЕННОМ гене. Предел специфичности (отсев некодирующей ДНК) → hexamer-content-модель = опц. большой этап. Тест `gene-parser.test.js`.
- [ ] **V152 — paste (Ctrl+V) не работает в editable SequenceView** (Игорь, 18.06, заметил в режиме сборки — «не работает контрол М», М на рус.раскладке = физическая V). Корень: paste подключён ТОЛЬКО через `onPaste` DOM-событие на корневом `<div tabIndex={0}>` (SequenceView/index.jsx:1033), но div **НЕ contentEditable** → браузер не шлёт `paste`-событие на non-editable элемент → `onRootPaste` мёртв; Ctrl+V-keydown обработчика нет вообще. Не специфично для сборки — paste не работал ни в одном editable SequenceView. **✅ ИСПРАВЛЕНО:** добавлена Ctrl+V (`e.code==='KeyV'`, layout-independent) ветка в `useSequenceKeyboard` — `navigator.clipboard.readText()` → `buildSequencePasteOp` → `onSequenceEdit`, gated на `editable`+`onSequenceEdit` (как copy). `onRootPaste` оставлен как fallback. Чинит сборку/Library/align. TDD `use-sequence-keyboard-edit.test.js`.
- [x] **V151 — ручная правка сиквенса → AA-трек выдаёт «XXXXX» вместо аминокислот** (Игорь, 18.06). Две причины: (1) `useSequenceKeyboard.js` вставлял `e.key` КАК ЕСТЬ — печать базы без Shift даёт СТРОЧНУЮ букву (`IUPAC_RE` принимает и строчные) → рабочая копия получает строчные `acgt`; (2) `walkCodons`/`walkSplicedRegion`/`pickSplicedFrame` (codon-walker.js) делают `CODON_TABLE[codon] || 'X'` БЕЗ `.toUpperCase()` (в отличие от `translateDNA`) → строчный кодон не в таблице → `X`. Прогон строчных = «XXXXX». Строчные ломали и детекцию несовпадений выравнивания (`basesCompatible` для строчных уходит в `expand()`-fallback → трактует как N → совпадает с чем угодно). **✅ ИСПРАВЛЕНО:** (root) `useSequenceKeyboard` нормализует набранный символ в верхний регистр (`key.toUpperCase()`) → рабочая копия канонична для ВСЕХ потребителей (AA-трек, выравнивание, save-diff); (defense) `walkCodons`/`walkSplicedRegion`/`pickSplicedFrame` делают `.toUpperCase()` на входе (как `translateDNA`) → AA-трек устойчив к любому строчному источнику (импорт soft-masked и т.п.). TDD: keyboard `'a'→insert 'A'`/`'g'→replace 'G'`; codon-walker `atggcgaaa→MAK`, lowercase spliced→`MAK*` без X. Браузер (cache-buster, dev-сервер кешировал старый модуль): lowercase+mixed→`MAK`. Полный прогон 5161 pass / 0 fail, build чист. Прочие `CODON_TABLE[]`-lookup'ы (mutagenesis/codon-optimize/sequence-diff/feature-match/plasmid-git) работают над хранимыми (канонически заглавными) последовательностями — root-фикс держит их корректными, в скоуп не входят.
- [x] **REV#2-S2-P1-A (✅ ИСПРАВЛЕНО 14.07, тест ловит дефект) — композитный entityKey (§10.4) не дошёл до worker-канала** (ревью Игоря 14.07, воспроизводимо). UI/runSearch перешли на `<kind>:<id>` (K1), но `toWorkerDocs` (`search-worker-client.js:55`) шлёт `{id: d.ref.id}` (голый), `searchAllSequences` ключует `byId[d.id]`, facade seqMatch читает `byId.get(doc.ref.id)` (`search-facade.js:61`). Молекула и праймер с одинаковым локальным id → seq-хит одного присваивается обоим. Фикс: worker doc.id = `entityRefKey(ref)`; facade lookup `byId.get(entityRefKey(doc.ref))`. TDD: entry+primer с одним id → хит только своему.
- [x] **REV#2-S2-P1-B (✅ ИСПРАВЛЕНО 14.07, тест ловит дефект) — дедуп legacy-праймера читает не то поле + seen-set не растёт** (ревью Игоря 14.07). `collectPrimerDocuments` читал `resourceHash`/`origin.resourceHash`, но реальная legacy-запись хранит хеш в `payload.resourceHash` → мост не дедупит; плюс poolHashes/poolIds не пополняются после добавления моста → два legacy-дубля оба проходят. Фикс: `hashOf = resourceHash ?? payload.resourceHash ?? origin.resourceHash`; после push добавлять hash+id в seen-наборы. TDD: payload-форма + два legacy-дубля → один док.
- [x] **REV#2-S2-P1-C (✅ ИСПРАВЛЕНО 14.07, тест ловит дефект) — `docType` неверно классифицирует project/primer** (ревью Игоря 14.07). `docType` (`library-search.js:48`) смотрит top-level `doc.kind`, которого у project/primer-доков нет → project падает в `linear` (`type:linear` его ловит), `type:primer` не находит канонический пул-праймер. Фикс: `ref.kind` первым (primer→primer, project→project — нет TYPE_SYNONYM → type-фильтры не ловят), иначе topology. TDD: type:primer→пул-праймер; type:linear НЕ ловит project.
- [x] **REV#2-S2-P1-D (✅ ИСПРАВЛЕНО 14.07, тест ловит дефект) — scope применяется до ранжирования, но ПОСЛЕ DNA-worker** (ревью Игоря 14.07, §10.3). Финальные строки фильтруются, но facade шлёт ПОЛНЫЙ `documents` в worker (`search-facade.js:48`) → `mol: seq:` сканирует праймеры, `primer: seq:` — молекулы (лишний worker-риск timeout/incomplete). Фикс: facade `filterDocumentsByScope(documents, plan.entityScope)` ДО обеих фаз + worker (закрывает и req 7 same-scope). TDD: mol: seq: → worker не получает primer-док.
- [x] **REV#2-S2-P1-E (✅ ИСПРАВЛЕНО 14.07, тест ловит дефект) — readiness реестра отстал от реализации** (ревью Игоря 14.07). enz/mol/primer/project исполняются после Stage 2 (enz=enzymeCatalog routing, mol/primer/project=scope narrowing), но `EXECUTABLE_PREFIXES` всё ещё parse-only → лишняя `prefix-parse-only` диагностика + не попадут в selector. Фикс: EXECUTABLE_PREFIXES += enz/mol/primer/project (name/feature/in остаются parse-only — движок их fieldClause не потребляет). Обновить registry-тест executable-set.
- [x] **REV#2-S2-K6-P1-1 (✅ ИСПРАВЛЕНО 14.07) — entry-pick обходит штатный выбор молекулы** (ревью Игоря 14.07, LibraryWorkspace sink). Прямой `setSelectedId(id)` пропускает `guardedSelect` (unsaved-changes подтверждение, переключение common→entry view, инициализацию perEntryState); nav-occurrence может остаться отложенным и сработать позже. Фикс: маршрутизировать entry через `guardedSelect`, парковать nav после успешного select.
- [x] **REV#2-S2-K6-P1-2 (✅ ИСПРАВЛЕНО 14.07, ЗАКРЫТО В ROUND-2) — primer-pick ведёт на пустой экран** (ревью Игоря 14.07). (a) архивный праймер найден глобально, но скрыт фильтром `active` пула → force-show выбранного; (b) legacy-only праймер есть в search-doc (bridge collectPrimerDocuments), но отсутствует в `primersById`. **Round-1 закрыл только (a); (b) де-факто открывал legacy как Library entry — расходилось с §10.2/§10.4 (primer → канонический пул, НЕ molecular inspector) + fallback проверял только RAW id → коллизия `primer:x` могла открыть молекулу `entry:x`.** Round-2 фикс (useSearchPickRouter openPrimer): legacy-only праймер КАНОНИЗИРУЕТСЯ в пул через `addPrimerToPool` (state-set синхронный до первого await → строка есть к моменту навигации), затем открывается пул; guard `legacy.kind === 'primer'` закрывает коллизию (id совпал с молекулой → no-op, НЕ открывает молекулу).
- [x] **REV#2-S2-K6-P1-3 (✅ ИСПРАВЛЕНО 14.07) — fail-open в entry-маршрутизации** (ревью Игоря 14.07). search-result-vm + LibraryTopBar подставляют `kind='entry'` при потере типа (`refKind ?? 'entry'`, `vmRow?.refKind || 'entry'`) → отменяет fail-closed чистого роутера (resolveSearchPick без kind → none). Фикс: передавать полный исходный `entityRef` (result.entityRef), без kind результат НЕ маршрутизируется.
- [x] **REV#2-S2-K6-P1-4 (✅ ИСПРАВЛЕНО 14.07) — cut-запрос с пробелами в имени фермента ломается** (ревью Игоря 14.07). EnzymeCard scan: `setQuery('cut:'+name)` → `My Enzyme` даёт невалидный `cut:My Enzyme`. Фикс: канонический сериализатор (quoteValue) → `cut:"My Enzyme"` с экранированием.
- [x] **REV#2-S2-K6-P2 (✅ ИСПРАВЛЕНО 14.07) — EnzymeCard/иконки (ревью Игоря 14.07, UX).** EnzymeCard=role dialog без focus mgmt/Escape/focus-return → сделать non-modal `region` ИЛИ полный dialog-контракт; `✕`/`✂️` → `Icon close/restriction`; `var(--font-mono)`+shadow-токен; containing block для absolute-карточки. SmartResultRow всегда рисует ДНК-иконку даже для project/primer/enzyme → kind-aware иконки.
- [x] **REV#2-S2-K6-P1-2b (✅ ИСПРАВЛЕНО 14.07, round-3) — миграция legacy-праймера РАЗРУШИТЕЛЬНО теряла данные.** Round-2 фикс переносил в пул только `id/name/sequence/tags/resourceHash/projectId`. Терялись: `tm`, `length`, `direction`, `bindingSequence`, `tail`, `status`, `origin`, `addedAt`, `origin.resourceHash`. Хуже: `addPrimerToPool` дефолт `status='imported'` — **валидный enum**, поэтому в `normalizePrimer` он ПОБЕЖДАЛ `input.status`, если не передать `status` отдельным аргументом. Итог: клик по legacy-праймеру `Tm=58.2 · direction=reverse · status=ordered` → запись `Tm=null`, без направления, `status=imported`. Фикс: (1) чистый helper `lib/legacy-primer-migrate.js` `legacyPrimerToCanonical(entry)` (top-level + `payload` + `origin` формы, зеркалит `primerToDocument`/`hashOf`); (2) передача `status` и `origin` ОТДЕЛЬНЫМИ аргументами `addPrimerToPool`; (3) интеграционный red-тест на РЕАЛЬНОМ store (все поля / праймер в `primersById` в момент `setActiveWorkspace` / `origin.resourceHash` / нет открытия молекулы при коллизии id). **Адверсариальный verify (3 скептика) закрыл ordering+regression с high-confidence и нашёл 1 остаток (P2): `description` — searchable dimension (`primerToDocument.textFields.description`) — не переносился, т.к. и helper, и `normalizePrimer` его не держали. Дозакрыто: `description` добавлен в `normalizePrimer` (аддитивно, migration-safe) + в helper + тесты. Теперь миграция буквально lossless.**
- [x] **REV#2-S2-K6-P2b (✅ ИСПРАВЛЕНО 14.07, round-3) — EnzymeCard: невидимый фокус.** Карточка получает программный фокус (`ref.focus()`), но `outline: 'none'` в inline-стиле подавлял глобальный `:focus-visible` → клавиатурный пользователь не видел положение фокуса. Фикс: убран `outline: 'none'`.
- [x] **REV#2-S2-K6-DS (✅ ИСПРАВЛЕНО 14.07, round-2) — design-system мелочи (ревью Игоря 14.07).** (a) PrimerPoolList рамка выбранного праймера использовала несуществующий токен `var(--accent, #6366f1)` (фиолетовый fallback вне палитры) → `var(--accent-500)`. (b) SmartResultRow ещё держал emoji `🧬`/`✂️` в protein/enzyme-explain строках где рядом уже системные иконки → заменены на `Icon dna`/`Icon restriction` (DESIGN_SYSTEM: no emoji in chrome). Плюс бэкфилл тест-матрицы round-2: реальный entry-sink (makeOpenEntry: dirty-guard cancel / common→entry+activateProject+perEntryState / nav-после-select / no-occ / missing-entry), legacy-primer→пул + коллизия primer:x↔entry:x (useSearchPickRouter), archived-force-visible + scrollIntoView + late-hydration (PrimerPoolList), workspace.context.selectedPrimerId passthrough (PrimerPoolWorkspace), EnzymeCard focus-on-open + Escape, kind-aware иконки (SmartResultRow). Полный прогон 6779 pass / 0 fail, build ok, scoped lint 0 errors.
- [x] **REV#2-S2-K7-GATE (✅ ИСПРАВЛЕНО 14.07) — fail-closed до коллекции/провайдеров.** `planIsBlocked/planBlockingErrors` (любая `severity:error`); `facade.search` короткозамыкает ДО scope/worker/providers; `enz:Bsa seq:GAATTC` → worker `built===0`, postMessage не зван; LibraryTopBar не собирает каталог для blocked-плана + баннер. (Остаток P1-K7-4 по инвалидации — ниже.)
- [x] **REV#2-S2-K7-P1-1 (✅ ИСПРАВЛЕНО 14.07, round-2) — `type:`→scope зависел от ПОРЯДКА.** `query-classify.js` брал только ПЕРВЫЙ type-clause: `type:primer type:circular`→`[primer]`, `type:circular type:primer`→`[entry]`. Фикс: цикл по ВСЕМ `type:` fieldClauses, ИНТЕРСЕКЦИЯ typeScope каждого → оба порядка дают `[]` (order-independent); неизвестный type не сужает.
- [x] **REV#2-S2-K7-P1-2 (✅ ИСПРАВЛЕНО 14.07, round-2) — нестрогая нормализация explicit `type:`.** Префиксные регулярки давали `type:primerjunk`→`primer`, `type:linearity`→`linear`. Фикс: `TYPE_EXACT`/`STATUS_EXACT` (Map, whole-value) + `normalizeExactEnum` для ЯВНЫХ фильтров; префиксное `matchSynonym` осталось только для свободного текста (`inferType`/`inferStatus`). RU-формы (`кольцевая`/`верифицированная`) в exact-таблицах; junk → не нормализуется → fail-closed.
- [x] **REV#2-S2-K7-P1-3 (✅ ИСПРАВЛЕНО 14.07, round-2) — ledger не охватывал весь QueryPlan.** Читал только `explicitFilters`+`textTerms`: `enz:Bsa`→`unconsumable:false`; `name:`/`feature:`/`in:` обходили ledger. Фикс: `planHasUnconsumableClause(plan)` покрывает provider-dims в explicitFilters + structured fieldClauses `∉{type,status,tag}` (name/feature/withinProject) + `enzymeCatalogQuery`; `planRequiresFullSearch` и `evaluateEntryClauses` теперь оба через него → каждый неисполнимый clause → `unconsumable`+`REQUIRES_FULL_SEARCH`.
- [x] **REV#2-S2-K7-P1-4 (✅ ИСПРАВЛЕНО 14.07, round-2) — blocked-запрос не инвалидировал предыдущую сессию.** `A:partial → B:blocked → A:final` с `A.stale=false`: blocked-ветка обходила service. Фикс: blocked-ветка (search-facade.js) вызывает `service.cancel()` (latest=null → поздний A stale-dropped). Тесты: facade `latestRequestId===null` после blocked + `A(in-flight)→B(blocked)` A.stale=true / A-final не доставлен; TopBar A(seq)→B(blocked) — notice, A не залипает.
- [x] **REV#2-S2-K7-P1-5 (✅ ИСПРАВЛЕНО 14.07, round-2) — тест порядка ID был вакуумным.** Условие `status.id < type.id || status.id !== type.id` истинно для любых различных ID. Фикс: прямая проверка `status.id==='c0'`, `type.id==='c1'`.
- [x] **REV#2-S2-K7-P1-6 (✅ ИСПРАВЛЕНО 14.07, round-3) — статусы праймеров конфликтовали со статусами entry.** `archived` (primer-статус) нормализовался в `deprecated` (entry-статус) через `STATUS_SYNONYMS`/`STATUS_EXACT` → `status:archived` находил deprecated-**молекулы** вместо архивных праймеров; `primer:T7 status:archived` ничего не находил; `status:архив` так же; bare `archived` тоже. Модели РАЗНЫЕ: entry `release/wip/deprecated`, primer `imported/designed/ordered/received/archived`. Фикс: (1) `archived/архив*→archived`, только `deprecated/устар*→deprecated`; primer-lifecycle статусы — отдельный enum в `STATUS_SYNONYMS`+`STATUS_EXACT`; (2) `STATUS_KIND` (status→entity-kind) + сужение scope по ВСЕМ `status:` (entry-status→[entry], primer-status→[primer]), интерсекция с `type:` и явным scope, оба порядка токенов; (3) несовместимый ЯВНЫЙ scope+status → `[]` + `incompatible-scope-status` (severity:error → блок по K7-gate); (4) свободный `archived` инферит primer-статус. E2E: archived-primer + deprecated-entry → `status:archived` возвращает только праймер. Полная сюита 6824 pass 0 fail.
- [x] **REV#2-S2-K7-P1-7 (✅ ИСПРАВЛЕНО 16.07, S3-CLOSE K1) — комбинированный metadata+provider запрос не требовал реального provider-hit.** Финальный проход теперь строгий AND по обязательным `sequence/protein/enzyme` dimensions; только явный `deferred` сохраняет невыбираемый `providerPending`-кандидат.
- [x] **REV#2-RESILIENCE-P1-BIOFAIL (✅ ИСПРАВЛЕНО 16.07, S3-CLOSE K2) — ошибка inline protein/cut provider не деградировала в честный incomplete-result.** Единый failure tracker доводит DNA/protein/cut сбои до `incompleteDims/providerFailures`; строгий проход при сбое отбрасывается и повторяется как `deferred`, без вечного partial и ложного нуля.
- [x] **REV#2-CONTRACT-P2-TYPES (обнаружено 14.07 при фактологической ревизии документации) — `search-types.js` отстал от runtime-контрактов.** `SearchDocument` не описывает `features/topology/kind/projectId`, `SearchMatch.dimension` — `enzyme`, `SearchSession.status` — `partial/blocked`; `SearchLocation` требует отсутствующий в provider output `targetKey`, а `ProviderOccurrence` не отделён от обогащённого `SearchOccurrence` с `targetRef`. Runtime работает по producer/consumer tests, но typedef вводит будущие правки в заблуждение. Нужна TDD/contract-синхронизация без изменения runtime shape. **✅ ИСПРАВЛЕНО** в S3-CLOSE K3.3 (принят 16.07): `search-types.js` приведён к runtime — `SearchDocument` +`features/topology/kind/projectId`, `SearchMatch.dimension` +`enzyme`, `SearchSession.status` = `partial|done|blocked`, `SearchLocation` −`targetKey` (его не ставил ни один продюсер), введён `ProviderOccurrence` (без `targetRef`) отдельно от обогащённого `SearchOccurrence` (`targetRef` обязателен, `location` опционален). Runtime shape не менялся — только JSDoc. Контракт защищён статически: `search-types-contract.test.js` (26 тестов) читает исходник через `?raw` и проверяет каждое поле по отдельности, поэтому удаление любого поля роняет ровно один тест, а не снапшот целиком. Отметка `[x]` поставлена 17.07 — в отчёте K3 пункт был пропущен (отмечены только `S3CK3-P1-1..3` / `S3CK3-P2-1`).
- [x] **V150 — после анализа CBHI: ген монолитный (без экзон/интрон разбивки) + стартовый M проигнорирован (рамка сдвинута на 2)** (Игорь, 18.06, скрин). Один корень: `createAnnotation` (через который идёт `create-batch` apply результатов аннатора) **всегда регенерировала `id`**, игнорируя явный. При apply ген получал НОВЫЙ id ≠ `geneId`, а интроны хранили `regionId = geneId` (старый) → связь рвалась. `getIntronsForRegion`: при заданном `regionId` идёт link-путём (не совпало → интрон исключён, containment-fallback не срабатывает). Итог: нет intron-детей → монолит (`GeneExonRects` не триггерится); нет сплайса → непрерывная трансляция, стопы интронов в рамке 0 → `pickSplicedFrame` уходит в др. рамку → M теряется. Вскрыто сменой region→detail (DEC-SPLICE-AA-09). **✅ ИСПРАВЛЕНО** `annotation-edit.js`: `createAnnotation` сохраняет явный `id` (`id != null ? id : generateAnnotationId(...)`) → кросс-ссылки ген↔интрон переживают apply. Браузер-верификация (реальный конвейер на CBHI в билде): linkedIntrons 2, geneIdPreserved true, protein «MYRKLA», 0 внутр. стопов. Тест `annotation-edit.test.js` (create-batch сохраняет связь). **Примечание:** старые «сломанные» аннотации в проекте надо удалить и пере-запустить анализ (dup-гейт пропустит перекрывающий свежий ген).

---

## Тест-инфра

- [ ] **FLAKE-1TEST (интермиттент, не воспроизводится) — полный `npm test` иногда даёт «1 failed | 6199 passed», но на ПОВТОРНОМ прогоне 0 failed.** Замечено 3× за сессию 29.06 (после FEAT-TOOLTIP / FEAT-CF-SHARE / FEAT-QUALIFIERS — но изменения были чистыми функциями без async/timing, т.е. флейк предсуществует). Один отказавший тест, имя НЕ поймано (никогда не падает под изоляцию/capture — timing-sensitive под полной конкуренцией ~6200 тестов). Отлично от V146 (тот = align lazy-чанк, пофикшен). **Правило: единичное «1 failed» в полном прогоне → перепрогнать; если 0 failed → это FLAKE-1TEST, не регрессия.** Поймать имя: запускать `vitest --reporter=verbose --no-file-parallelism` или `--retry=0 --bail=1` несколько раз. Низкий приоритет (зелёный на ре-ране), но раздражает чек-пойнты.

- [x] **V146 — `align-routing.test.jsx` флакает (Suspense lazy-чанк AlignWorkspace).** ИСПРАВЛЕНО 18.06.2026: `findByTestId('align-workspace')` таймаут 1000→**5000мс** — `React.lazy(() => import('./AlignWorkspace'))` под нагрузкой vite-node не успевал резолвиться за 1 с (DOM застревал на «Загрузка…»). Это рекомендованный здесь же warm-up-альтернативой фикс; **не контент-баг** (прод-сборка/живой воркспейс работали). Подтверждено: изоляция падала 3/3 при 1000мс → проходит при 5000мс; тест на ПУСТОМ воркспейсе (`AlignReferenceView` не на пути PERF-8). Полный прогон 5157 pass / 0 fail.

---

## OPEN

### Высокие

- [ ] **BODGE-PUB-01 (privacy/export, подтверждено аудитом 13.07.2026) — `public-supp` не применяет строгий профиль внутри writer.** `bodge-export-profiles.js` описывает профиль, но `bodge-zip.js` в живом public path очищает главным образом `manifest.deviceId`, а runtime attachment/blob maps могут быть подмешаны обратно. Риск: notebook, local refs, source/evidence bytes или extension snapshot способны попасть в публичный архив. **План:** allowlist-first closure по выбранному product, render только из filtered snapshot, post-write recursive leak scan. Источники реализации: `docs/SPEC_REPRODUCIBLE_RECIPE.md` §6/§11–13 и `docs/BODGE_V2_IMPLEMENTATION_PLAN.md` F1–F8/G0–G4. Не чинить внутри M-CANVAS-FIX.1.

- [ ] **BODGE-CORE-02 (data integrity, подтверждено аудитом 13.07.2026) — skeleton extension остаётся скрытым вторым source of truth.** `skeleton-bodge-bridge.js` сохраняет полный snapshot и при read предпочитает его structured sections; поэтому внешне изменённый/отфильтрованный canonical container или assembly может быть перекрыт старой копией, а structured round-trip выглядит lossless только внутри BodgeGene. **План:** biology только из canonical project/container/assembly DTO; extension — исключительно optional UI/layout, без override path. Источник: `docs/BODGE_V2_IMPLEMENTATION_PLAN.md` B1–B7. Не чинить внутри M-CANVAS-FIX.1.

- [x] **V186 (RE-визуализация, найдено аудитом V183/V184) — кольцевая Part: сайт через origin не виден в сиквенс-вью. ✅ ИСПРАВЛЕНО 29.06.2026.** Из адверсариального аудита RE-cloning пайплайна. `PlasmidViewer.jsx:88` звал `scanAllSites(seq)` БЕЗ `circular` → дефолт false → wrap-расширение (restriction-db.js L13) не срабатывало → RE-сайт, пересекающий origin кольцевой плазмиды (напр. ApaI GGGCCC: последний G + первые GGCCC), невидим в нижнем сиквенс-вью, хотя кольцевая карта слева его показывает. Точно класс жалобы Игоря «его не видно таки». **Фикс:** `scanAllSites(seq, { circular: part.topology === 'circular', minSiteLen: 6 })` (паттерн как PlasmidMapV2:134 / LinearMapV2:88 / file-summary:72) + `part.topology` в deps useMemo. TDD PlasmidViewer.test.jsx +2 (кольцо → сайт виден; линейный → нет фантома) 5/5. Якорь: PlasmidViewer.jsx reCutMap.

- [x] **V188 (align, найдено аудитом) — RE-сайт через origin не виден при выравнивании на КОЛЬЦЕВОЙ референс. ✅ ИСПРАВЛЕНО 29.06.2026.** `SequenceView/index.jsx:508` сканирует `scanAllSites(fullSeq, { circular, ... })`, но `circular` (проп, дефолт false) НЕ прокидывался до референс-фрагмента в Align → биолог гонит Sanger-read на кольцевую плазмиду, origin-straddling RE-сайт на референсе невидим в RestrictionTrack. **Фикс (TDD, 4 файла, прокидка topology):** `alignmentSlice.normalizeInput` несёт `circular` (из явного флага ИЛИ `topology==='circular'`); `toInputFromEntry` читает `entry.payload.topology==='circular'`; `AlignResultView` строит referenceFragment с `circular` (4 ветки: multi/single × editing/source из refInput/aInput); `AlignReferenceView` прокидывает `circular={!!referenceFragment.circular}` в SequenceView (уже потребляет проп для скана). Тот же класс/корень, что V186 («передать topology в scanAllSites»). TDD: alignmentSlice +2 (setAlignInputs circular/topology; openAlignmentWith payload.topology), align-reference-view-memo +2 (svProps.circular true/false). Якоря: alignmentSlice.js normalizeInput/toInputFromEntry, AlignResultView.jsx, AlignReferenceView.jsx.

- [ ] **V187 (RE-лигирование, найдено аудитом — НУЖНА проверка конвенции) — `executeLigate(ends:'sticky')` триммит шов по случайной 1-6bp гомологии.** `ligate.js:41` зовёт `findOverlap(acc, seqs[i], 6, 1)` (minLen=1!) → ЛЮБОЕ случайное совпадение суффикс/префикс 1-6bp на стыке триммится → продукт теряет базы шва. Для модели «сегмент = top-strand срез между верхними резами» overhang уже учтён один раз → триммить НЕ надо (overlaps=0, как blunt). **НО:** адверсариальный verify установил, что ОСНОВНОЙ realise-путь RE-cloning продукт строит НАПРЯМУЮ через `concatSegmentAnnotations` (realiseAssembly), НЕ через executeLigate → этот баг бьёт только РУЧНОЙ canvas-Ligate-оп со sticky. Есть конвенционная неоднозначность: executeLigate (S2-дизайн) мог ожидать overhang на ОБОИХ концах (тогда трим нужен). **Действие:** проверить, в какой конвенции приходят фрагменты в executeLigate (top-cut vs both-ends-overhang) ДО фикса; если top-cut → `ends==='sticky'` тоже overlaps=0. Не фиксить вслепую (bio-инвариант STOP).

- [ ] **V189 (RangePicker, дизайн-вопрос — НЕ баг) — кольцевой source в ЛИНЕЙНОМ виде карты показывает origin-straddling сайт.** `RangePickerModal.jsx:616` в ветке `mapShape==='linear'` передаёт `topology={sourceCircular?'circular':'linear'}` → LinearMapV2 сканирует circular:true → рисует wrap-сайт, что противоречит само-документированному контракту LinearMapV2 («no phantom origin site»). Аудит назвал это багом, НО: сайт на кольцевой молекуле РЕАЛЕН независимо от линейного отображения — показать его = БОЛЬШЕ корректной инфы. Решение за Игорем: (а) скрыть (topology='linear' в линейной ветке, контракт-консистентно) или (б) оставить (реальный сайт). НЕ менял — дизайн-суждение, не корректность.

- [ ] **V-RC-ANNOT-EDGE (минор, найдено аудитом, частично misdiagnosed) — реверснутый RE-фрагмент: аннотация у самого торца ≤overhang(≤4bp) рассинхрон с overhang-aware последовательностью.** V184 `reverseComplementSegment` для ПАЛИНДРОМНОГО overhang сдвигает терминальные overhang-базы; `reflectAnnotations` делает наивное координатное отражение → у фичи, СИДЯЩЕЙ на самом cut-overhang, координаты на ≤4bp разойдутся с последовательностью. Фичи редко на cut-торце → минор. (Агент назвал «non-palindromic», но для непалиндрома `reverseComplementSegment` падает в обычный RC = совпадает с reflectAnnotations → там как раз ОК; реальный край — палиндромный.) Фикс при необходимости: overhang-aware отражение аннотаций. Низкий приоритет.

- [x] **V184 (restriction-cloning, био-корректность) — реверснутый RE-фрагмент терял сайт рестрикции на стыке. ✅ ИСПРАВЛЕНО 29.06.2026 (Игорь, RE-cloning скрины).** «После лигирования не обнаруживается сайт ApaI … он на обратной цепи теперь». Корень: сегменты сборки хранятся как top-strand срез между ВЕРХНИМИ резами — это даёт корректную реконституцию общего overhang РОВНО ОДИН раз при ПРЯМОЙ конкатенации (`segment-overhangs.js` stickyEndExtent). При перевороте фрагмента простой `reverseComplement(topSlice)` НЕ восстанавливает ступеньку липкого конца: физические концы перевёрнутого фрагмента садятся на НИЖНИЕ резы, сдвинутые на overhang с каждой стороны → шов перестаёт писать сайт узнавания (ApaI `GGGCC^C` палиндром: продукт 2→0 сайтов на конкретном примере). Проверено: `orientFragments` для палиндромного кольца НЕ переворачивает по умолчанию → баг бьёт при РУЧНОМ перевороте или направленном клонировании. **Фикс (TDD):** новый чистый хелпер `reverseComplementSegment(segment, reEnzymes)` (segment-overhangs.js) = перевёрнутая top-цепь ЕСТЬ нижняя цепь 5′→3′ = `RC(source[a+δL : b+δR])`, δ=cut[1]-cut[0] (δ<0 3′, δ>0 5′, 0 blunt); выражено через хранимый top-срез + палиндромные overhang-базы (длина сохраняется; blunt/непалиндром → fallback на обычный RC). Подключён в `draftFromZone` в ДВУХ местах: per-range построение (orientation:'reverse') и orientation-нормализация (orientFragments override). Затрагивает и live-DAG-preview, и realiseAssembly (оба через draftFromZone). Сверено для 3′(ApaI)/5′(EcoRI)/blunt(EcoRV). Тесты `ligation-reversed-fragment-site.test.js` (oracle: число ApaI-сайтов в продукте = числу в исходнике, forward И reversed) 5/5. Якоря: segment-overhangs.js reverseComplementSegment, zone-pieces-to-dag.js draftFromZone.

- [x] **V183 (restriction-cloning UX) — продукт лигирования терял фичи + фичу шва. ✅ ИСПРАВЛЕНО 29.06.2026 (Игорь, RE-cloning скрины).** Две жалобы: (1) «потеряна фича стыковки двух фрагментов», (2) «потеряно отображение фичей на собранной плазмиде» (пустое кольцо «Сборка N (плазмида)»). Корень: узел-продукт в live-DAG-preview (`pieces-to-dag-preview.js`, кольцевая ветка) хардкодил `annotations: []`, тогда как realiseAssembly корректно агрегировал через `concatSegmentAnnotations`. Плюс `reflectAnnotations` использовал 0-based формулу RC на 1-based координатах сегмента (сдвиг на 1 bp у каждой реверснутой фичи). **Фикс:** (а) preview-продукт наследует `concatSegmentAnnotations(segs)` + маркер шва `_seam` на каждой границе фрагмента (вкл. замыкание кольца); (б) `reflectAnnotations` → 1-based inclusive RC `[L-e+1, L-s+1]` (как transferAnnotations). TDD `ligation-product-annotations-repro.test.js` 4/4 + full suite зелёный. Якоря: pieces-to-dag-preview.js (узел-продукт), segment-overhangs.js reflectAnnotations.

- [x] **V190 (библиотека, визуальный беспорядок дерева — Игорь, скрин) — строки веток и плазмид «вразнобой». ✅ ИСПРАВЛЕНО 30.06.2026.** Игорь: «в библиотеке как-то вразнобой все вкладки, ветки и плазмиды визуальный беспорядок». Корень (после VTREE-редизайна): `VersionLineageNode.HeadRow` рендерил 12px-шеврон ПЕРЕД мини-иконкой, а обычный `TreeItemRow` колонку раскрытия НЕ резервировал → при одинаковом indent иконка lineage-заголовка стояла на +20px правее простой плазмиды (браузер-замер: head iconLeft 136 vs plain 116) — колонки иконок/имён не сходились. Плюс бейдж версии зигзагил: у заголовка `v{N}` справа, у дочерних версий `v{order}`/⑂ слева. **Фикс (TDD, 2 компонента — обе зоны Loose/Project их переиспользуют):** (A) **правильная сторона выравнивания** — шеврон заголовка линии (`HeadRow`) втянут В зону отступа (`paddingLeft − (TWIST_W+GAP)`), а у простого `TreeItemRow` гаттера НЕТ (иконка прямо на отступе) → head-иконка садится на ту же x, что простая плазмида, БЕЗ лишнего левого поля. (Первая итерация добавляла пустой 12px-гаттер ВСЕМ строкам — это выровняло, но толкнуло простые строки вправо на +20px → Игорь вторым сообщением «слева слишком большое пустое место»; переделано: тянем заголовки ВЛЕВО к простым.) (B) `badge` в `TreeItemRow` перенесён из левого края в правый кластер (перед origin-char) → имена остаются выровнены, чипы версий = одна правая колонка с заголовком; (C) `HeadRow` decluttered как папка (`[шеврон][иконка][имя + «N версий»][статус?][счётчик]`, убран избыточный правый `v{N}`, дублировавший «N версий» + счётчик и создававший зигзаг). **Браузер-замер живьём:** folder-иконка 94, plain==head iconLeft 96 (`head_aligns_plain:true`, притянуто влево с 116), дочерние версии — indent+1 глубже. TDD `tree-row-alignment.test.jsx` (4: нет гаттера у plain + иконка первым ребёнком; head paddingLeft = plain − TWIST_W − GAP; бейдж справа; head без vN-чипа) + регресс дерева 90/0, Library-сьют 492/0. Якоря: TreeItemRow.jsx (нет гаттера + badge справа), VersionLineageNode.jsx HeadRow (chevron-in-indent).

- [x] **V191 (библиотека, Electron) — кнопка «создать папку» (📁+) ничего не делала. ✅ ИСПРАВЛЕНО 30.06.2026 (Игорь).** Корень: `LooseZone.handleCreateFolder` звал `window.prompt('Имя новой папки:')`, но в УПАКОВАННОМ Electron-приложении `window.prompt()` — no-op (возвращает null, хотя `typeof === 'function'`; подтверждено браузер-замером `isElectron:true`) → клик = ранний return, тишина. **Фикс (TDD, Electron-safe):** заменил нативный prompt на ИНЛАЙН-поле ввода под заголовком «Контейнеры» — клик 📁+ открывает `<input autoFocus>`, Enter создаёт папку (санитайз `/`/`:`→`-`, запись в `looseFolders`, авто-раскрытие), Escape/blur отменяет. **Браузер-верифицировано в живом Electron:** клик→поле появляется+автофокус, Escape→закрывается (папка не создаётся). TDD: `loose-zone-create-folder.test.jsx` (4: появление поля; Enter→looseFolders; Escape→отмена; санитайз `A/B:C`→`A-B-C`) + обновлён старый prompt-мок-тест в `library-tree-v2.test.jsx`. Якорь: LooseZone.jsx (creatingFolder + commitNewFolder + input). Связан с V192.

### Средние

- [x] **V192 (Electron, системное — найдено при V191) — ещё 5 `window.prompt`-флоу мертвы в упакованном Electron. ✅ ИСПРАВЛЕНО 30.06.2026 (Игорь «делай что предложил»).** Тот же корень, что V191 (`window.prompt()` = no-op в Electron). Были мертвы: (1) `TreeItemRow.doRename` — переименование записи; (2) `VersionHistoryList` — переименование версии; (3) `EditorWindowShell` — имя контейнера при форке; (4) `ZoneContextMenu` — переименование зоны + (5) правка заметок. **Фикс (TDD, общий примитив):** новый in-app `PromptModal` + ui-slice `requestPrompt(opts)→Promise<string|null>` / `resolvePrompt(value)` (зеркало toast-паттерна — колбэк в state; `createUiSlice(set,get)`). `<PromptModal>` смонтирован один раз в App root (рядом с `<ToastStack>`), overlay zIndex 4000 — перекрывает дерево/инспектор/канвас. Enter (одностроч.)/Ctrl+Enter (многостроч.)→ОК, Escape/overlay/Отмена→null. Все 5 точек: `const next = await requestPrompt({title, defaultValue, multiline?})` (компоненты Library — через `useStore(s=>s.requestPrompt)`; canvas — `useStore.getState().requestPrompt`). Заметки зоны → `multiline:true` (textarea). `window.confirm` РАБОТАЕТ в Electron — не трогал. TDD: `prompt-modal-slice.test.js` (3) + `PromptModal.test.jsx` (6: рендер/seed/Enter/Escape/Отмена/multiline) + обновлены 3 существующих prompt-мок-теста (version-history-list rename, editor-window-shell fork, ZoneContextMenu rename) под `requestPrompt`-стаб. Якоря: store/uiSlice.js (requestPrompt/resolvePrompt), components/PromptModal.jsx (NEW), App.jsx, TreeItemRow/VersionHistoryList/EditorWindowShell/ZoneContextMenu (call-sites). **Браузер-верификация в живом Electron — после полного прогона.**

- [x] **V179 (PRIMER-8, robustness — НЕ воспроизведённый баг) — PrimerBindingSites гидрирует библиотеку на маунте. ✅ 28.06.** Проверка из аудита: `state.libraryEntries` (источник «куда садится») наполняется `hydrateLibrary()` на App-boot (App.jsx:68) и читается реактивно — архитектура верна (ранее в сессии дерево показывало 20+ записей, «куда садится» работало). Браузер-замеры после ~15 reload давали 0/`appReady:null` — артефакт dynamic-import инстанса + multi-reload (консоль чиста, сайдбар рендерится), НЕ продуктовый баг; чинить нечего. **Усиление (defensive, нулевой риск):** PrimerBindingSites сам зовёт `hydrateLibrary()` на маунте (idempotent, как PrimerPoolList/useEntryPrimers для пула) → «куда садится» получает данные даже если пользователь открыл праймер-вид до завершения boot-гидратации (холодный старт/медленный Dexie). TDD: primer-binding-sites + seed `_libraryHydrated:true` (эффект no-op, не стирает сид). Якорь: PrimerBindingSites.jsx.

- [x] **V178 (PRIMER-12) — UPDATE_ASSEMBLY_PRIMER оставлял stale tail при правке sequence. ✅ ИСПРАВЛЕНО 28.06.** Из аудита V174 (кластер мелочей). При ручной правке последовательности assembly-праймера редьюсер `{...p, ...patch}` обновлял `sequence`+`bindingSequence`+`status='edited'`, но старый `tail` оставался (если patch без tail) → PrimerTrack рисовал фантомный overhang, рассинхрон с новой последовательностью. **Фикс** (skeleton-state-assembly.js UPDATE_ASSEMBLY_PRIMER): при `patch.sequence` без явного `tail` → `next.tail = ''` (правка заменяет весь олиго; без tail вся правленая seq = binding, нет overhang); явный tail сохраняется; non-sequence patch (notes/name) tail не трогает. TDD update-assembly-primer-tail.test.js 3/3. **Два других пункта кластера — НЕ баги (проверено по коду):** (1) PrimerOrderPanel collectFromOps экспортит full oligo в order-sheet (TSV/FASTA) — это и есть правильный заказ; binding/tail split в order-листе не нужен. (2) reuse `findCompatiblePrimers` ключуется по `bindingSequence` (primer-reuse.js:55), затем проверяет совместимость хвоста (substring) → re-tailed одинаковый binding МАТЧИТСЯ; агент спутал reuse (binding-keyed, верно) с dedup (`checkPrimerDedup` по resourceHash/full — верно для идентичного олиго). Якорь: skeleton-state-assembly.js:402.

- [x] **V177 (PRIMER-10) — дедуп праймеров не срабатывал вне Importer (resourceHash не вычислялся). ✅ ИСПРАВЛЕНО 28.06.** Из аудита V174. `checkPrimerDedup(hash)` → early-return на пустом hash; пул-праймеры из library-selection (`buildEntryPrimerPayload`) и assembly НЕ имели `resourceHash` (normalizePrimer ставил null) → импорт того же праймера не дедуплицировался против них (кросс-путь дедуп мёртв; только within-import работал, т.к. PrimerWizardStepModal сам считает `computeResourceHash`). Верифицировано: модалка (PrimerWizardStepModal.jsx:53) считает `computeResourceHash({sequence, topology:'linear', ends:{left:'5',right:'3'}})` и ставит на праймер; остальные пути — нет. **Фикс:** единая точка — `addPrimerToPool` (async) считает ТОТ ЖЕ канонический хэш (полный олиго + 5'/3' линейные концы), если `resourceHash` не задан → каждое добавление в пул хэшируется одинаково, дедуп работает кросс-путь. Явный хэш (Importer) сохраняется. crypto недоступен → null (name-only degrade). TDD `primer-dedup-hash.test.js` (стамп sha256, findable через checkPrimerDedup, case-insensitive same-hash, explicit preserved). Якорь: primerSlice.js addPrimerToPool (+import computeResourceHash из components/Library/lib/resource-hash — чистый leaf-util, без цикла).

- [x] **V176 (PRIMER-11) — импорт .dna/.gb роняет встроенные праймеры. ✅ ИСПРАВЛЕНО 28.06.** Из аудита V174. `importFeatures` (import-annotations.js:322) возвращает `{annotations, primers}` (праймеры из `primer_bind`-фич), но `file-import.js` (обе ветки — .dna ~119, .gb/FASTA ~163) брал только `result.annotations`, а `result.primers` отбрасывал; `_metadata` ставился лишь из backend `data.metadata` (праймеров не содержит). `Library/index.jsx` primerCandidates читает `it._metadata.primers` → всегда пусто → PrimerWizardStepModal не показывал праймеры → из .dna праймеры не импортировались. Подтверждено чтением кода. **Фикс:** обе ветки захватывают `result.primers` и кладут в `_metadata.primers` (.dna: мерж с backend-metadata; .gb: `{primers}`; при пустом — `_metadata` остаётся null для back-compat). TDD `file-import-primers.test.js` (GenBank с primer_bind→`_metadata.primers`[1]; без праймеров→null). Якоря: file-import.js parseFile обе ветки.

- [x] **V175 (PRIMER-9) — Gibson primer-pair: executePCR падал «primer не найден» (искал homology-плечо на шаблоне). ✅ ИСПРАВЛЕНО 28.06.** Из аудита V174. `designGibsonPrimerPair` (gibson-primer-design.js) даёт олиго `sequence = homology+anneal` + `homologyLen`, но `GibsonOpPopup.jsx` клал в `payload.sequences` только `{sequence, homologyLen}` без `bindingSequence`/`tail`. `executePCR` (pcr.js, ветка `primerPairId && !autoDesign`) делал `tplSeq.indexOf(seqs[0].sequence)` — полный праймер с homology-плечом (комплементарным СОСЕДНЕМУ фрагменту, не шаблону) → indexOf=-1 → «Forward primer не найден в темплейте», PCR падал. Подтверждено чтением кода (не доверяя haiku-аудиту). **Фикс:** (a) GibsonOpPopup split каждого олиго на `bindingSequence = sequence.slice(homologyLen)` (anneal, на шаблоне) + `tail = sequence.slice(0, homologyLen)` (homology overhang) → канонично для PrimerTrack/binding-поиска; (b) executePCR (single+multi) аннелит по `bindingSequence` (fallback на full seq), ампликон = `fwdTail + templated[fwdbind..revbind] + RC(revTail)` — биологически верный продукт С homology-оверхенгами (раньше даже при успехе терял бы хвосты). Tailless-олиго байт-в-байт как раньше (binding=full, tail=''). TDD `pcr-gibson-primer-pair.test.js` (tailed→ампликон с хвостами, tailless→regression). Якоря: pcr.js executeSingle/MultiTemplatePCR, GibsonOpPopup.jsx:92.

- [x] **V174 (PRIMER-AUDIT) — «дружба» праймерной подсистемы: tail-поле рассинхрон + .bodge роняет binding/tail. ✅ ИСПРАВЛЕНО 28.06.** (Игорь: «прогони всю кодовую базу на дружбу друг с другом».) Кросс-кодовый аудит (ultracode-воркфлоу `wf_005d5ce4`, 31 агент, 6 осей → adversarial-verify). Подтверждённые **реальные** рассогласования, исправленные в этом заходе: **(A) two-name tail field** — PCR-экосистема (local-primer-design/mutagenesis/primer-reuse/exports/PlasmidMap) использует `tailSequence`, assembly-экосистема (primer-derive/PrimerTrack/entry-primers/primerSlice V173) — `tail`. На кросс-границе хвост невидим (PCR-праймер в PrimerTrack не рисовал overhang; assembly-праймер в PlasmidMap/reuse/exports игнорил хвост). **Фикс:** толерантность к обоим именам на кросс-границах — `tailOf()` helper в PrimerTrack (`tail ?? tailSequence`), `primer-reuse`/`exports`/`PlasmidMap` читают `tailSequence ?? tail`, `normalizePrimer` канонизирует `tail` из обоих. **(B) .bodge serialization** — `serializePrimer` (bodge-primers-json.js) роняла `bindingSequence`/`tail`/`direction`/`status` → сохранил проект → перезагрузил → праймер потерял overhang + binding-поиск стал по полному олиго. **Фикс:** сериализуем V173-форму. TDD: bodge round-trip (binding/tail/direction/status/tags), PrimerTrack рисует `tailSequence`-хвост, primer-rc-contract guard. Полный прогон зелёный. **⚠ Ложные срабатывания аудита (НЕ баг, проверено вручную по коду):** «реверс-праймеры double-RC → никогда не матчатся» — оба продьюсера (local-primer-design `revBindRC`, primer-derive `makePrimer`) консистентно хранят `bindingSequence` = собственную 5′→3′ последовательность праймера (= RC участка шаблона); PrimerTrack/scan делают RC один раз → находят участок. Контракт верен; haiku-агенты перепутали направление RC, палиндромные фикстуры маскировали. Добавлен guard-тест с непалиндромным реверсом (ловит будущий неверный «фикс»). **Остаток аудита (отдельные задачи, не блокеры):** Gibson primer split (GibsonOpPopup без bindingSequence/tail), resourceHash-дедуп никогда не срабатывает, .dna import роняет праймеры, assembly-праймеры невидимы binding-поиску, PrimerOrderPanel re-import теряет split, UPDATE_ASSEMBLY_PRIMER stale tail, reuse ключуется по full sequence. Якоря: PrimerTrack.jsx (tailOf), primer-reuse.js, exports.js, PlasmidMap.jsx, primerSlice.js normalizePrimer, bodge-primers-json.js serializePrimer.

- [x] **V173 (PRIMER-7) — праймер из выделения С ХВОСТОМ не отображался на сиквенсе (хвост/binding терялись при сохранении). ✅ ИСПРАВЛЕНО 28.06.** (Игорь, скрин «Новый праймер из выделения» с 5'-tail GGATCC + RE/обвес-чипы). Цепочка create-flow роняла `tail`+`binding`: (1) `SequenceView/index.jsx:1455` onCreate деструктурировал только `{name,sequence,direction}`; (2) `useEntryPrimers.onWritePrimer` принимал только name/sequence/direction; (3) `buildEntryPrimerPayload` писал лишь `sequence` (=tail+binding), без `bindingSequence`/`tail`; (4) `normalizePrimer` (primerSlice) не хранил `bindingSequence`/`tail`; (5) `selectEntryPrimers` отдавал `bindingSequence: p.sequence`. Итог: `PrimerTrack.findHits` искал на шаблоне tail+binding (хвост — оверхенг, его там нет) → **праймер с хвостом не рисовался**. **Фикс:** `tail`+`binding` проведены через все 5 звеньев; `bindingSequence`+`tail` теперь хранятся на праймере (binding fallback = full seq для tail-less/legacy). PrimerTrack уже tail-aware (рисует хвост по `hit.tail` отдельным сегментом) — теперь данные доходят. TDD: `primer-tail-persist.test.js` (buildEntryPrimerPayload+normalizePrimer+selectEntryPrimers хранят tail/binding), `entry-primers.test.js` обновлён под расширенную форму, `primer-track-tail.test.jsx` (рендер хвоста, уже был). Полный регресс **6040 passed**, build ✓. Якоря: SequenceView/index.jsx:1455, useEntryPrimers.js, entry-primers.js (buildEntryPrimerPayload/selectEntryPrimers), primerSlice.js normalizePrimer. **Браузер живого жеста (выделение→контекст-меню→модалка→Создать→рендер) отложен:** записи библиотеки в этой сессии не в `state.libraryEntries` (дерево читает Dexie), жест по SVG-выделению хрупок; рендер хвоста доказан `primer-track-tail` + персист — `primer-tail-persist`.

- [x] **V170 (F1) — junctionInterlock: непалиндромный overhang давал ЛОЖНЫЙ `compatible` (аудит assembly-logic-audit `wf_cf2017e5-072`, 28.06). ✅ ИСПРАВЛЕНО 28.06.** `junctionInterlock` (`segment-overhangs.js:199`) сравнивал overhang'и string-equality как прокси reverse-комплементарности — это верно ТОЛЬКО для палиндромных концов (классический RE-набор: AATT==RC(AATT)). Для ~6 коммерческих непалиндромных каттеров (BauI 5′ ACGA, BseYI 5′ CCAG, BbvCI, GsaI, Bst2BI) два фрагмента с ОДИНАКОВЫМ 5′ ACGA помечались `compatible`, хотя идентичные непалиндромные 5′-выступы НЕ отжигаются (нужен 5′ TCGT). Модель хранит ОДИН overhang на фермент (палиндромное допущение, нет различения upstream/downstream-половины), поэтому RC-сравнение дало бы уверенно неверный ответ → честно вернуть `unknown` (ручная проверка), зеркало вырожденного RC-BIO-4. **Фикс (RC-BIO-5):** добавлен `import { reverseComplement }` + `palindromic(seq)` чек ПОСЛЕ degenerate-чека, ДО string-equality: любой непалиндромный конец → `unknown` с сообщением «Непалиндромный липкий конец … — совместимость не проверяется автоматически». Комментарий L148-149 («все RE-overhang палиндромны») исправлен. Палиндромный путь не тронут (AATT/TGCA/GATC → как раньше). TDD: `junction-interlock-nonpalindrome.test.js` (7: палиндром-регресс ✓, ACGA→unknown, односторонний→unknown, RC-пара→unknown, полярность-mismatch→incompatible, blunt→blunt). Якорь: `segment-overhangs.js:199`.

- [x] **V172 (GAP-5/P1) — orientFragments: жадный солвер промахивался на замыкаемом N≥3 кольце. ✅ ИСПРАВЛЕНО 28.06.** Старый `orientFragments` (`segment-overhangs.js`) якорил seg0 на stored-ориентации и жадно ориентировал каждый следующий под предыдущий — пропускал кольцо N≥3, чьё ЕДИНСТВЕННОЕ валидное замыкание требует перевёрнутого seg0. Конкретный кейс (палиндромные 5′, выведен вручную): f0=EcoRI\|BamHI (AATT,GATC), f1=EcoRI\|HindIII (AATT,AGCT), f2=HindIII\|BamHI (AGCT,GATC) — замыкается ТОЛЬКО как f0 rc + f1/f2 fwd (AATT~AATT, AGCT~AGCT, GATC~GATC); жадный (seg0 fwd) не находил мэйта на f1 → `closes:false`. **Фикс:** бэктрекинг-солвер, STORED-orientation-first (валидная раскладка сохраняется, без лишних флипов; провабли достаточен для палиндромной модели — для i≥1 у жадного выбор вынужден, seg0 — единственная свободная переменная, которую DFS теперь перебирает), с откатом к жадному при отсутствии полного решения (линейные/смешанные/неразмыкаемые — байт-в-байт без изменений). Перф: pruned DFS ~O(n) на практике (мэйт ветвит ~1), кап n≤16. TDD: `orient-solver-n3-v172` (5: N≥3 с флипом seg0 закрывается; all-fwd кольцо сохраняет seg0 fwd; неразмыкаемое→false; 2-фраг регресс; линейная цепь). Якорь: `segment-overhangs.js` (greedyAssign + DFS + `reversed = solution || greedyAssign()`).

- [x] **V171 (F2) — realiseAssembly: closure-метод не гейтился по топологии (defense-in-depth, аудит `wf_cf2017e5-072`). ✅ ИСПРАВЛЕНО 28.06.** `zone-pieces-to-dag.js` closure-пути брали `draft.assemblyMethod`/`perBoundaryMethods` без валидации: метод = `overlap_pcr` (ЛИНЕЙНЫЙ продукт, не кольцо) или `kld` при >1 фрагменте (KLD = self-closure ОДНОГО фрагмента) → биологически неверный closure проходил при обходе UI (CircularizeModal гейтит, бэкенд нет). **Тот же defense-in-depth, что V169 follow-up.** **Фикс:** хелпер `guardClosureMethod(m,n)` (`overlap_pcr→gibson`; `kld && n>1→gibson`; остальное как есть — **важно: `direct_ligation`/blunt СОХРАНЯЕТСЯ**, blunt-лигирование — валидное замыкание, поэтому точечный transform двух невалидных случаев, НЕ проверка членства в CLOSURE_METHODS, которая ложно срезала бы blunt) применён в 3 точках: assembly-op `repMethod` (только при circular), self-closure `closeMethod` (n=1), closure-junction `method` (n=fragCount). TDD: `closure-method-guard-v171` (6: overlap_pcr→gibson, kld-multi→gibson, gibson/restriction/direct_ligation сохраняются, 1-frag self overlap→gibson/kld остаётся) + C5-регресс зелёный. Якорь: `zone-pieces-to-dag.js` (guardClosureMethod + 3 closure-сайта).

- [x] **V169 — DAG-превью рисует валидную Gibson→кольцевую плазмиду для биологически НЕВОЗМОЖНОЙ сборки (Игорь 26.06, скрин: 2 RE-фрагмента → Gibson → «Сборка 4 (плазмида) 1202 bp»). ✅ ИСПРАВЛЕНО 26.06.** Адверсариальный 4-агентный воркфлоу + перепроверка кодом. **Фикс в `derivePiecesToGraph` (pieces-to-dag-preview.js):** (A) **метод следует химии фрагментов** — `allRE` (все сегменты `acquisitionMethod==='restriction'`) → `closeMethod='restriction'` (op-kind `ligate`, RE-лигирование), причём **ПЕРЕБИВАЕТ** неприменимый явный gibson/golden_gate: классические RE-фрагменты (NsiI/AjiI) нельзя собрать Gibson (нет гомологии) / Golden Gate (нет Type IIS) — единственный метод лигирование (live Сборка 4 имела golden_gate, маскировавший несовместимость, т.к. GG/Gibson «переделывают концы» и гейт V161 на них не блокирует). Mixed-сборка (не all-RE) по-прежнему чтит явный метод. (B) **гейт совместимости** — превью пересчитывает тот же `incompatible`, что блокирует Realise: `segmentOverhangs(seg, RE_ENZYMES)` ×N → `junctionInterlock(a.right,b.left)` по парам + closure (last→first) → `assemblyReadiness(zonesLite, closure)`; при `incompatible>0` продукт помечается `_virtualState:'disconnected'` + `_virtualWarnings:[reason]` (из `assemblyJunctionConflicts`), op `status:'failed'`. (C) **проводка** — `ZoneGraphContent` пробрасывает `_virtualState/_virtualWarnings` в `ContainerBlock` → продукт рисуется через `VirtualBlock` «disconnected» (красное разорванное кольцо + причина), не запечатанная плазмида. Браузер-верифицировано (Сборка 4): Gibson/Golden Gate исчезли, op «Ligate!» (failed), продукт disconnected «Несовместимые липкие концы: 3′ TGCA ≠ тупой». Тесты: pieces-to-dag-preview +3 (RE→ligate-метод, compatible→не блокирован, incompatible EcoRI×SalI→disconnected+failed). Переиспользованы V160/V161/V166-хелперы (interlock/readiness/method) — не переписаны. **⚠ FOLLOW-UP (отдельный заход, не блокер):** воркфлоу нашёл, что сам `realiseAssembly` (zone-pieces-to-dag.js) НЕ гейтит совместимость (слепой `parts.join('')`); единственный гард — UI (`AssemblyShellBody.onRealise` bail + `canRealise`, V161). Превью и UI теперь честны, но движок при обходе UI собрал бы невалидный продукт — defense-in-depth: перенести `incompatible`-гейт в `realiseAssembly` (return `ok:false`), чтобы `handleAssemblyRealise`'s `if(!r.ok)` был единственным жёстким стопом.

- [ ] **V168 — на wrap-bridge-строке наведение на RE-метку «обёрнутой» половины не рисует рез/каретку/линию/связывание на цепях** (review wq20q3xjv, 22.06). `RestrictionTrack` wrap-aware (рисует метку сайта из начала плазмиды в render-колонке `wrapAt+position`), но `StrandsTrack` НЕ получает `wrapsOrigin/wrapAt/seqLength` и фильтрует cuts/overhangs/bindings по АБСОЛЮТНОЙ позиции против `[lineStart,lineEnd]`. На последней main-строке `lineStart≈seqLength`, поэтому сайт wrap-половины (abs pos≈0) не проходит фильтр → каретка/коннектор/overhang/binding молча отсутствуют, хотя метка подсвечена. Display-only, только bridge-строка, только ферменты с узнаванием через ориджин — **minor**. Фикс: прокинуть wrap-метаданные в `StrandsTrack` и транслировать abs-позиции в wrap-render-колонку (зеркалить `renderCi`) до фильтра. Пред-существовало (StrandsTrack никогда не получал wrap-пропы для резов); RS-RE-VIS-редизайн сделал заметнее.

**V167 — S7 закрыт как НЕ-БАГ (продукт корректен) + per-fragment method-picker (UX/прозрачность).** **✅ 21.06.** **(1) S7/TD-ASM-PRODUCT-OVERLAP — состязательная верификация → НЕ-БАГ:** adversarial-workflow (4 ридера + 3 верификатора на конкретных примерах Gibson/RE/blunt) единогласно: `computeAssemblySequence` (`parts.join('')`) КОРРЕКТЕН для всех методов, дельта/стык = 0. `segment.sequence` = чистый source-слайс; homology-плечо живёт ТОЛЬКО в primer-тейле (`buildOverlapTail`), RE — топ-рез-конвенция. Дедупить нечего. Воркфлоу спас от «починки» корректного кода. TD закрыт + заменён на гардрейл-инвариант (INV-1 не расширять RE-слайсы, INV-2 не сворачивать тейлы в слайс — TECH_DEBT). Кода НЕ писал. **(2) Method-picker (остаток V166 + UX-прозрачность):** `SegmentList` inline-editor получил select «Метод» → `SET_PIECE_ACQUISITION_METHOD {pieceId, method, params:{}}` (экшен есть, params пустые ОК — reducer чекает только enum, `applyAutoReactions` создаёт нужную реакцию после). Курсор→ПЦР/Overlap-ПЦР/Синтез/Без-ПЦР (Рестрикция — read-only, ставится RE-пикером). Промоут курсор→ov-pcr → фрагмент амплифицируется (праймеры по V166-гейту + PCR-реакция); S5-бейдж следует. Интеграционный тест через реальный store (курсор→ov-pcr → store + бейдж «OV»). Прогон **5438/0**, build clean. Якорь: `SegmentList.jsx` (select), `skeleton-state-pieces.js:248`. **Остаток:** курсор по-прежнему АВТО-получает overlap-праймеры (до явного выбора метода) — но теперь юзер может явно задать метод. Полная гармония методов = V166-гейт + V167-picker.

**V166 — КРИТ: «мешанина методов» — на рестрикционном стыке считались overlap-праймеры (Игорь).** **✅ ИСПРАВЛЕНО 21.06.** Игорь: «при выборе рестриктаз почему-то всё равно на стыке идёт расчёт праймеров… оверлап оверлапом, рестриктазы рестриктазами — должно уживаться гармонично, это критично». **Корень (две графа расходились):** `auto-reaction-builder.shouldHaveReaction`/`mapMethodToKind` ПРАВИЛЬНО даёт RE-фрагменту (`acquisitionMethod:'restriction'`) реакцию `cut` (дайджест, не PCR); но `deriveAutoPrimers` (`primer-derive.js`) гейтил обход ТОЛЬКО по `kind` (skip snippet/gap), НЕ по `acquisitionMethod` → разрезанному фрагменту всё равно эмитились fwd/rev-праймеры с хвостом по методу стыка (overlap-плечо). Плюс `suggestMethodForBoundary` (`assembly-realise-suggest.js`) — sequence-only эвристика, НЕ смотрит `acquisitionMethod` → RE-фрагмент без авто-детектируемого сайта → дефолт `overlap_pcr` → overlap-химия на дайджесте. **Фикс (2 части):** (A) `deriveAutoPrimers` пропускает piece с `acquisitionMethod==='restriction'` (дайджест → НЕ амплифицируется → нет праймеров; но остаётся валидным overlap-СОСЕДОМ, так что PCR-фрагмент рядом получает правильный хвост); pcr/ov-pcr/курсор по-прежнему амплифицируются. (B) `suggestMethodForBoundary`: оба фланговых фрагмента RE-acquired → метод `restriction` (метод следует за acquisition, даже без авто-сайта/op-группы). Итог: overlap-стыки = PCR+гомология, рестрикционные = дайджест+лигирование, без праймеров на RE. TDD: `method-mixing-v166` (5: 2×RE→0 праймеров, курсор→4 сохранены, mixed→только PCR-сосед, оба-RE→метод restriction, курсор→не restriction) + k11/v109-регресс (group('restriction') с PCR-piece для добавления RE-сайта в хвост — НЕ ломается, piece не 'restriction'-acquisition). Прогон **5437/0** (НИ ОДНОГО регресса в realise/protocol/primer/zone), build clean. Якоря: `primer-derive.js:407` (гейт), `assembly-realise-suggest.js:120` (acquisition-ветка). **Остаточная (named, не критично):** курсор-фрагмент ('undefined') всё ещё авто-получает overlap-праймеры (reaction-граф говорит «нет реакции») — глубже: «promote курсор→ov-pcr» нужен UI выбора метода фрагмента (`SET_PIECE_ACQUISITION_METHOD` без вызова) — в UX-заход.

**V165 — сборщик-полировка S5: визуальный трекинг — метка способа получения + провенанс на баре.** **✅ РЕАЛИЗОВАНО 21.06.** Карта: «нет per-segment acquisition-глифа + нет провенанса на AssemblySegmentBar — мульти-source сборки неотличимы». Чистый `lib/acquisition-label.js::acquisitionLabel(method)` → `{short,full}` RU (restriction→RE, pcr→PCR, ov-pcr→OV, synthesis→син, direct→=, undefined→⌖ «выбран курсором»). `AssemblyShellBody.coloredZones` теперь несёт `acquisitionMethod`+`sourceName`; `AssemblySegmentBar` рисует под лейблом сегмента `short`-бейдж (`assembly-segment-acq`, `data-acquisition`) + провенанс в title; `SegmentList` row — бейдж `segment-acq-badge` рядом с kind-иконкой. Чистый derive, без store/schema. Тесты: acquisition-label(2)+интеграционные ассерты (cursor→⌖, RE→RE бейджи через реальный store). Прогон **5432/0**, build clean. Якорь: `lib/acquisition-label.js` (NEW).

**V164 — сборщик-полировка S4: единый вердикт «собирается + почему-нет» (прозрачность).** **✅ РЕАЛИЗОВАНО 21.06.** Чистый `lib/assembly-verify.js::verifyAssembly({readiness,endChem,reCloning})` → `{buildable, blockers:[{kind,message}], warnings:[{kind,message}]}` — агрегирует S1 (incompatible = BLOCKER), S2 (5′-OH-фосфорилирование, unresolved-ferment), S3 (sequential digest, дефос) + tentative в ОДИН структурный вердикт (источник истины для readiness-строки, протокола, будущего build-plan). `buildable = есть стыки && нет blockers` (зеркалит Realise-гейт: только incompatible блокирует; остальное — протокол-варнинги, не невозможность). **Консолидация UI:** разрозненные янтарные клаузы S2/S3 (`assembly-endchem`+`assembly-recloning`) слиты в ОДНУ `assembly-verify-warnings` строку (tentative остаётся в хедлайне); агрегат-data-attrs (`data-incompatible/needs-phos/sequential/dephos/buildable/warnings`) на readiness-div. 5 чистых тестов + S2/S3-интеграционные переведены на консолидированный вывод. Прогон **5430/0**, build clean. **Отложено (named):** полный порт `validateJunctionEnds` (GC/Tm/repeat/palindrome чеки overlap-стыков) на 4-tier через адаптер fragments[]/junctions[] — глубже, чем агрегатор; «Проверка»-панель с раскрытием. Якорь: `lib/assembly-verify.js` (NEW).

**V163 — сборщик-полировка S3: RE-cloning химия (double-digest + directional/self-ligation/дефосфорилирование).** **✅ РЕАЛИЗОВАНО 21.06.** Третий шаг плана. До этого: `checkDoubleDigest` подключён ТОЛЬКО к standalone cut-опу (CutOpPopup), НЕ к сборке; directional/selfLigationRisk из `digest()` — только в legacy-wizard; дефосфорилирование — только в closure-modal. **Фикс (assembler-сторона):** чистый `lib/restriction-cloning.js` — `segmentRestrictionPlan(segment, reEnzymes)` (зеркалит `digest()`-логику restriction-db.js:575 + переиспользует `checkDoubleDigest`): `directional` = разные оверхенги обоих концов (seq ИЛИ полярность); `selfLigationRisk = !directional` (одинаковые концы → беккбон может самозамкнуться); `doubleDigest` = `checkDoubleDigest(e1,e2)`→`{simultaneous, sequential, warnings}` для 2 РАЗНЫХ ферментов (одинаковые буфер+темп→одновременно, иначе последовательно); `recommendDephosphorylation = selfLigationRisk`. `assemblyRestrictionCloning` rollup. Это **двухмеханизмовая модель фона пустого вектора из ресёрча (NEB):** (1) разные концы→направленно→беккбон не рециркуляризуется; (2) одинаковые→дефосфорилировать вектор (Quick CIP/rSAP). 8 чистых тестов. **Выведение (additive, НЕ блокирует Realise — это протокол-гайд):** янтарная строка `assembly-recloning` (`data-sequential`/`data-dephos`) «N: последовательный дайджест · M: дефосфорилировать вектор (CIP/rSAP)» + per-segment чипы `⇄ посл.` (`segment-recloning-sequential`) и `○ CIP` (`segment-recloning-dephos`). НЕ трогает ranges/tails/product (pure derive). TDD: restriction-cloning (8 pure: EcoRI+BamHI directional/one-pot, EcoRI+BglII sequential, EcoRI+MfeI self-lig/дефос, single-enzyme, non-restriction, rollup) + readiness-интеграционный через РЕАЛЬНЫЙ store (2 RE-фрагмента: EcoRI+BglII→sequential, EcoRI+MfeI→дефос → `data-sequential=1`/`data-dephos=1` + оба чипа). Прогон **5425/0**, build clean. Якоря: `lib/restriction-cloning.js` (NEW), `AssemblyShellBody.jsx` (reCloning memo + `assembly-recloning` строка + `reCloningBySegment` проп), `SegmentList.jsx` (чипы). **Отложено (named):** шаг «дефосфорилировать вектор» / «последовательный дайджест» в `protocol-export.buildProtocol` (S3b — отдельная ProtocolPanel-поверхность, не редактор); backbone-vs-insert различение (self-ligation сейчас флагуется на ЛЮБОМ same-overhang сегменте — юзер опознаёт вектор; нет needs-backbone-инварианта по карте).

**V162 — сборщик-полировка S2: end-chemistry / 5′-P-модель для курсор-фрагмента.** **✅ РЕАЛИЗОВАНО 21.06.** Второй шаг плана. Цель: курсор-фрагмент (без RE-сайтов) — полноценный участник, концы достроены под метод. **Вердикт заземления (workflow, подтверждён по коду):** тейлы УЖЕ junction-driven — `deriveAutoPrimers` (`primer-derive.js`) гейтит обход по `kind` (sourced≠snippet/gap), НЕ по `acquisitionMethod`, и тянет tail из `cfg.method` (junction). Значит курсор-фрагмент (`acquisitionMethod:'undefined'`) УЖЕ получает overlap-плечо / RE-сайт+защитные / GG-тейл из стыка. → S2 = **5′-P-модель + выведение в UI**, НЕ перевод тейл-пути. Реальные дыры: (1) нет 5′-фосфат-модели нигде (load-bearing инвариант: рестрикция→5′-P, ПЦР/курсор/синтез→5′-OH; лигированию нужен 5′-P), (2) курсор-фрагмент невидим в readiness («тихий blunt-срез»). **Фикс:** новый чистый `lib/end-chemistry.js` — `fragmentFivePrimePhosphate(segment)` (restriction→true, иначе 5′-OH), `junctionEndPlan(left,right,method,enzyme,reEnzymes)` → per-side `{needsTail:overlap|re-site|gg-site|null, has5P, protectiveBases=minFlanking‖6}` + **junction-level** `needsPhosphorylation` = blunt-family-метод (kld/ligation/direct_ligation) И ОБА конца 5′-OH (правило NEB «≥1 фосфат для лигирования» — точнее, чем per-side из плана), `assemblyEndChemistry(segments,zoneJunctions,assemblyMethod,reEnzymes)` rollup. 12 чистых тестов. **Выведение (additive, не блокирует Realise — фосфорилирование = шаг протокола, не невозможность):** янтарная строка в `assembly-readiness` «N стык(ов) 5′-OH — нужно фосфорилирование (T4 PNK)» (`data-needs-phos`) + per-segment чип `⚡5′-OH` в `SegmentList` row. **НЕ трогает** хранимые диапазоны / тейлы / продукт (bio-safe; pure derive). TDD: end-chemistry (12 pure) + readiness-интеграционный через РЕАЛЬНЫЙ store (2 курсор-фрагмента + direct_ligation → `data-needs-phos=1` + янтарь + `⚡5′-OH` чип + Realise НЕ заблокирован). Прогон **5416/0**, build clean. Якоря: `lib/end-chemistry.js` (NEW), `AssemblyShellBody.jsx:159` (endChem memo) / readiness-строка / `:765` (endChemBySegment проп), `SegmentList.jsx` (чип). **Отложено (named):** шаг T4 PNK в `protocol-export.stepLigate` (след. потребитель `fragmentFivePrimePhosphate`); per-segment method-picker UI (`SET_PIECE_ACQUISITION_METHOD` без UI-вызова); `generateRETail` использует `||2`, S2-слой показывает `6` (дефолт NEB) — расхождение во **флаг Chat**, не форкаю константу молча; TD-ASM-PRODUCT-OVERLAP (S7).

**V161 — сборщик-полировка S1: readiness отражает ХИМИЮ, а не клики + активация RE-overhang-цепи в реальном потоке.** **✅ РЕАЛИЗОВАНО 21.06.** Первый шаг плана полировки сборщика (research→карта→backlog). **Проблема:** `assemblyReadiness` (`junction-derive.js:211`) показывал «✓ готово к сборке» когда `tentative===0` (каждый стык кликнут) — НУЛЬ химии: химически невозможный конструкт (несовместимые липкие концы) сертифицировался как готовый, Realise не блокировался. `junctionInterlock` (V160) считал реальный вердикт, но кормил только пиксели шва. **Фикс S1:** `assemblyReadiness` считает `incompatible` = число стыков с `interlock.verdict==='incompatible'` **method-aware** — только на стыках, что соединяют концы КАК ЕСТЬ (`STICKY_JOIN_KINDS = re_ligation/ligation/kld`); overlap/Gibson/Golden-Gate перестраивают концы (homology/fusion), поэтому RE-mismatch там НЕ блокирует (иначе ложно резали бы валидную Gibson-сборку RE-фрагментов). `ready = ... && incompatible===0`. UI: красная строка «⚠ Несовместимые липкие концы на N стык(е/ах) — сборка невозможна» (приоритет над trust-state), `data-incompatible`. Гейт: `canRealise && readiness.incompatible===0` (кнопка disabled) + defensive bail в `onRealise`. **⚡ КРИТИЧЕСКИЙ побочный фикс (вскрыт S1-интеграционным тестом):** `draftFromZone` (`zone-pieces-to-dag.js:247`) — конвертер piece→segment для редактора сборки — **НЕ переносил `acquisitionMethod`/`acquisitionParams` на сегмент**. Значит `segmentOverhangs` возвращал null → **ВСЯ RE-overhang-цепь (V158-чипы, V160-шов, S1-гейт) была МЕРТВА на реальном store-драфте** — рендерилась только на crafted `coloredZones` в тестах/probe (поэтому V158/V160 «проходили» браузер-probe, но в живом приложении RE-фрагменты концов не показывали!). Фикс: `draftFromZone` теперь несёт `acquisitionMethod: p.acquisitionMethod || 'undefined'` + `acquisitionParams: p.acquisitionParams || {}` → V158/V159/V160/S1 впервые живут в реальной сборке. TDD: `assembly-readiness-ux` (+3 pure: incompatible блокирует на re_ligation / compatible-blunt-unknown не блокируют / incompatible на overlap НЕ блокирует; +1 интеграционный через РЕАЛЬНЫЙ store→editor: EcoRI×SalI mismatch → `data-incompatible=1` + красная строка + Realise disabled). Прогон **5403/0**, build clean. Якоря: `junction-derive.js:211` (STICKY_JOIN_KINDS+incompatible), `AssemblyShellBody.jsx:600` (красная строка) / `:523` (canRealise) / `:197` (bail), `zone-pieces-to-dag.js:247` (draftFromZone pass-through). **След. шаги:** S2 (достройка концов курсор-фрагменту + 5′-P-флаг), S3 (checkDoubleDigest+дефос), S4 (assembly-verify мост), S5 (не-RE-шов+провенанс), S6 (build-plan), S7 (overlap-aware продукт).

**V160 — визуализировать СТЫК: липкие концы соседних RE-фрагментов в сборке должны сцепляться (Игорь).** **✅ РЕАЛИЗОВАНО 21.06.** «ступенька на ЕГО фрагментах четко соединялась… необходимо визуализировать стык». Смысл липких концов (V158/V159) — увидеть, что правый конец фрагмента A и левый конец фрагмента B СОЕДИНЯЮТСЯ в шов (и совместимы ли). **Ключевой инсайт:** assembly Sequence view — ОДНА непрерывная пост-лигированная цепь без зазора на стыке, поэтому «interlock» — это ПРЕ-лигированная картинка: два оверхенга занимают ОДНИ И ТЕ ЖЕ колонки на ПРОТИВОПОЛОЖНЫХ цепях (5′: B-верх + A-низ; 3′: A-верх + B-низ) — это и есть «ступенька вниз + ступенька вверх = встык». **Совместимость = канон проекта** (`restriction-db.getCompatible`: равный `overhang seq` + равная полярность `type`; blunt+blunt → ок; оверхенги хранятся palindrome-folded 5′→3′, поэтому string-equality = reverse-complementarity для классических RE). Чистый `junctionInterlock(aRight, bLeft)` (`segment-overhangs.js`, 11 тестов) → `{verdict: compatible|incompatible|blunt|unknown, length, message, overhang, side, topOwner, botOwner}` (геометрия: какая цепь чьего фрагмента в зоне оверхенга). `AssemblyShellBody.coloredZones`: на каждый внутренний boundary вешает `interlock = junctionInterlock(ovr[i].right, ovr[i+1].left)`. `SegmentZonesOverlay` рисует шов на колонке `p=zone[i].end` (reuse существующий line/strand-probe): **совместимо** → двухцветные ступеньки на противоположных цепях + зелёный коннектор (соединено) + бейдж «✓ AATT»; **несовместимо** → красный пунктир-разделитель + «✕» + V158-чипы ОБОИХ концов остаются видны («5′ AATT»/«5′ TCGA») + тултип «Несовместимые липкие концы: … ≠ …»; **тупые** → чистая линия + «тупой». Слойность чиста: `interlock` считается выше, едет в `zones`-пропе, SequenceView НЕ импортит CanvasSkeleton. На внутреннем compatible/blunt-стыке V158-чипы подавляются (шов их заменяет); на outer-концах и incompatible — остаются. TDD: `junction-interlock` (11: совместимость+геометрия 5′/3′) + `junction-seam-v160` (4: рендер бейджа/разделителя через реальный SequenceTab-путь). Прогон **5399/0**, build clean. Браузер-probe (2 вьюера: EcoRI/EcoRI → ✓ AATT + 2 двухцветные ступеньки + зелёный коннектор, чипы скрыты; EcoRI/SalI → ✕ + красный разделитель + 2 чипа AATT/TCGA). **Отложено (scope V160):** Golden Gate interlock (нужен реальный RC по непалиндромным 4-нт fusion-сайтам — пока GG → unknown, шов не рисуется); SVG-мини-диаграмма в JunctionPopover; overlap/Gibson «homology mate». Связано: V158 (per-segment ступеньки), V159 (пикер из тени).

**V159 — правый липкий конец RE-фрагмента в пикере «в тени» (Игорь, скрин).** **✅ РЕАЛИЗОВАНО 21.06.** «когда выбираешь то второй хвост сейчас все еще "в тени" но там же есть нужные нуклеотиды, надо обязательно и правую рестриктазу добавить». RE-pair-выбор берёт фрагмент МЕЖДУ верхними резами `[cutA,cutB]`; нуклеотиды оверхенга на ВЫСТУПАЮЩЕЙ цепи (5′ справа — низ протыкает за `end`; 3′ слева — низ перед `start`) лежат за резом → out-of-range-маска (`OutOfRangeMaskOverlay`, `[0,lo)+[hi,L)`) их затемняла, хотя это липкий конец фрагмента. **Уточнение Игоря (AskUserQuestion):** «Включить оверхенг в фрагмент». **НО** буквальное расширение хранимого `[start,end]` биологически НЕВЕРНО: продукт собирается СЫРОЙ конкатенацией `segment.sequence` (`computeAssemblySequence`, без дедупа оверлапа ни для какого типа стыка) — топ-рез-конвенция уникальна тем, что общий оверхенг считается РОВНО раз (5′-правый принадлежит СЛЕДУЮЩЕму фрагменту, чья верхняя цепь стартует с того же реза). Расширь слайс — оверхенг удвоится на каждом ligation-стыке и испортит восстановленный RE-сайт (трасса EcoRI/HindIII: insert `…A`+backbone `AGCTT…`=AAGCTT ✓; расширенный insert `…AAGCT`+`AGCTT…`=AAGCTAGCTT ✗). **Фикс:** оверхенг — это и есть нижняя цепь фрагмента (где 5′-правый и живёт биологически), её надо НЕ прятать. Чистый `stickyEndExtent` (`segment-overhangs.js`, переиспользует `segmentOverhangs`: `s=lo+min(0,leftΔ)`, `e=hi+max(0,rightΔ)`, кламп `[0,seqLen]`, 10 тестов) расширяет ТОЛЬКО маску пикера до дуплекс-предела → оверхенг выходит из тени и читается как липкий конец (V156-ступенька его уже красит на нижней цепи). Хранимый/подтверждаемый `[start,end]` ОСТАЁТСЯ на топ-резах (продукт-безопасно; длина фрагмента = gel-конвенция). `RangePickerModal`: `maskRange` для `outOfRangeMask` + хинт «липкие концы: тупой | 5′ AATT». TDD: `segment-overhangs` (+10: 5′-право/3′-лево/оба/тупые/кламп/нормализация) + `range-picker-sticky-mask-v159` (3: маска расширяется, confirm на резах не меняется). Прогон **5385/0**, build clean. Браузер-probe (PvuII@23 тупой + MfeI@37 5′): маска `[23,41]` (правый край на 41, не 37 → оверхенг AATT [37,41] не затемнён), confirm `[23,37]`, хинт «14 bp · липкие концы: тупой | 5′ AATT». **Открыто (TECH_DEBT):** `computeAssemblySequence` наивна — НЕ дедупит оверлап/оверхенг ни для overlap/Gibson, ни для ligation; точная сборка продукта (overlap-aware конкатенация) ждёт отдельного движкового прохода.

**V158 — липкие концы (хвосты) RE-сегмента видны в СБОРКЕ (Игорь, скрин).** **✅ РЕАЛИЗОВАНО 20.06.** «вот тут тоже после пикера хвосты должны быть видны — это в сборке». Данные уже есть (V157: сегмент несёт `acquisitionMethod:'restriction'` + `acquisitionParams.{enzymes,cutSites}`, доносится `pieceToSegmentShape`). Чистый `lib/segment-overhangs.js` (`segmentOverhangs` — мапит каждый КОНЕЦ на свой фермент по cut-позиции, delta=`cut[1]-cut[0]`; `overhangLabel` → «5′ AATT»/«3′ TGCA»/«тупой», 5 тестов). `AssemblyShellBody` цепляет `reOverhangs` на `coloredZones`. `SegmentZonesOverlay` рисует **ступеньку** (штрих-заливка на выступающей цепи: для 5′ — верх слева/низ справа, для 3′ — наоборот, геометрия как у пикер-stickyEnds) + **чип-подпись** на каждом конце (надёжно, даже где хвост выходит за границу сегмента — выбор Игоря «ступенька+подпись»). Слойность чистая: `overhangLabel` зашит в `reOverhangs`, SequenceView НЕ импортит CanvasSkeleton. Прогон **5372/0**, build clean. Не-RE сегменты — без изменений. **Фикс по live-фидбеку (Игорь «левый сдвинут на 1, правый уезжает»):** хвост клампится к `[zStart,zEnd]` сегмента (а не только к строке) — оверхенг, выходящий за границу фрагмента (5′ справа / 3′ слева живёт на утопленной цепи ПОСЛЕ конца), коллапсирует → хвоста нет, чип несёт последовательность (ровно выбранное «ступенька там, где оверхенг в показанном участке»). «Сдвиг на 1» был артефактом probe (сегмент включал recognition-G); в реальном фрагменте `segStart` = рез (picker даёт `[cutA,cutB]`), хвост `[0,4]`=AATT без сдвига. Браузер-probe (фрагмент `AATTC…G`): левый хвост = AATT (база 0, 24px≈4), правый — только чип, «уезжания» нет.

**V157 — «Недопустимый метод получения куска» при RE-pair-select → вставке фрагмента.** **✅ ИСПРАВЛЕНО 20.06.** Игорь, 20.06: тост вылетает при попытке выбрать две рестриктазы (вскрылось после V155/V156, когда RE-pair реально заработал и юзер им пользуется). **Пре-существующий** (не от V155). Корень: `zone-assembly-write-adapter.js` (INSERT_SEGMENT) ставит RE-куску `acquisitionMethod:'restriction'` но `acquisitionParams: {}` (пусто); а `piece-invariants.validatePiece` (155-159) требует НЕ-пустые `acquisitionParams` для param-несущих методов (pcr/ov-pcr/restriction/synthesis) → `fail('INVALID_METHOD')` → тост. **Фикс:** прокинуть ферменты пары из пикера (`onPairCommit` → `onConfirm` → `insertSegment` opts → INSERT_SEGMENT) в `acquisitionParams = { enzymes:[firstEnzyme,secondEnzyme], cutSites:[{position:firstPos},{position:secondPos}] }` (форма, которую читает `auto-reaction-builder` для 'cut'-реакции — `ap.enzymes`/`ap.cutSites`); adapter использует прокинутое, иначе safe-фолбэк `{enzymes:[]}` (НЕ пусто → проходит инвариант). 4 файла: `zone-assembly-write-adapter` (params + фолбэк), `skeleton-context.insertSegment` (+acquisitionParams), `AssemblyShellBody.onRangeConfirm` (проброс), `RangePickerModal` (reParams из onPairCommit → onConfirm). TDD: `assembly-re-pair-insert-v157` (3, red→green: restriction+params/без params/cursor→undefined) + `range-picker-re-pair-v88` (+assert acquisitionParams проброшен). Прогон **5367/0**, build clean. **Браузер e2e** (probe: RangePickerModal RE-pair → onConfirm → РЕАЛЬНЫЙ skeletonReducer): две EcoRI → выделение [21,81] → piece создан (method='restriction', params={enzymes,cutSites}), **toasts: [] (нет ошибки)**.

**V155 — RE-pair-select в пикере смещён на cut[0] от реального реза (пре-существующий рассинхрон).** **✅ ИСПРАВЛЕНО 20.06.** Найден при реализации «липких концов» (20.06). `flattenSites` (feature-map.js) хранит `reSites[].position = recognition + cut[0]` (= ТОП-рез), и `RestrictionTrack` получает `sites={reSites}` → `onSiteClick` отдаёт `site.position = топ-рез`. Но `useSequenceSelection.onRestrictionClick` трактует `site.position` как НАЧАЛО сайта узнавания и добавляет `cut[0]` ещё раз (`cutA = stored.position + cut[0]`) → выделение пары встаёт на `recognition + 2·cut[0]`, т.е. на cut[0] правее настоящего реза. Юнит-тест (`useSequenceSelection.test.js:114`) проходит, потому что подаёт `position:100` как recognition (не как flattened топ-рез) — тест ≠ продакшен-данные. **Эффект:** (1) выделение RE-фрагмента в пикере смещено на cut[0]; (2) «липкие концы» (V156) НЕ срабатывают в пикере, т.к. концы выделения не совпадают с `reSites[].position`. **Фикс (Игорь «делай так чтобы работало»):** `onRestrictionClick` теперь трактует `site.position` как топ-рез — pair `[stored.position, site.position]` (без двойного `+cut[0]`), first-click snap к сайту узнавания `[pos−cut[0], …]`. Заодно тот же двойной сдвиг в `SequenceLine.reCutLayout` (hover cut-bars + binding-box) → `tAbs=s.position`, `bAbs=s.position+(cut[1]−cut[0])`, recogStart=`s.position−cut[0]`. **Проверка digest:** `onConfirm` → `insertSegment(start,end)` режет `[start,end]` напрямую — старое поведение давало RE-фрагмент на 1 базу короче с 5′-конца (реальный био-баг!), теперь корректно. Тесты `useSequenceSelection`/`range-picker-re-pair-v88` переведены на flattened-координаты (входы = топ-резы, выходы прежние). Полный прогон **5364/0** (фикс границ фрагмента НЕ сломал ни одного digest/assembly-теста — значит никто не зависел от старого смещения), build clean. Браузер (probe RangePickerModal, две EcoRI): два RE-клика → выделение `[31,91]` ровно на резах (start-поле 32=1-based от 31, «60 bp · по RE-сайтам»), **липкие концы видны** (top/bottom сдвиг 24px≈4 базы AATT).

**V156 — sticky-ends + invert выделения в SequenceView (запрос Игоря 20.06).** **✅ РЕАЛИЗОВАНО 20.06.** (1) **Липкие концы**: когда концы выделения попадают на RE-рез (`reSites[].position` = топ-рез), `SelectionOverlay` рисует верхнюю и нижнюю цепь СО СДВИГОМ на длину оверхенга (`bottom = [lo+leftΔ, hi+rightΔ]`, Δ=`cut[1]−cut[0]`), полупрозрачность даёт «ступеньку» (ds-ядро темнее, ss-оверхенг светлее). (2) **Инвертировать выделение**: пункт правого клика «Инвертировать выделение» красит/копирует КОМПЛЕМЕНТ ([0,lo)+[hi,len), «оба куска» — топология-агностично), тогглится, сбрасывается при смене выделения; copy через origin для circular. Чистые хелперы `lib/selection-ops.js` (`complementSegments`/`invertedSlice`/`stickyEnds`, 13 тестов) + меню (3 теста). Браузер-probe (две EcoRI): top/bottom рект сдвинуты на 24px≈4 базы (AATT); invert красит всё кроме фичи. Прогон 5364/0, build clean. **Работает в пикере** после фикса V155 (RE-pair-выделение встаёт ровно на резы → концы совпадают с `reSites[].position` → sticky рендерится; браузер-verified в RangePickerModal).

**V154 — кольцевая минимапа (PlasmidMapV2) не показывала интроны: ген рисовался сплошной дугой.** Игорь, 20.06: «минимапа не знает о существовании интронов. Чини — гэпы должны быть видны». **✅ ИСПРАВЛЕНО 20.06 (V154).** `PlasmidMapV2` рисовал каждый region одной `featureArrow`, игнорируя интрон-детей (`level:'detail', type:'intron', parentId===gene.id` — та же модель, что у линейного `AnnotationTrack`/`GeneExonRects`). **Фикс:** переиспользован чистый `exonSegments` (из `gene-exon-spans.js`); сплайс-ген теперь рендерится как **экзонные блоки** (`arcBand` + терминальный экзон с стрелкой по цепи) с **видимыми гэпами на месте интронов** + тонкий пунктирный коннектор-дуга (`arcStrokePath`) через каждый интрон (чтобы блоки читались как один ген). Геометрия — новые `arcBand`/`arcStrokePath`/`collectIntronsByParent` в `lib/plasmid-map-v2.js` (+тесты); `featuresFromFragments` прокидывает offset-сдвинутые интрон-спаны (вьюер-путь), `annotations`-путь собирает через `collectIntronsByParent`. Гена без интронов — как раньше (одна стрелка). TDD: +геометрия (arcBand/arcStrokePath/collectIntrons/featuresFromFragments-introns) + компонент (3 теста: data-spliced, 4 экзона/3 коннектора, клик). Браузер (probe, glaA 3 интрона): 4 экзон-блока + 3 коннектора, гэпы видны, 0 ошибок. Прогон **5348/0**, build clean. Старая `PlasmidMiniMap` (MultiImportView, крошечные тайлы дерева) интроны не показывает — вне скоупа (V2 — основная минимапа).

**V153 — drag-selection в SequenceView не авто-скроллит у нижней (и верхней) кромки.** Игорь, 20.06: «когда тянешь выделение в аннотаторе (и в других сиквенс-вьюверах) то оно упирается в низ и не двигается — оно должно двигаться вниз за курсором». **✅ ИСПРАВЛЕНО 20.06 (V153).** Авто-скролл-механика существовала (`useSelectionState.updateAutoScroll/tickAutoScroll` + RAF), но **`findScrollingAncestor(el)` начинал обход с `el.parentElement` — пропускал сам элемент.** Корневой `<div ref={containerRef}>` самого `SequenceView` несёт `overflowY:auto` и в Annotator/Align (bounded-height flex-host, `minHeight:0`) скроллится ВНУТРИ себя → функция возвращала предка/`document` (чей `rect.bottom` за экраном) → `clientY > rect.bottom-28` не срабатывал → скролла нет. (В Library инспекторе корень распухает на всю высоту, скроллит предок `importer-single-tab-content` — там и старый код находил скроллер, поэтому баг был не везде.) **Фикс:** `scroll-handle.js` — обход стартует с `el` самого (`let cur = el`); self-скроллер теперь возвращается первым, host-с-предком-скроллером по-прежнему проваливается вниз (корень не скроллит → идём к предку). Помогает и `scrollToPosition` (точнее «уже на экране»). TDD: `lib/__tests__/scroll-handle.test.js` (3, red→green). Прогон **5340/0**, build clean. Браузер (Annotator self-scroll root): `findScrollingAncestor`→root, drag к нижней кромке проскроллил 0→648px за курсором.

> **V139–V141 — canvas-UX кластер**, surfaced на приёмке слоя 2 набора live-junction (05.06.2026). **НЕ движковые баги:** слой 2 трогал `primer-derive.js` + `zone-pieces-to-dag.js`, не canvas-компоненты → это не регрессия слоя 2. Фикс — в отдельной canvas-сессии (§7: приёмка движка ≠ canvas-UX звено), пересекается со слоями 3 (JUNCTION) и 5 (CANVAS_LIVE_PROJECTION). Перед фиксом — читать код от симптома (§0), не от догадки.

**V139 — клик по ромбу PCR/Cut (op) на канвасе ничего не делает.** Найдено Игорем на приёмке слоя 2 (05.06). Симптом: операция (ромб) на графе не реагирует на клик. **✅ ИСПРАВЛЕНО 28.06 (CANVAS-CLICK-2, /loop).** Корень (браузер-подтверждён): на DAG-вью операции — **derived** (`dag-op-…`, `_derived:true`), не committed store-операции, поэтому `onOperationClick→actions.openEditorOpTab(op.id)` искал несуществующую store-op → no-op (не drag-guard). **Фикс:** консистентно с CANVAS-CLICK-1 — клик по операции открывает **панель деталей реакции** (`OperationInfoPanel` + чистый `operation-info.js`): kind-лейбл + иконка, фермент(ы), метод, вход→выход; работает и на превью, и на committed (читает узел напрямую, не зависит от tab-системы). Взаимоисключение с панелью фрагмента. Браузер: клик по Cut → «Рестрикция (Cut)» + ферменты AjiI·ApaI + плазмида→фрагмент. TDD: operation-info 4/4. **Остаток:** паритет на основном канвасе ZoneFrame (op-клик там идёт через CanvasLayoutView) — CANVAS-CLICK-3.

**V140 — зона-контейнер: сборку нельзя двигать внутри зоны, контейнер не масштабируется.** Найдено там же (приёмка слоя 2, 05.06). Симптом: внутри зоны узлы/сборка не перетаскиваются, габариты зоны-контейнера не меняются. Радиус — zone bounds / drag-within-zone / resize handles; читать от симптома до фикса. Тот же кластер, пересечение слои 3/5. **Половина «контейнер не масштабируется» — адресована 11.06** (см. FIXED 11.06: видимые угловые ручки + кнопка «⤢ подогнать» + `SET_ZONE_AUTO_RESIZE` opt-out, чтобы ручной ресайз не отскакивал). Половина «узлы не таскаются» — теперь **by-design** (M-CANVAS-FIX.1 K1: авто-раскладка авторитетна, таскается ЗОНА, не узлы). Финальная диспозиция V140 — за приёмкой M-CANVAS-FIX.1.

**V141 — панель «Контейнеры → Праймеры» обрезана/тесная.** Найдено там же. Праймеры в панели присутствуют, но читаемость низкая (layout/overflow панели). Тот же кластер. Косметика/layout, не корректность данных.

### Низкие

**V120 — ORF detection не видит circular wrap.** Найдено Code-аудитом 28.05. `orf-detection.js:39-67` сканирует линейные `seq` + `rcSeq`; ORF, пересекающий начало координат (origin кольцевой плазмиды), пропускается или обрезается. Плазмиды кольцевые. **Фикс:** для circular сканировать `seq + seq.slice(0, maxORFnt)`, фильтровать по старту в `[0, len)`. **⏸ ОТЛОЖЕНО 28.05.2026** — не быстрый фикс: требует проброса topology через `detectORFs`/`runPredictors` + wrap-scan + поддержку wrap-координат аннотаций (feature-sized, риск регрессии в predictor-пайплайне). Низшая severity (origin-spanning ORF — меньшинство, ORF — подсказка). Оставлен в OPEN для отдельной задачи.

**V121 — SapI `cutOffset: 4` неверен (мёртвые метаданные).** Найдено Code-аудитом 28.05. `golden-gate.js:31` — по конвенции остальных 4 GG-ферментов (`recognition_len + top_spacer`) для SapI `GCTCTTC(1/4)` должно быть 8, не 4. НО `cutOffset` нигде не читается (`designOverhangs` использует `overhangLength` + границы фрагментов) → функционального эффекта НЕТ. Чинить для корректности данных / будущих консьюмеров. (Не блокер, фикс не срочен.)

**V137 — смена типа фичи в Annotator плодит новый трек вместо смены типа текущей фичи.** Найдено Игорем на partial-приёмке 30.05.2026 (репортёр GFP). При смене `type` существующей фичи в Annotator создаётся НОВАЯ запись фичи (новый трек), а тип текущей не меняется → дублирующиеся фичи. Не диагностировано — вне скоупа partial-приёмки, отдельное звено. Радиус неизвестен (Annotator type-редактор / update-path аннотации) — читать код от симптома перед фиксом.

**V199 — [x] ИСПРАВЛЕНО 12.07 (inline-путь; off-thread → TD-SEARCH-WORKER).** Живой ДНК-поиск возвращал 0 для присутствующей последовательности. **Корень (браузер-диагностика dynamic-import + `[SEARCH-DBG]` инструментирование):** НЕ данные — все 309 записей в Dexie И в реальном сторе (`?t=` версионный импорт: hydrated:true, withSeq:309, HaloTag=891bp) имеют `payload.sequence`; движок `seqMatch`, `search.worker.js` (сырой `new Worker`), `createSearchFacade` с реальным Worker на РЕАЛЬНЫХ 309 доках → 13 хитов dim `sequence`. Но живой `LibraryTopBar`-лог: `resolve` входит (seqQuery ок, docs=309, docsWithSeq=309, hasWorker=true), затем `await workerClient.searchSequences` **ВИСНЕТ навсегда** — ни ответа, ни ошибки. Т.е. **Vite-конструкция `new Worker(new URL('../../lib/search.worker.js', import.meta.url), {type:'module'})` внутри guard-обёртки `makeSearchWorker` создаёт воркер, который не отвечает в этом DEV-окружении** (сырой `new Worker(absoluteUrl)` отвечает). **Фикс:** `makeSearchWorker` → `return null` → фасад использует inline-путь (main-thread, ~385мс/1МБ с дебаунсом — приемлемо, correctness>speed). Браузер-верификация: «ПОИСК ПО ДНК · 13» HaloTag+варианты, честная «100% идентичность»+чип «ДНК»; клик по результату→открыл запись+вкладка Sequence+16bp выделение (P3-прыжок). **TD-SEARCH-WORKER — [x] ЗАКРЫТО 13.07 (off-thread работает + ИСТИННЫЙ корень V199 найден).** Вернул sequence-поиск в воркер через Vite `?worker` статический импорт (`import SearchWorker from '../../lib/search.worker.js?worker'`, `new SearchWorker()` в `makeSearchWorker`, guard `import.meta.env.VITEST/MODE==='test'`→null→inline под happy-dom; try/catch→inline при отсутствии Worker). **ИСТИННЫЙ КОРЕНЬ V199 (диагноз 12.07 был НЕВЕРЕН — не «конструкция `new URL` виснет»):** браузер-проба доказала, что воркер отвечает всеми способами конструкции (`?worker` dynamic, raw `new Worker(url,{type:'module'})`, versioned `?t=` URL — все ~30-50мс, корректные хиты; фасад+воркер на 309 доках→95 хитов, final delivered за 1866мс). Реальная причина = **React StrictMode double-invoke эффектов**: фасад создавался ВО ВРЕМЯ рендера (`if(!facadeRef.current) facadeRef.current=createSearchFacade(...)`), а `terminate()` — в cleanup `useEffect([])`. StrictMode: mount → cleanup(**terminate воркер!**) → mount, но ref держал уже-мёртвый фасад, никогда не пересоздавался → `searchSequences` виснет навсегда → 0. Inline был иммунен (`terminate()`=noop) — поэтому только off-thread ломался, и поэтому inline-фикс 12.07 «сработал» (замаскировал). **Фикс:** создавать фасад+воркер ВНУТРИ `useEffect([])` (не в рендере), cleanup terminate+обнуляет ref → remount пересобирает живой фасад. + null-guard в debounced-search. Билд эмитит `search.worker-*.js` (7.35КБ). **Браузер-верификация (309 записей):** ДО фикса топбар «ДНК·0» (виснет); ПОСЛЕ — `CGGCATCAGAGCAGATTGTACTGA`→«ДНК·95», **95 отрендеренных строк** (BlueScribe/pAN7-1/pANT7…, «100% идентичность·24/24 нт»), 0 console-ошибок. Предиктор-воркер (`annotator-worker-client.js`) использует ту же `new URL`-форму и всегда работал — потому что конструирует ЛЕНИВО в lib-модуле, а не при рендере .jsx. Мелочь: scroll-to-hit центрирует не идеально — доводка.

**V198 — дубли React-ключей `region:<start>:<end>:CDS:<X>` в PlasmidMap.** Замечено попутно в консоли dev-сервера при браузер-верификации умного поиска (12.07, НЕ регрессия P2 — пре-существующее). React warning «Encountered two children with the same key» для region-аннотаций CDS (напр. `region:2394:2922:CDS:G`, `region:0:136:CDS:A` ×N) на карте плазмиды — одинаковые ключи у разных дочерних элементов → возможны дубли/пропуски при ре-рендере. Ключ строится из type+coords+name без уникального `id` аннотации; при совпадении координат/имени (или дублирующихся аннотациях) коллизия. **Фикс:** использовать `annotation.id` (инвариант annotation-contract) как React key вместо составного coords-ключа. Низкая severity (визуально не проявилось), но annotation-contract рекомендует стабильный id-key. Радиус: PlasmidMap region-render — читать от симптома.

---

## APP-AUDIT SWEEP (воркфлоу wxu8bexq2 + живой тур, 06.07.2026) — «исправить ВСЕ видимые косяки» (Игорь)

Мотив: «движок есть — проводки нет» (validateConstruct/GG-оверхенги/Phred/заказ→статус/tags реализованы, но не на экране); дубли-поверхности; тёмная тема сломана на био-панелях; не грибо-first (кодоны только E. coli). Полный текст 9-агентного аудита — `tasks/wxu8bexq2.output`. Чиню волнами.

**Батч 1 — copy/дизайн-система (✅ 06.07):**
- [x] AUD-1 ThemeToggle эмодзи ☀/🌙 → Icon sun/moon (DS §1.2). ThemeToggle.jsx
- [x] AUD-2 DAG empty-state врал («после Реализовать» → «Добавьте фрагменты»). AssemblyDagView.jsx
- [x] AUD-3 Интрон-варнинг задом-наперёд («удалите для E. coli!») → host-aware. validate.js +test
- [x] AUD-4 Вкладки сборки полу-английские → русские (Последовательность/Граф/Праймеры/Протокол). AssemblyViewTabStrip.jsx
- [x] AUD-5 Онбординг Главной: дубль плашки «наполните библиотеку» — убрал верхнюю. MainPanel.jsx

**Открыто (по темам аудита):**
- [ ] AUD-10 (P0-A) validateConstruct/validateJunctionEnds без UI — вывести в readiness сборки (обратный промотор/нет терминатора/рамка≠×3/идентичные). validate.js→AssemblyShellBody
- [ ] AUD-11 (P1-A) GG designOverhangs/validateOverhangs/resolveConflicts без .jsx — оверхенги+ортогональность+«Adjust» в GoldenGateOpPopup
- [ ] AUD-12 (P0-Sanger) Phred парсится, не показан/не триммится — Q-шейдинг, «обрезать по качеству», буквы над пиками, low-Q вне метрик. AlignmentChromatogram/ReadTrack
- [x] AUD-13 (P0-primer) заказ олигов не двигал статус — кнопка «Отметить заказанными»: planOrderStatusUpdate матчит по seq → promotePrimerStatus(ordered) + addPrimerToPool для новых. PrimerOrderPanel.jsx +test ✅06.07
- [ ] AUD-14 (P1-A) tags-db (His6/FLAG/GS) без UI — тег-пикер в primer-from-selection. tags-db.js
- [ ] AUD-15 (P2-A) checkReadingFrame на RE-стыке CDS не показан — scar±bp в JunctionPopover. restriction-db.js
- [ ] AUD-20 (P0-B) PlasmidViewer/PlasmidUseWizard деградир. путь (fragments→нет саб-фич; forward-only AA → реверс-CDS без трансляции) — через SequenceView+annotations
- [ ] AUD-21 (P0-B) 3 стора праймеров — удалить мёртвые PrimerPanel.jsx/OligoManager.jsx + registry-fns. primer-reuse.js
- [ ] AUD-22 (P2-B) 2 палитры фич (theme.js vs feature-palette.js) — единый источник feature-palette. theme.js
- [ ] AUD-23 (P0-Sanger) статус клона руками, не из выравнивания — связать Align-вердикт ↔ SangerCloneRow
- [ ] AUD-24 (P1-B/L) 2 роутера окон «корень хаоса»; assembly в DEV-only route — унифицировать workspace.active. App/AppShell/Sidebar/Breadcrumb
- [ ] AUD-30 (P1-C) readiness/conflicts только во вкладке Sequence — поднять на workspace (видно на всех вкладках). ProjectAssemblyWorkspace
- [ ] AUD-31 (P0-C) DAG красная карта без причины — FragmentInfoPanel показать _readiness+conflict+↻-flip. AssemblyDagView
- [ ] AUD-40 (P0-D) кодоны только E. coli — A. niger/T. reesei (Kazusa) + organism-поле + селектор. codon-optimize-ecoli/CodonStatsPanel  ⚠ нужны реальные таблицы
- [ ] AUD-41 (P0-D) интрон-бейдж host-aware на CDS + 1-клик→кДНК. intron-utils/gene-parser
- [~] AUD-42 (P1-D) легенда Type IIS vs RE — нота в GoldenGateOpPopup («режет вне сайта → бесшовно; классические RE в другой реакции»). Осталось: зеркальная нота в RE-cut попапе. частично ✅06.07
- [ ] AUD-50 (P0-E) тёмная тема сломана на 13 био-панелях (raw gray/#1a1a1a) — токен-миграция + lint-гард. JunctionBlock/JunctionDNA/AnnotationEditor/...
- [ ] AUD-51 (P1-E) деструктив на native window.confirm — requestConfirm+ConfirmModal (как PromptModal). uiSlice ~7 мест
- [ ] AUD-52 (P1-E) 84 эмодзи-в-chrome/40 файлов → Icon-глифы
- [ ] AUD-53 (P2-E) Icon-кнопки без aria-label/focus-ring (46/200)
- [ ] AUD-60 (P0-lib) папки-тупик: положить нельзя (writer без вызовов; wired путь пишет tags) — DnD на TreeFolderRow + пикер. librarySlice/TreeFolderRow
- [ ] AUD-61 (P1-lib) поиск библиотеки только по имени — +теги/описание/фичи/seq:. ProjectZone/LooseZone matchesQuery
- [ ] AUD-62 (P1-lib) нет overwrite/«сделать текущей» — только форк. LibrarySaveActions/VersionHistoryList
- [ ] AUD-63 (P1-lib) диф версий без позиций/AA — +ProteinEffect. VersionHistoryList
- [ ] AUD-64 (P1-lib) origin.kind грубый — типизировать (sanger/mutation/assembly). version-lineage
- [x] AUD-65 (P2-lib) disabled «Сортировка — в разработке» — убрана мёртвая кнопка (реализовать сорт позже). LibraryTreeRoot ✅06.07
- [ ] AUD-66 (P2-lib) действия дерева только hover/right-click — ⋯-overflow. TreeItemRow
- [ ] AUD-70 (P1-seq) быстрое создание фичи (select→имя→Enter, авто-тип ORF). CreateAnnotationPopup
- [ ] AUD-71 (P1-seq) map↔seq split-view одновременно, не switch-tab. LibrarySingleInspector
- [ ] AUD-80 (P0-IA) ContainerWindow — заглушка «В разработке» — минимальный read-only инспектор. ContainerWindowPlaceholder
- [ ] AUD-81 (P0-IA) Главная — нет goal-first старта (restriction/gibson/primer/sanger). MainPanel/QuickStart
- [x] AUD-82 (P1-IA) глоссарий-заглушка «Скоро» — наполнен 9 терминами модели (проект/контейнер/сборка/операция/стык/праймер/.bodge). HelpPopover.jsx ✅06.07
- [~] AUD-83 (P1-IA) термин «зона»==«сборка» протекает в UI — вычищен из reset-confirm (CanvasSkeleton/index.jsx). Осталось: прочие user-facing строки. частично ✅06.07
- [ ] AUD-84 (P2-IA) 4 двери «создать/открыть проект» — свести к одной. Sidebar/MainPanel/ProjectContextBar
- [ ] AUD-90 (P1-Sanger) сессия выравнивания транзиентна (reload → пропадает) — персист Dexie + «сохранить к молекуле». alignmentSlice
- [ ] AUD-91 (P1-Sanger) нет 1-жеста «проверить секвенированием»; слой праймеров «в разработке». AlignWorkspace
- [ ] AUD-92 (P2-Sanger) правка референса не линкуется к .ab1/Q. alignmentSlice/describe-edit
- [ ] AUD-100 (живой тур) «Базовый набор» врёт по длине: pUC19=432bp (реально 2686), pET-28b=360bp — заглушки под именами реальных плазмид. ⚠ нужны канонические seq
- [x] AUD-101 (живой тур) правая панель инспектора клиппила подписи по слову — жёсткий `320px 1fr` → responsive `repeat(auto-fit, minmax(240px,1fr))` (стекается, не сжимается <240px). OverviewTab.jsx ✅06.07
- [ ] AUD-102 (живой тур) пустая Библиотека — снова 3 способа добавить. LibraryWorkspace empty-state
- [ ] AUD-103 (док-стейл) FEATURE_ANNOTATION_SYSTEM/UX_PROPOSALS рекламируют фикс-баги (V180/V-FEAT) как open — сверить с кодом

**Combinatorics-hunt дефекты движка (воркфлоу w5o8p6akm, 11 кандидатов → 6 confirmed → 1 refuted мной, 07.07):**
- [~] CH-1 (P0-candidate → REFUTED) `reverseComplementSegment` смешанная полярность (5′-слева/3′-справа) якобы теряет overhang → verify-агент confirmed, но НЕВЕРНО: длина сохраняется `len+δR−δL` во всех 4 комбо (top-slice владеет overhang один раз). Guard-тест `reverse-complement-segment-mixed-polarity.test.js` 3/3. Correct-код не трогал. ✅07.07 (ложное срабатывание)
- [x] CH-2 (P1, #2) `realiseAssembly` хардкодил `kind:'pcr'`+праймеры для ВСЕХ sourced-сегментов, вкл. restriction → протокол велел ПЦР-амплифицировать дайджест. Фикс: ветка `acquisitionMethod==='restriction'` → `kind:'cut'` (params enzymes/cutSites, БЕЗ праймеров) — зеркало preview opKindForMethod. re-amplified pcr/ov-pcr не затронуты. zone-pieces-to-dag.js +test (restriction→cut). ✅07.07
- [x] CH-3 (P1, #1/#4/#5) `assemblyEndChemistry` цикл `i<segs.length-1` не проходил closure-шов → варнинг T4 PNK не срабатывал для blunt/KLD-замыкания/self-closure. Фикс: опц. 5-й параметр `closure` → evaluate ring/self-closure seam; caller передаёт closureSeam. end-chemistry.js + AssemblyShellBody.jsx +4 теста. ✅07.07
- [x] CH-4 (P2, #3) `digest` excise роняли аннотацию, пересекающую рез (`else return null`) — фича молча терялась. Фикс: `_clipToBackbone` (пересечение с R_high/R_low двумя source-диапазонами) клипит straddler к backbone; straddle-обоих → 2 дуги; fully-in-excised → drop; не-straddler совпадает с прежним. restriction-db.js + test (6). ✅07.07

**RangePicker RE-cut дефекты (Игорь 07.07, живой тур — «При инверсии теряются липкие концы, если были выбраны две рестриктазы»):**
- [x] **V193 — ✅ ИСПРАВЛЕНО 07.07. Инверсия бэкбона теряет липкие концы при двух рестриктазах.** Инвертированный бэкбон двухферментной вырезки оборачивает начало кольца (ranges `[hi..len]+[0..lo]`, хранится forward), его top-strand стартует с ВЫСШЕГО реза → физический ЛЕВЫЙ конец = фермент высшей позиции. Но `segmentOverhangs` сортировал `cutSites` по позиции и назначал left=низший/right=высший, свопя только по `reverseComplement` → у обёрнутого бэкбона концы выходили ЗЕРКАЛЬНО (не тот фермент на каждом конце) → шов/гейт видели их как «потерянные». **Фикс:** обёртка флипает left/right так же, как reverse-complement → сигналы XOR-ятся: `left=высший, если РОВНО ОДИН из {reverseComplement, originWrap}`. `segment-overhangs.js` (XOR) + `zone-pieces-to-dag.js` (`originWrap: ranges.length>1` на сегменте). Тесты: `segment-overhangs-origin-wrap.test.js` 4/4 (unit) + `zone-pieces-to-dag.test.js` e2e (draftFromZone→segmentOverhangs, backbone left=BamHI/right=EcoRI; excised обратно). Single-range = `originWrap:false` → байт-в-байт прежнее назначение. bio-invariants: концы палиндромны → seq/полярность инвариантны, флипается только СТОРОНА.
- [x] **V194 — ✅ 07.07. При выборе одного сайта — возможность использовать весь линейный фрагмент (линеаризация).** «Разрезать здесь (линеаризовать)» предлагалась ТОЛЬКО при КЛИКЕ по уникальному сайту (`firstRESite`); тот же фермент, выбранный из ВЫПАДАШКИ, форс-роутился в гель (одна полноразмерная полоса = лишний крюк). **Фикс:** чистый хелпер `re-single-cut.js::resolveSingleLinearizeCut` — один фермент, режущий РОВНО 1× (клик ИЛИ выпадашка) → линеаризация; пара/мульти-каттер/2 фермента → null (гель, биол. корректно). Primary-кнопка теперь линеаризует ДО геля; `confirmSingleCut` берёт enzyme+pos из хелпера; хинт-текст «линеаризует (весь линейный фрагмент)» для обоих путей. NB: position для single-cut — только provenance (`segmentOverhangs` берёт overhang из фермента, не из позиции). `re-single-cut.test.js` 7/7 + регресс range-picker 103/103.
- [x] **V195 — ✅ 07.07. «нет, всё ещё обжирает липкие концы» — V193 чинил post-confirm канвас, а биолог смотрит на PRE-confirm ПРЕВЬЮ в модалке (воркфлоу wk8zvxyxy: 4 трейсера × адверсариальная верификация, 6/16 loss-points confirmed).** Три точки, все НЕ тронуты V193, все в превью модалки: **(1) ГЛАВНАЯ, визуал** — `SelectionOverlay.jsx:80` `stickyActive = … && !inverted`: нажатие «Инвертировать» ставит `inverted=true` → лесенка липких концов гаснет, инверт-ветка (125-130) рисует плоские `complementSegments` без ступеньки → «обжирает». Фикс: новая ветка `stickyInverted` рисует ЛЕСЕНКУ самого бэкбона — top оборачивает начало (`[sHi,len]+[0,sLo]`), bottom ступенчатый по δ каждого реза (чистый `selection-ops.js::invertedStickyStrandRanges`), а вырезаемый инсерт вуалируется STRAND-AWARE (top `[sLo,sHi]`/bottom `[sLo+δL,sHi+δR]`) — НЕ полным прямоугольником, иначе вуаль (zIndex 5) снова накрыла бы торчащий overhang-слайвер бэкбона. **(2) текст** — статус-строка `RangePickerModal.jsx:877` брала invertActive-ветку ПЕРВОЙ («Бэкбон: N bp» без меток), а `· липкие концы: L\|R` жила только в hasPiece-ветке → на инверте метки исчезали. Фикс: добавил метки в invert-ветку. **(3) метаданные** — `reInfo` (:177) строился без `originWrap`/`reverseComplement` → для бэкбона зеркально-своп. Фикс: `originWrap: invert && sourceCircular`, `reverseComplement: rc` (тот же XOR, что V193/draftFromZone) → left=высший рез. Тесты: `invertedStickyStrandRanges` 5/5 (5′/3′/blunt/clamp), `selection-overlay-inverted-sticky.test.jsx` 4/4 (лесенка на инверте + strand-aware вуаль + stagger + non-invert контроль), `range-picker-invert-sticky.test.jsx` 2/2 (метки не пропадают + свопятся). NB не loss-point: `splitWrap` single-range collapse (lo=0 недостижим — min cut=1; hi=len — узкий угол) — hardening на будущее. Регресс SequenceView 707 + full 6267.

**E2E cloning-workflow hunt (07.07, воркфлоу wbdvc3zyk — 5 сквозных сценариев молбиолога × 3-линзовая адверсариальная верификация, 12/16 confirmed). Каждый findings перепроверен кодом.**
- [x] **E2E-1 = V196 — ✅ ИСПРАВЛЕНО 07.07 (HIGH). Сборка из RE-фрагментов с НЕСОВМЕСТИМЫМИ липкими концами проходила гейт и реализовывала невозможный продукт.** `seedJunction` дефолтил метод (когда `zone.assemblyMethod` null) в `overlap_pcr` → kind `overlap`, а readiness-гейт (`incompatibleInternal`, junction-derive.js) считал incompatible ТОЛЬКО для `STICKY_JOIN_KINDS` (re_ligation/ligation/kld) — гомология-методы намеренно игнорируют RE-mismatch. Но НЕРЕШЁННЫЙ дефолт (`state !== 'decided'`) — НЕ осознанный выбор гомологии. `readiness.ready` и так падал на tentative, но кнопка Realise гейтится на `incompatible===0` → продукт всё равно собирался. **Фикс:** хелпер `countsAsIncompatible(interlock, junctionRight)` — блокирует при `interlock==='incompatible'` И (`STICKY_JOIN_KINDS` ИЛИ `state !== 'decided'`); осознанный overlap/gibson/GG (decided) по-прежнему перестраивает концы → не блок. Общий источник для readiness + `assemblyJunctionConflicts` (инвариант length===incompatible). `assembly-readiness-re-gate-v196.test.js` 5/5 + регресс readiness/junction 36 + 118 realise-filtered.
- [x] **E2E-8 = V197 — ✅ 07.07 (MEDIUM). MutationModal ложно предупреждал «олиги амплифицируют дикий тип» для standalone-плазмиды KLD.** Interior-варнинг (позиция вне ~36 нт зоны отжига терминального праймера) — верен для ФРАГМЕНТА, амплифицируемого с концов, но для whole-plasmid KLD (circular + 1 сегмент) мутагенные праймеры отжигаются ВСТЫК у мутации → любая позиция несётся. **Фикс:** проп `kldApplies` (AssemblyShellBody: `circular && segments.length===1`) гейтит `interior = … && !kldApplies`. Общий note уже упоминает KLD-доставку. `audit-b4-a15.test.jsx` +2 (kldApplies→нет варнинга; linear/фрагмент→варнинг остаётся).
- [ ] **E2E-GG-ORTHO (HIGH, 3 находки — golden-gate.js:80/170 designOverhangs/resolveConflicts МЁРТВЫЙ КОД).** Ортогональность 4-нт fusion-свесов GG/MoClo НЕ проверяется ни в одном живом пути: `designOverhangs`/`validateOverhangs` (duplicate/rc_match/palindrome) не вызывается в проде (грепа вне __tests__ = только определения). Плюс `assemblyReadiness`/`assemblyJunctionConflicts` исключают `golden_gate` целиком (junction-derive.js:234 STICKY_JOIN_KINDS), а `assemblyEndChemistry` (end-chemistry.js:61-95) гейтит GG только на «фермент выбран». → MoClo со сталкивающимися/самокомплементарными свесами зелёный, продукт скремблированный. **Фикс:** `ggJunctionConflicts(segments,junctions)` через `designOverhangs`, issues (duplicate/palindrome/rc) → блокер в `verifyAssembly` + именование шва; вывести META «уникальные 4-нт свесы» только когда реально проверено. Держать GG_ENZYMES отдельно от RE.
- [ ] **E2E-3 (HIGH — PrimerOrderPanel.jsx:92). KLD-праймеры экспортируются на заказ БЕЗ 5′-фосфорилирования** (`asTSV` хардкодит `-` в Modifications). Лигаза KLD физически не сшивает без 5′-P → нет колоний, тихий провал — самый частый реальный фейл KLD, а тул уводит от фикса. **Фикс:** флаг `phosphorylated` на олиго-записи (buildMutagenesisPayload kld → оба праймера; deriveSelfClosurePrimers kld/blunt; протянуть через derived-primer-records) → экспорт `5Phos`/код модификации. RE-концы НЕ флагать (нативный 5′-P, end-chemistry.js:36). Только ordering-метадата, продукт не трогать.
- [ ] **E2E-7 (MEDIUM — adapters/kld.js:111). Мутагенез/KLD продукт несёт мутацию в сиквенсе, но БЕЗ point-аннотации.** Нет `K77E`-метки → мутация невидима кроме сырого основания, не переносится в GenBank/библиотеку. **Фикс:** в executeMutagenesis/executeKLD добавить point-аннотацию `{id, type:'mutation', level:'point', name:label, start,end}` (0-based end-excl, координата в рамке мутанта: `fwd.length + (pos - fwdAnnealEnd)` для KLD-интерьера).
- [ ] **E2E-4 (MEDIUM, advisory — local-primer-design.js:123). `checkRepeats` не ловит AT-богатые low-complexity участки** (доминируют в межгенной ДНК Aspergillus/Trichoderma) → нет варнинга мисприминга. **Попытка 07.07 откачена, НО диагноз был неверен:** «регрессия 24 теста» оказалась **флейком таймаутов** (FLAKE-TIMEOUT-LOAD ниже), не checkRepeats — все фейлы были `Test timed out in 5000ms` в НЕ связанных подсистемах (MarkdownView/Annotator/RestrictionSites/wrap-tail), а собственные тесты E2E-4 (12/12) + прямые вызовы designPrimersLocal (39) прошли. Откат оставлен как есть (advisory, не критично). **Правильный фикс (при взятии):** dominance-порог — варнить когда low-complexity **доминирует** binding (≥~60% длины, по образцу tag ≥50%), а не просто «содержит» (безопаснее для не-fixture-специфичных праймеров с коротким повтором); прогнать транзитивные вызовы (PcrModeShell/operation-pcr-bridge) под нагрузкой отдельно (не в общем прогоне) чтобы не спутать с флейком.
- **FLAKE-TIMEOUT-LOAD (07.07, важно для верификации).** Полный `npm test` под CPU-контеншеном (два прогона параллельно / загруженная машина) даёт **10-13 файлов × ~20-24 теста «Test timed out in 5000ms»** — плавающий набор, всегда компонент-тяжёлые (Annotator auto-run/introns, RestrictionSitesWorkspace, SequenceView wrap-tail, plasmid-map-v2, MarkdownView async, pipeline-panel/auto-group/smoke). Зелёный прогон = 115с; контеншен = 294-507с. **Проверка:** эти же файлы в изоляции — 85/85 pass. Не регрессия. **Правило:** гонять `npm test` ОДИН за раз (не параллельно с фоновым прогоном); подозрительные таймаут-фейлы перепроверять запуском тех файлов изолированно ПРЕЖДЕ чем винить свой diff. **Вариант (13.07): `[vitest-pool]: Worker forks emitted error` + `Errors N errors` при `0 failed`** — тот же контеншен, но форк-краш (не per-test timeout): счётчик passed ПЛАВАЕТ между прогонами (6674↔6685), «2 errors» то появляются, то нет (второй прогон подряд — уже 0). Диагностика та же: подозрительные файлы в изоляции зелёные → не регрессия.
- [ ] **E2E-INTRON (MEDIUM/LOW, 3 находки — экспрессионная кассета молбиолога).** (a) **AA-трек игнорирует интроны в hybrid (auto/all) frame-режиме** (AATrack.jsx:494-533 + aa-opacity.js) → показывает ложный сдвинутый белок, читающий насквозь интрон; фикс — сплайс-aware строки (buildCdsAARows/walkSplicedIntoRows) побеждают сырые кодоны на своей (frame,strand). (b) **`spliceRegion` слепо верит координатам интрона** (intron-utils.js:213-246 + codon-walker.js) — нет GT-AG donor/acceptor-чека и frame-гарда → off-by-one граница экзона тихо даёт сдвиг рамки; фикс — non-blocking флаги `nonCanonicalSplice`/`matureLenMod3` + бейдж-варнинг. (c) **LOW:** несвязанный интрон усыновляется ЛЮБЫМ охватывающим translatable-регионом (intron-utils.js:186-194) — вложенный/marker CDS ошибочно сплайсится; фикс — предпочесть строго меньший внутренний регион.
- [ ] **E2E-11 (LOW — JunctionBlock.jsx:312). Легаси GG-панель печатает хардкод-бейдж «уникальный» без кросс-junction сравнения.** Компонент не смонтирован (dormant) → риск при будущей реинтеграции. **Фикс:** убрать бейдж (одиночная панель не может верифицировать ортогональность) либо считать коллизии в родителе.

## FIXED — assembly audit (14.06.2026, interface-audit; ждёт визуальной приёмки)

> Не sprint-задача — находка interface-аудита по ветке `feature/assembly-tab-workspace`. Версия НЕ бампается. Git/трекеры — за Игорем.

**V146 — ✅ ИСПРАВЛЕНО 14.06.2026. Праймеры, нарисованные в PcrModeShell, не попадают в экспорт заказа/протокола.** Найдено interface-аудитом. **Корень:** `executeSingleTemplatePCR` (`canvas/operations/adapters/pcr.js`) в ветке `userPrimers` эмитил ТОЛЬКО amplicon-контейнер, в отличие от ветки auto-design (которая вдобавок зовёт `makeDesignedOligoContainer` → output `oligonucleotide`). Праймеры, которые пользователь спроектировал в вьювере (`op.params.userPrimers`) и «заказал» через `OrderOligosConfirmGate`, никогда не становились `oligonucleotide`-контейнером → невидимы для (1) `PrimerOrderPanel.collectOligoPrimers` (TSV/FASTA-экспорт олигов сканирует только `kind==='oligonucleotide'`) и (2) `protocol-export.js` `stepPCR` + `buildReagentsBlock` (строки праймеров берутся из output oligonucleotide-контейнеров или `params.primerPairId`, никогда из `params.userPrimers`). **Фикс:** новая фабрика `makeUserOligoContainer` (`adapters/_shared.js`) — зеркалит `makeDesignedOligoContainer`, но источник fwd/rev — пара пользователя; в контейнер кладётся ПОЛНАЯ заказываемая последовательность (`forward`/`reverse` с 5′-хвостами — синтезирует вендор), Tm = пользовательский Tm-на-связывании (конвенция Tm-on-binding), GC считается из полной последовательности; имена берутся из `fwdName`/`revName`. Ветка `userPrimers` теперь возвращает `[amplicon, oligo]` (паритет с auto-design). **Тест (red→green):** `skeleton-adapters-s2.test.jsx` — ветка `userPrimers` возвращает 2 output'а (amplicon + oligonucleotide) с fwd/rev пользователя; отдельный кейс — олиго несёт ПОЛНУЮ seq с 5′-хвостом + Tm + имя. Full Vitest 4624 pass / 18 skip / 0 fail, `vite build` clean.

---

## FIXED — workspace UX (12.06.2026, скриншот Игоря; ждёт визуальной приёмки)

> Не sprint-задача — прямой запрос Игоря по скриншоту Sequence-вида двухуровневого верстака (M-WORKSPACE R1/R2). Версия НЕ бампается. Git/трекеры — за Игорем.

**V143 — ✅ ИСПРАВЛЕНО 12.06.2026. Каретка курсора невидима в редакторе сборки.** Найдено Игорем по скриншоту («исчезает курсор, при этом курсор работает просто его не видно»). **Корень:** `CaretOverlay` скрывается через `hidden={selectedPrimers.length > 0}` (`SequenceView/index.jsx`) — каретка гаснет, пока выделен ЛЮБОЙ праймер, а `selectedPrimers` чистится ТОЛЬКО по Del. В ассемблере праймеры (asm-fwd/asm-rev) под рукой → клик по праймеру гасит каретку насовсем, даже при правке сиквенса → элемент возвращает `null` («исчезает»). Поведение задумано для read-only Library-вьюера (Del → праймер). **Фикс:** `hidden={!editable && selectedPrimers.length > 0}` — в EDITABLE-виде (ассемблер) каретка остаётся видимой при выделенном праймере; read-only — без изменений (Del по-прежнему таргетит праймер, его keydown-гард смотрит на `selectedPrimers`, не на каретку). **Тест:** `primer-delete-keydown.test.jsx` — editable + клик по праймеру → `sequence-view-caret` присутствует; существующий read-only hide-on-select сохранён.

**V145 — ✅ ИСПРАВЛЕНО 12.06.2026. Ромб-стык не читается как кликабельный.** Найдено Игорем по скриншоту области стыка («плохо читается область стыка, непонятно можно на неё нажать или нет»). **Корень:** глиф стыка (и на полосе сиквенса `SegmentZonesOverlay`, и в «блоке сборки» `AssemblySegmentBar`) рисовался как голый ~9px ромб без hover-состояния и без фоновой «подложки» → читался как декоративный маркер, не как контрол. **Фикс:** ромб обёрнут в круглую кнопочную «подложку» (`.bg-junction-chip` внутри `.bg-junction-btn`) — при наведении подложка подсвечивается + scale (1.22) + тень (CSS в `index.css`, токены дизайн-системы: `--accent-500` terra cotta, `--surface-1/2`); увеличен хит-таргет; в title добавлено «— нажмите, чтобы настроить». Состояния сохранены: decided = залитый ромб + сплошная рамка подложки в цвете метода; tentative = полый пунктирный ромб + нейтральная рамка. Применено к ОБОИМ глифам (полоса + блок-бар) для консистентности. **Тесты:** `junction-glyph-state-ux` + `assembly-segment-bar` — кнопка `.bg-junction-btn` с круглой подложкой `.bg-junction-chip`, ромб `[data-testid="junction-diamond"]` (decided залит / tentative прозрачен); существующие decided/tentative + onZoneClick сохранены.

**V144 — ✅ ИСПРАВЛЕНО 12.06.2026. Панель «Происхождение» (LineagePanel) перекрывает/блокирует низ окна.** Найдено Игорем по тому же скриншоту («происхождение закрывает окно»). **Корень:** `LineagePanel` — canvas-эра оверлей, пришпилен `absolute; bottom:20; left:24` к `CanvasArea` без `pointer-events`. В двухуровневом верстаке `CanvasArea` целиком занят активным видом сборки → панель ложится поверх футера SegmentList «Источник» + тулбара «+ Сегмент» / «Codon stats» И, без `pointer-events:none`, ест клики. **Фикс:** перенос в `top:12; right:12` (пустой жёлоб таб-стрипа/хедера) + `pointerEvents:'none'` — чисто информационный чип происхождения, не закрывает и не блокирует поверхность редактирования. **Тест:** `lineage-panel-position.test.jsx` — `pointerEvents==='none'`, не bottom-pinned (`style.bottom===''`, `style.top` задан), цепочка происхождения рендерится.

---

## FIXED — canvas realise UX (11.06.2026, прямой запрос Игоря; ждёт визуальной приёмки)

> Не sprint-задача — прямой запрос Игоря по скриншотам. Версия НЕ бампается. Git/трекеры (PROJECT_STATE/RELEASES/DECISIONS) — за Игорем.

**V142 — ✅ ИСПРАВЛЕНО 11.06.2026. Сборка дублируется при повторной «Реализации».** Найдено Игорем по скриншоту (зона «16 узлов» = 2× всё: два frag-1, два frag-2, два Gibson, два product). **Корень (пред-существующий, усилен снятием модалки):** `handleAssemblyRealise` (`store/skeleton-state.js`) делал `operations:[...state.operations, ...diff]` БЕЗ удаления прежнего вывода, а для ЗОНЫ `nextRev` всегда 1 (revision-cap — концепт legacy-драфта) → каждый клик «Реализовать» доклеивал полную копию DAG. Раньше модалка делала realise 2-кликовым (реже случайный повтор); прямой realise (этой же сессии) клик-в-клик умножал. **Фикс:** новый чистый хелпер `pruneZoneRealiseOutput(state, zoneId)` (`lib/zone-pieces-to-dag.js`) — перед добавлением diff сносит ПРЕДЫДУЩИЙ вывод realise этой зоны (каждый созданный realise op/container несёт `origin.assemblyId`; junctions — `realisedFrom.assemblyId`; источники тега не несут → переживают). **Только для зоны** — legacy-драфты сохраняют multi-revision накопление (design-variants, DEC-REAL-08). Re-realise теперь **идемпотентен** (само-лечится: повторный клик схлопывает уже наплодившийся дубль в один DAG). `skeleton-state.js` остался под hard (24.89/25 — хелпер вынесен). **Тесты:** unit `pruneZoneRealiseOutput` (prune-by-tag + same-ref fast-path) + integration в `zone-assembly-realise.test.jsx` (двойной клик → counts не удваиваются, ровно один `realised-product`). Full Vitest 4472 pass / 17 skip / 0 fail, build clean.

**Сопутствующее (review-driven, та же сессия):** убрана модалка «Реализовать как DAG» (битый SVG-preview после фикса топологии 10.06 → прямой realise + тост из редьюсера, единый источник); зона на канвасе сделана регулируемой (видимые угловые ручки + `SET_ZONE_AUTO_RESIZE` opt-out на ПЕРВОМ движении ресайза — не на голый клик; кнопка «⤢ подогнать»). Двойной тост и false-success на revision-cap устранены снятием компонентного тоста.

**Канвас-рендер графа (та же сессия, по скриншотам Игоря).** (1) **Стрелки шли «криво / из центра» — корень:** код рёбер в `ZoneGraphContent` выбирал размер узла по `fromNode.kind` (всегда `undefined` — kind лежит в `node.data.kind`, реальный размер — top-level `node.width/height`) → ВСЕ рёбра якорились op-размером 120×60, даже для блоков 240×150 → линия выходила из горизонтальной середины / у верха прямоугольника. Фикс: рёбра берут `node.width/height`; `edgeAnchors(...,{flow:'LR'})` форсит горизонтальный right→left поток (не 4-сторонний авто, который плёл углы на merge 2→1); offset-aware кривизна для пологого изгиба. (2) **Канвас компактнее:** `computeGraphPositions` кормит dagre реальными per-node размерами (ромб 120×60 ≠ блок 240×150), `computeAutoLayout` конвертит центр→top-left по собственному размеру узла (раньше единый 270×180 смещал ромбы на (75,60)); `ranksep` 80→56, `nodesep` 50→36. (3) **Кнопка «⛶ под размер сборки»** в zoom-контролах (`contentBBox`+`fitZoomToContent`, zoom+центрирование; инлайн-SVG, т.к. глиф ⛶ рендерится пустым в Windows-UI-шрифтах — проверено в живом приложении). Тесты (red→green): `dag-layout` per-node `sizeOf`; `contentBBox`/`fitZoomToContent`; `edgeAnchors` `flow:'LR'`; регресс в `zone-frame-graph-render` (ребро от блока стартует в `(left+240, top+75)`). Full Vitest **4484 pass / 17 skip / 0 fail**, `vite build` clean.

> **Отложено (не реализовано в этой сессии):** in-zone op-popup на клик по ромбу (паритет с `CanvasGraphView`: picker для draft / `OpPopupRouter` для committed, position-anchored на CanvasLayoutView). Сейчас клик в зоне диспатчит `OPEN_EDITOR_OP_TAB` (K4-минимум). Исследовано (CanvasGraphView/OpPopupRouter/op-kinds-registry — эталон готов), реализация — отдельная `.2`-задача.

---

## FIXED — live-junction ENGINE слои 1–2 (приёмка 05.06.2026)

> Часть набора live-junction сборки (4 спеки в `docs/`); это слои 1–2 из 6. **Версия НЕ бампается** (mid-spec). Канон ориентации хвостов — DEC-PRIMER-TAIL-01 (DECISIONS). Коммиты: `20d2978` (слой 1, pydna-диагностика), `4b9e5d5` (слой 2, `buildOverlapTail` + V130 + Tm), `ba423c1` (V130-тест). Git-коммит — за Игорем в терминале.

**V130 — ✅ ИСПРАВЛЕНО + принято (слой 2 PASS) 05.06.2026. realise роняла overlap-хвосты авто-праймеров сборки (tailless-материализация).** `deriveAutoPrimers` (`components/CanvasSkeleton/lib/primer-derive.js`) эмитит праймеры `source.kind='auto-group'` (с хвостами overlap/GG/RE) в `state.assemblyDraftPrimers[zoneId]`. `mapPrimersForSegment`/`realiseAssembly` (`components/CanvasSkeleton/lib/zone-pieces-to-dag.js`) матчил ТОЛЬКО `source.kind ∈ {segment, boundary}` → auto-group мимо → fallback `autoPrimerPair(seg.sequence)` = `slice(0,20)`/`rc(slice(-20))`, tm:0, БЕЗ хвостов (тихий неверный продукт — сборка физически не соберётся). **Фикс (слой 2, `4b9e5d5`):** `mapPrimersForSegment` матчит auto-group по `source.pieceId === seg.id` независимо от kind; `getBoundaryPrimerInfo` берёт праймер с самым длинным хвостом на стыке (односторонний overlap делает upstream-rev пустым). **Тест (`ba423c1`):** новый `describe` в `zone-pieces-to-dag.test.js` — `zoneStateWithGroup()` (CREATE_OP_GROUP `overlap_pcr` → 4 хвостатых auto-group драфта) → `realiseAssembly(s,'zn-1',{0:'gibson'},{})` → per-op ассерты `userPrimers[0].source==='assembly'` (НЕ `'auto'`-fallback), `fwdTm!==0`, `forward===` draft-fwd с хвостом + односторонний overlap (downstream fwd > binding, upstream rev = binding-only). До фикса вернулось бы `source:'auto'`/tm0/20нт. Code-verify §9b PASS (assertion на данные сильнее визуала, не зависит от сломанного canvas-UI — V139). Full Vitest 4328 pass / 17 skip / 0 fail, build clean.

**V131 — ✅ ИСПРАВЛЕНО + принято (слой 2 PASS) 05.06.2026. GG- и RE-хвосты `deriveAutoPrimers` расходились с принятыми V124/V125 (тот же класс ориентации сайта).** `primer-derive.js`: GG rev был `rc('GGTCTCN'+oh)` (rc ВСЕГО recognition+spacer+overhang → digest BsaI оставляет низовой конец BLUNT, не лигируется); RE fwd был `reSite+'GG'` (защитные основания ВНУТРИ, нет 5′-фланкинга → плохой/нулевой рез forward-плеча). Фиксы V123/V124/V125 легли только в `local-primer-design.js` (канвасом НЕ используется) → primer-derive их не получил. **Фикс (слой 2, `4b9e5d5`, через единый `buildOverlapTail`):** GG rev → `recognition + spacer + rc(oh)` (recognition НЕ rc, V124; pydna-proven — оба designed 5′-overhang'а AATG+AAGC лигируются); RE fwd → protective СНАРУЖИ сайта (V125; NEB-данные о терминальном резе); литерал спейсер `'N'`→`'A'`. Overlap-ветка primer-derive уже была верна (fwd дословно / rev rc). pydna-эталоны `tools/pydna/primer_tail_golden.json` (слой 1, `20d2978`): GG-инвариант перевёрнут `it.fails`→`it`. Full Vitest 4328 pass / 17 skip / 0 fail, build clean. **Не входит в V131 (→ TECH_DEBT):** per-enzyme recognition (BsaI-хардкод `GGTCTC` / protective `GCGC`, RE rev только 2 нт фланка vs fwd 4 нт) — пробрасывается из JUNCTION; ручной `buildAssemblyPrimer` A1-паритет (несёт старый хвост до унификации на `buildOverlapTail`).

---

## FIXED (v0.8.4-alpha — приёмка 31.05.2026)

> Трекер сброшен 27.05 (нумерация с V118), ничего старше 2 спринтов → архивация не требуется.
> **Все записи ниже приняты Игорем 31.05.2026 («всё принято»)** — пометки «Ждёт приёмки» / «Ждёт визуальной приёмки» в телах записей считать снятыми.
> Git-коммит фиксов (math/bio + partial) — за Игорем в терминале.

**V119 — ❌ ОТОЗВАНО 28.05.2026 — НЕ БАГ. Вырожденно-палиндромные сайты якобы считаются дважды.** **Снято при попытке фикса:** эмпирическая проверка (probe + полный прогон) показала, что `findSitesInSequence` возвращает РОВНО 1 хит для HincII/StyI/PpuMI — двойного счёта НЕТ. Ошибка исходного диагноза: `restriction-db.js` использует СОБСТВЕННЫЙ локальный `reverseComplement` (стр.108-113) с полной IUPAC-таблицей, а НЕ sequence-utils-версию (которая теряет коды → V118). Поэтому `rcSite('GTYRAC')==='GTYRAC'` → палиндром-skip срабатывает корректно → обратная цепь не сканируется второй раз. Правок в restriction-db не требуется. (V118 — реальный и НЕЗАВИСИМЫЙ: это ДРУГОЙ revComp в sequence-utils.)

### Критичные

**V123 — КРИТ: overlap/Gibson/OE-PCR праймеры не имеют общего перекрытия → сборка физически не соберётся.** Найдено Code math/bio-аудитом 28.05 (доказано симуляцией). **✅ ИСПРАВЛЕНО 28.05.2026** — все ветки `overlapTail` (split WT / split overlapSequence / left_only / right_only) + single-circular self-closure приведены к стандартной Gibson/OE-PCR конвенции (подтверждена исходниками pydna `assembly_fragments`: fwd-хвост = 3′-конец соседа как есть, rev-хвост = `rc(5′-начала соседа)`). Добавлен biology-invariant тест в `local-primer-design-overlap.test.js` (реконструкция амликонов → проверка общего overlap + бесшовной сшивки), переписаны 3 теста, фиксировавших инвертированные хвосты. Full Vitest 4083 pass / 0 fail, build clean. `local-primer-design.js:180-194` (`overlapTail`). Затронуты ВСЕ overlap/Gibson/OE-PCR сборки ≥2 фрагментов + single-circular self-closure. KLD/RE-ligation/Golden Gate идут другими ветками — не задеты. Канон — DEC-PRIMER-TAIL-01.

### Высокие

**V124 — Golden Gate: reverse-primer Type IIS хвост инвертирует recognition → BsaI-сайт у 3′-терминуса смотрит НАРУЖУ → это плечо не режется → GG не собирается.** Найдено Code math/bio-аудитом 28.05 (тот же класс, что V123). **✅ ИСПРАВЛЕНО 28.05.2026** — rev-хвост GG теперь `recognition + spacer + rc(oh)` (recognition БЕЗ rc, как в верном forward-плече). Добавлен Type IIS-симулятор дайджеста в `__tests__/primer-tail-orientation.test.js`: оба плеча режутся внутрь → комплементарные overhang'и (`rc(ohR)===ohL`, `===` designed oh). Full Vitest 4086 pass / 0 fail, build clean. `local-primer-design.js` (`overlapTail`, ветка `golden_gate`): rev = `enz.recognition + spacer + rc(oh)`. Канон — DEC-PRIMER-TAIL-01.

**V125 — RE-ligation: forward-primer RE-хвост реверс-комплементирован → сайт впритык к 5′-концу без защитных оснований снаружи → плохой/нулевой рез forward-плеча.** Найдено Code math/bio-аудитом 28.05 (тот же класс, что V123/V124). **✅ ИСПРАВЛЕНО 28.05.2026** — RE-ветка теперь `return tail` (literal) для обеих сторон; forward-продукт получает защитные основания СНАРУЖИ сайта (5′-фланкинг). Тест в `__tests__/primer-tail-orientation.test.js` (forward RE-сайт имеет ≥1 нт 5′-фланкинга). Full Vitest 4086 pass / 0 fail, build clean. `local-primer-design.js` (`overlapTail`, ветка `ligation`/`re_ligation`): `return tail;` для обеих сторон. Канон — DEC-PRIMER-TAIL-01.

**V180 — ✅ ИСПРАВЛЕНО 29.06.2026 (найдено аудитом фич). Правка координат региона сиротила его саб-фичи (introns/domains/points).** `updateAnnotation` ([annotation-edit.js](gui/designer/src/lib/annotation-edit.js)) перегенерирует `id` региона на детерминированный `region:start:end:type:name` при изменении start/end/type/name (даже если был UUID); дети `detail`/`point` с `regionId = старый id` НЕ каскадировались → сироты (пропадал exon-блок на карте, AA-сплайсинг, FeatureEditorModal не находил `a.regionId === feature.id`). Нарушен инвариант №4 (docs/FEATURE_ANNOTATION_SYSTEM.md). Биология: подвинул координаты гена → интроны/домены тихо отвалились. **Фикс (TDD):** `updateAnnotation` отслеживает сдвиг id (`prevId = a.id || generateAnnotationId(a)` → `merged.id`) и при изменении каскадно переписывает `regionId` всех детей со старого id на новый (второй проход по массиву; no-op если id не менялся или у фичи нет детей). Сохранена существующая семантика регенерации id (тесты id-контракта зелёные). 6 новых тестов в `annotation-edit.test.js` (coord/rename/strand-only/point-children/legacy-без-id/не-трогать-чужих). Full Vitest 6113 pass / 0 fail. **Остаётся отдельно (НЕ блокер, тот же класс):** `splitAnnotation`/`mergeAnnotations` тоже меняют id региона — дети там могут осиротеть; редкий путь, отдельная правка по запросу.

### Средние

**V138 — ✅ ИСПРАВЛЕНО + принято визуально 31.05.2026 (DEC-FDP-01). Бокс CDS-фичи не покрывал всю ДНК гена: белковый детектор подрезал прямоугольник к кодонной сетке (≤2 nt), импорт голого сиквенса держал полный.** Найдено Игорем 31.05 (после приёмки V134), сравнение «Annotator vs импорт голого сиквенса». Воспроизведено Chat на реальном sfGFP. **АА верны — дело только в боксе** (Игорь подтвердил дважды). AA-дорожка `AATrack` (single) уже выбирает кадр через `pickReadingFrame` (минимум стопов, не start%3 — V133) и транслирует только полные кодоны внутри бокса → АА корректны независимо от ширины бокса, AATrack НЕ трогать. Расходилась только ширина БОКСА: `feature-detection.js` блок `protein_partial` маппил protein-матч на кодонную сетку (`frame.offset+k*3`) → бокс на ≤2 nt короче ДНК гена во фрагменте с разрезом посреди кодона. **Фикс (провалидирован Chat-харнессом, fwd+rev):** в блоке `protein_partial`, после кодонно-сеточных `ntStart/ntEnd`, nt-refine каждого края — тянуть наружу ≤2 nt, пока ДНК-таргет совпадает с собственной ДНК фичи `feat.sequence` (strand-aware), стоп на мисматче/краю; cap=2 ограничивает совпадение со фланком. Только `protein_partial`; `protein_exact`/`protein_fuzzy` и `dna_partial` — НЕ тронуты. Валидация: bare GFP-фрагмент fwd/rev → бокс `0..len` полный = как импорт; embedded → клип во фланк ≤2 nt (cap); exact/fuzzy не задеты.

**V134 — ✅ ИСПРАВЛЕНО + принято визуально 31.05.2026 (DEC-FDP-01). Partial mis-fragmentation: неполный фрагмент фичи с правкой на стыке либо дробился на два `_part_` (A), либо терял клочок < floor (B, ~45 nt чистого GFP пропадали).** Найдено Игорем на приёмке partial 31.05 — два кадра: A `_part_7-213`+`_part_217-684`; B `_part_217-585` с потерянной головой 172-216. Воспроизведено Chat (20+ сценариев, реальный sfGFP из `common-features.json`). Один корень: `extendSeedPartial` вставал на ≥2-кодонном мисматч-ране, а отколотый кусок < `minLen` (17 aa/50 nt) не выпускался → merge склеивать нечего. **Фикс (2 слоя, провалидирован Chat-харнессом):** Слой 1, главный — заменить оконный разрыв в `extendSeedPartial` на BLAST-style X-drop (score +1/−1, граница = пик/snap-back, protein `xdrop=8` / DNA `xdrop=20`; финальный identity-гейт + coverage-floor — бэкстоп): мостит замену-ран → ОДИН кусок с точными границами (чинит A и B сразу). Слой 2, вторичный — `mergeCollinearPartials` выпущенных кусков для indel'ов, которые X-drop не мостит (И featureGap И targetGap ≤ `PARTIAL_MERGE_MAX_GAP`=18 → слить; вставка ≥30 nt → split). Провалидировано: чистый фрагмент 217-630 → ровно `_part_217-630`; стык-ран → один кусок; вставка 3/6/15 nt → слить, 30/90 → split; варианты GFP (fuzzy/exact) без изменений.

**V133 — Annotator Level 1: устаревший результат «прилипает» к новой плазмиде (чужой хит мапится, пока не нажмёшь «Run again»).** Найдено Игорем на partial-приёмке 29.05. `Annotator/index.jsx` авто-ран L1 ключевался по `scope.sequenceId` + гейт `if (results[L1]) return`. В embedded-режиме `sequenceId` залипает → старый L1-хит остаётся в `annotator.results` и рисуется на новой последовательности (тихий неверный результат). **✅ ИСПРАВЛЕНО 29.05.2026** — авто-ран ключуется по СОДЕРЖИМОМУ `sequence` (deps `[annotator.open, sequence]`, `autoRunFiredFor` по контенту); при смене контента → `resetAnnotatorScope()` + ре-ран. Идемпотентность сохранена. Тест в `annotator-autorun.test.jsx`. Full Vitest 4110 pass / 0 fail, build clean.

**V127 — сборщик последовательностей показывает координаты фич/сегментов с 0 (должно быть 1-based).** Найдено в диалоге с Игорем 29.05. Хранение 0-based, end-exclusive (⚓ DEC-ANN-10), UI везде 1-based — НО в сборщике (`assembly-mode`) четыре места печатали сырой 0-based `start`. **✅ ИСПРАВЛЕНО 29.05.2026** — `RangePickerModal` (`featureLabel` + start-инпут) и `SegmentList` (`rowSource` + inline-редактор) переведены на 1-based дисплей через `toUiCoords`/`fromUiCoords`; хранение/slice/onConfirm/updateSegmentRange/caretAnchor не тронуты. Новый `assembly-coords-1based-v127.test.jsx` + обновлены 5 ассертов в 4 файлах. Full Vitest 4097 pass / 0 fail, build clean. ✔ Визуальная приёмка пройдена 29.05.2026 (Игорь: «стало с 1»).

**V118 — `reverseComplement` молча теряет IUPAC-коды → N.** Найдено Code-аудитом 28.05. `sequence-utils.js`. `sanitizeSequence` при вводе ambiguity-коды СОХРАНЯЕТ, а `reverseComplement` маппил R/Y/S/W/K/M/B/D/H/V → 'N'. Радиус: feature-detection (−цепь), orf-detection (rcSeq), golden-gate `checkInternalSites`, primer design на −цепи. **НЕ затрагивает рестрикцию** (свой локальный IUPAC-корректный revComp, см. отзыв V119). **✅ ИСПРАВЛЕНО 28.05.2026** — `COMPLEMENT_MAP` расширен до полной IUPAC-таблицы (R↔Y, M↔K, S↔S, W↔W, B↔V, D↔H, N↔N + lowercase). Тест `__tests__/iupac-revcomp.test.js` + переписан `sequence-utils.test.js`. Full Vitest 4091 pass / 0 fail, build clean.

**V122 — digest: аннотации, пересекающие линию реза, ломаются или теряются.** Найдено Code-аудитом 28.05. `restriction-db.js`: `_shiftAnnotations` при linearize — аннотация через `cutPos` получала `start > end`. **✅ ИСПРАВЛЕНО 28.05.2026 (linearize)** — `_shiftAnnotations` теперь split'ит straddling-фичу на ДВЕ валидные дуги (`[tail..seqLen]` + `[0..head]`); тест в `restriction-digest.test.js`. Full Vitest 4094 pass / 0 fail. **Остаётся отдельно (НЕ блокер, follow-up):** excise-ветки (`_exciseTwoEnzymes`/`_exciseSameEnzyme`) всё ещё ДРОПают straddling-аннотации — lossy, но НЕ corrupting (вывод валиден); split там — отдельная правка по запросу.

### Низкие

**V182 — ✅ ИСПРАВЛЕНО 29.06.2026 (найдено в UX-1). Кольцевая карта не делала exon-split интронов, размеченных по стандарту (`regionId`).** **Фикс (TDD):** `collectIntronsByParent` теперь группирует по `a.regionId ?? a.parentId` (regionId канон — `annotate-genes.js:41` эмитит интроны с regionId; parentId legacy-fallback); detail-слой PlasmidMapV2 исключает из дуг интрон с ЛЮБЫМ родителем (`regionId!=null||parentId!=null`) → нет двойной отрисовки. LinearMapV2 не exon-split-ит — не затронут. Контракт-тест ANN3 переписан + 1 lib-тест; plasmid-map-v2 70 pass. [исходный анализ ниже] — `collectIntronsByParent` ([lib/plasmid-map-v2.js:69-78](gui/designer/src/lib/plasmid-map-v2.js)) собирает интроны по полю `a.parentId`, но модельный стандарт связи саб-фичи с родителем — `regionId` (annotation-contract, getDetails фильтрует `a.regionId === regionId`; intron-detection эмитит интроны с regionId). → стандартные интроны НИКОГДА не split-ятся в exon-блоки на PlasmidMapV2 (фича `exonSegments` эффективно мертва для них). **Митигировано в UX-1:** detail-слой PlasmidMapV2 рисует regionId-интрон вложенной дугой (виден), но без красивого exon-split (только parentId-интрон split-ится). **План фикса:** `collectIntronsByParent` читать `a.regionId ?? a.parentId` (+ обновить detail-фильтр исключать обе формы exon-split интронов). Не блокер — интроны видны как дуги.

**V181 — ✅ ИСПРАВЛЕНО 29.06.2026 (UX-2). Нет двусторонней подсветки выделенной фичи карта↔сиквенс.**
Было: `AnnotationTrack` не читал `selectedRegionId`; выделение на карте не подсвечивало фичу в сиквенсе. **Фикс (TDD, 6 файлов):** AnnotationTrack принимает `selectedRegionId` → accent-обводка совпавшей фичи (data-selected); проводка SequenceView→SequenceLine→AnnotationTrack; в Library-инспекторе `LibrarySingleInspector` держит общий `selectedRegionId`, ставит его в `onNavigateToFeature` (клик фичи на карте/списке) + сбрасывает на смене записи, прокидывает в OverviewTab (PlasmidMapV2 self-highlight) и SequenceTab→SequenceView (+ обратный путь onAnnotationClick). Вкладки Overview/Sequence не co-visible (единственный split-pane PlasmidWorkspace — мёртв), поэтому sync = подсветка переживает переключение вкладок. Тесты: annotation-track (2) + index-composition (1b) + инспектор-регресс. Full 6125 pass/0 fail + build. **Старая запись ниже (была открыта):**
~~Нет двусторонней подсветки выделенной фичи карта↔сиквенс.~~ `AnnotationTrack` ([SequenceView/tracks/AnnotationTrack.jsx](gui/designer/src/components/SequenceView/tracks/AnnotationTrack.jsx)) не принимает и не читает `selectedRegionId` (grep пуст). Клик по фиче на кольцевой/линейной карте (`onSelectRegion(id)` → `selectedRegionId`) не подсвечивает её в сиквенс-вьюере; обратный путь (выделение в сиквенсе → подсветка на карте) тоже не подключён. Фича работает «на 3+» — выделение есть на картах, но не синхронизировано с треком фич сиквенса. **План фикса:** проброс `selectedRegionId` в AnnotationTrack + подсветка строки по `region.id === selectedRegionId` (учесть legacy-без-id через `matchesAnnotationId`); обратный onSelectRegion из клика по фиче в сиквенсе. Не блокер.

**V129 — RC-ориентация сегмента не видна у координат (флаг «скрыт» в дальней RC-колонке).** Найдено Игорем на приёмке V127 (29.05). **Решение (конвенция):** числа НЕ переворачиваем (координаты = диапазон в источнике, по возрастанию — GenBank `complement(1..69)`), добавляем явный маркер: `pUC19 [1:69] ←RC`. **✅ ИСПРАВЛЕНО 29.05.2026** — `SegmentList.rowSource` (`reverseComplement` → ` ←RC`); тесты в `assembly-coords-1based-v127.test.jsx`. Full Vitest 4105 pass / 0 fail, build clean. ✔ Визуальная приёмка пройдена 29.05.2026 («работает»).

**V128 — audit close-out: enrich мутировал чужие объекты + detail/point без write-path id + генератор id на Math.random.** Найдено Code-аудитом модуля аннотаций 29.05 (продолжение V127, три не-блокера). **✅ ИСПРАВЛЕНО 29.05.2026** — (1) enrich клонирует каждую аннотацию на входе (`annotations.map(a => ({...a}))`) + новый common_db-region получает `id`; (2) `importFeatures` / `autoAnnotate` / `migratePartAnnotations` стампят `id` на каждую аннотацию всех уровней перед возвратом (write-path TD-IMPORTER-NO-ID закрыт); (3) новый `lib/ids.js::makeId` (crypto.randomUUID feature-detect), `generateRegionId` делегирует. Тесты: новый `annotation-write-path-ids.test.js` + 2 в `enrichment.test.js`. Full Vitest 4103 pass / 0 fail, build clean. Остаток: read-path net для legacy detail/point оставлен открытым (TD).

**V126 — auto-annotate: детекция стоп-кодона «в конце» использует неверную формулу позиции последнего кодона для регионов с длиной не кратной 3.** Найдено Code math/bio-аудитом 28.05. `auto-annotate.js:70` — `lastCodonPos = Math.floor((upper.length - 1) / 3) * 3`. Для длины `L%3 ∈ {1,2}` указывала на НЕПОЛНЫЙ хвостовой кодон и пропускала реальный последний полный in-frame кодон. **✅ ИСПРАВЛЕНО 28.05.2026** — формула заменена на `(Math.floor(upper.length / 3) - 1) * 3`. Тест `__tests__/auto-annotate-stop.test.js` (стоп находится при L%3≠0 + регрессия L%3==0). Full Vitest 4094 pass / 0 fail.

---

## REV#2 Stage 3 — K1 round-2 дефекты (ревью Игоря 15.07; UI-примитивы `components/Search/`, git untracked)

Найдено адверсариальным ревью round-2. K1 ещё НИГДЕ не смонтирован → пользовательской регрессии НЕТ, но фундамент чиним до интеграции (дешевле/безопаснее сейчас). TDD-first: red-тест ловит дефект ДО фикса. Био-значимость (косвенная): эти дыры позже могли открыть НЕ ТОТ объект — заблокированное поле всё ещё выбирает строку; старое выделение оживает в новой выдаче; два поиска цепляются к чужому popup → Enter/клик открыл бы не тот праймер/шаблон/фермент.

**✅ ИСПРАВЛЕНО 15.07 (round-2), TDD-first.** K1-R2-1..8 + доп-a/b/c закрыты; кластер `components/Search` **92/92 pass**, scoped lint 0 errors, полная сюита **6933 pass / 0 fail** (чистый прогон, флейков нет).

- [x] **K1-R2-1 — Disabled fail-open.** `disabled=true, open=true`: popup виден, клик мышью зовёт `onSelect`. `SearchCombobox.jsx:89` рендерит listbox по `{open && …}` без учёта disabled; `SearchResultsListbox.jsx:63` onClick зовёт onSelect без гарда. Фикс: combobox не рендерит popup при disabled; listbox принимает `disabled`, не выбирает/не ставит mousedown-гард. Тесты: open+disabled → popup нет; open→disabled → закрыт; click при disabled не выбирает.
- [x] **K1-R2-2 — Active key переживает границу запроса.** Исчезнувший результат остаётся в state и сам активируется при повторном появлении в НОВОЙ выдаче. `useSearchComboboxNavigation.js:40`. Фикс: проп `sessionKey` — active держится внутри partial→final одной сессии, сбрасывается при новом запросе (reset во время рендера при смене sessionKey, БЕЗ эффекта). Тест: тот же sessionKey сохраняет active; смена sessionKey → active=null, reappearing не оживает.
- [x] **K1-R2-3 — ARIA id не уникальны/не стабильны.** Два combobox без явного id → одинаковый `search-listbox` (`SearchCombobox.jsx:31`); option-id из индекса переезжает между сущностями после reorder (`searchUiContract.js:52`). Фикс: `useId()` для дефолтного listboxId; `optionDomId(listboxId, optionKey)` от стабильного key (санитизация whitespace). Тесты: два combobox → разные id; option-id стабилен при reorder.
- [x] **K1-R2-4 — IME защищён только на уровне клавиш.** `onChange` отдаёт значение контроллеру без признака composition (`SearchField.jsx:91`) → K3-parser разберёт промежуточный `:` до compositionend. Фикс: `onValueChange(value, {isComposing})` (ref + `nativeEvent.isComposing`). Интеграционный тест через настоящий input (compositionStart→change→isComposing:true; compositionEnd→change→false).
- [x] **K1-R2-5 — Focus/open lifecycle не завершён + контракт-противоречие.** `SearchCombobox` не принимает/не пробрасывает focus/blur; нет close-on-blur/click-away и гарантии focus-after-selection; K1.0-док: «open принадлежит combobox», реализация хранит open на поверхности. Фикс: ОДИН контракт — combobox ВЛАДЕЕТ open (uncontrolled), open-on-arrow/type, close-on-blur/escape/select/Tab, focus на input после выбора; forward focus/blur. Тесты: blur закрывает; выбор закрывает+focus на input; open-on-arrow.
- [x] **K1-R2-6 — Boundary-тест обходится.** `./../store/foo` проходит `startsWith('./')`; подкаталоги / `export…from` / `import()` / `require()` не проверяются (`search-ui-boundary.test.js:19`). Фикс: рекурсивный обход + резолв пути импорта (внутри cluster / react / Icon) + все формы импорта. Мутационный контроль: `./../store` → RED.
- [x] **K1-R2-7 — Ручной `useCallback` (запрещён при React Compiler, AGENTS.md:244).** `SearchField.jsx` (setInputNode/handleClear) + `useSearchComboboxNavigation.js` (onKeyDown/activateIndex/compositionHandlers/resetActive). Фикс: убрать ручную мемоизацию, довериться компилятору.
- [x] **K1-R2-8 — Полная сюита формально красная** (было 8 failed, все `Test timed out 5000ms` = FLAKE-TIMEOUT-LOAD). Перезапуск round-2 дал **чистый прогон: 6933 pass / 0 fail / 18 skipped** (753 файла, 0 упавших) — флейки не воспроизвелись. Подтверждает, что 8 падений были load-flake, не связаны с K1.
- [x] **K1-R2-доп-a — hardcoded `clearAriaLabel="Clear"`** → убрать дефолт (строки только снаружи).
- [x] **K1-R2-доп-b — loading live-region пустой** (`<span/>` без текста, ничего не объявляет) → `loadingContent` проп в status-region.
- [x] **K1-R2-доп-c — claim «nested controls конструктивно исключены» слишком сильный** — `renderOption` технически может вернуть кнопку; ослабить формулировку + отметить, что K4-renderer нужен отдельный контракт-тест.

### K1 round-3 дефекты (ревью Игоря 15.07 — fail-open защитные контракты; TDD-first, до фиксов)

Round-2 подтверждён (disabled-гейт/useId/sessionKey/uncontrolled open/recursive boundary/no-useCallback реализованы), но осталось несколько мест, где контракт fail-open.

**✅ ИСПРАВЛЕНО 15.07 (round-3), TDD-first.** K1-R3-1..9 закрыты; кластер `components/Search` **99/99 pass**, scoped lint **0 errors** (source-warnings 0, все warnings — test-фикстуры). Полная сюита **6939 pass / 1 flake** (`NotebookEntryEditor.test.jsx` waitFor-таймаут = FLAKE-TIMEOUT-LOAD, изолированно 12/12, вне Search).

- [x] **K1-R3-1 — Стабильная идентичность не гарантирована.** `defaultGetOptionKey` (`searchUiContract.js:70`) откатывается к `idx-N` → после reorder Enter может уйти на другую сущность; bare `id` без `kind` допускает коллизию entry/project/primer. Фикс: fail-closed — `entityKey ?? key ?? id`, БЕЗ позиционного fallback (throw при отсутствии стабильного ключа); entityKey (kind-qualified) в приоритете. Cross-kind списки обязаны давать entityKey.
- [x] **K1-R3-2 — DOM-id кодируется с потерями.** `optionDomId` (`searchUiContract.js:58`) `\s+→_` схлопывает `My Enzyme` и `My_Enzyme` в один id. Фикс: инъективная кодировка (`encodeURIComponent`) + collision-тест.
- [x] **K1-R3-3 — Disabled только скрывает popup.** `SearchCombobox` (`:74`) сохраняет `openState`+active → `open→disabled→enabled` снова показывает старую выдачу без действия. Фикс: при disabled render-time сброс openState=false (combobox) + activeKey=null (nav).
- [x] **K1-R3-4 — IME без гарантированного commit.** После `compositionEnd` браузер может не слать `change` → контроллер остаётся с `isComposing:true`. `SearchField:111`. Фикс: на compositionEnd повторно слать текущее значение с `isComposing:false` (safe re-send); тест БЕЗ дополнительного change.
- [x] **K1-R3-5 — Доступность необязательна (fail-open).** (a) clear-кнопка может появиться без `clearAriaLabel` → без доступного имени; (b) `loading=true` допускает пустой live-region без `loadingContent`; (c) `resultCount` визуально с aria-hidden. Фикс fail-closed: clear рендерится ТОЛЬКО при clearAriaLabel; loading-узел ТОЛЬКО при loadingContent (aria-busy остаётся); resultCount = декоративный бейдж, озвучка через listbox aria-live statusContent (задокументировать).
- [x] **K1-R3-6 — Escape не гасит всплытие** → закрытие списка может закрыть родительскую модалку. Фикс: `e.stopPropagation()` на Escape-open в nav-hook + тест.
- [x] **K1-R3-7 — `sessionKey` необязателен** → без него old-active-баг возвращается. Фикс: безопасный fallback `sessionKey ?? value` в combobox (смена query-текста = новая сессия) + тест.
- [x] **K1-R3-8 — Boundary пропускает template/computed dynamic import** (`import(\`./${x}\`)`, `import(v)`). Фикс: детект non-literal `import()`/`require()` → violation (нельзя статически проверить = fail-closed) + тест.
- [x] **K1-R3-9 — Cyrillic в source-комментариях** (`SearchFilterChips.jsx:1` и др.) даёт lint-warning → формулировка «все warnings — фикстуры» неточна. Фикс: комментарии в source → English; после этого source-warnings = 0.

### K1 round-4 дефекты (ревью Игоря 15.07 — 4 обходимых fail-closed + 3 non-block; TDD-first, до фиксов)

Round-3 подтверждён (encodeURIComponent / visual disabled-reset / Escape propagation / нет idx-N реализованы), но 4 контракта всё ещё обходятся.

**✅ ИСПРАВЛЕНО 15.07 (round-4), TDD-first.** K1-R4-1..7 закрыты; кластер `components/Search` **112/112 pass**, scoped lint **0 errors** (source-warnings 0), полная сюита **6953 pass / 0 fail** (чистый прогон). Boundary переведён на AST (`@babel/parser`), мутация `import('./'+x)` → RED.

- [x] **K1-R4-1 — cross-kind идентичность неоднозначна.** `defaultGetOptionKey` принимает голый `id` → `entry:x` и `primer:x` без entityKey дают ОДИН key/DOM-id/activeKey; клавиатура не отличит второй объект. Фикс: для kind-bearing выводить `kind:id` обязательно; + `assertUniqueOptionKeys` (throw при коллизии ключей списка).
- [x] **K1-R4-2 — `sessionKey ?? value` не идентифицирует сессию.** При том же тексте меняются scope/профиль/фильтры/поколение → старый activeKey переживает переход. Фикс: `sessionKey` ОБЯЗАТЕЛЕН для общего combobox (throw без него); fallback на `value` только при явном `textOnly`.
- [x] **K1-R4-3 — boundary обходится.** Регулярка проверяет только первый символ аргумента → `import('./' + moduleName)` начинается с кавычки (не computed), но и literal-regex его не извлекает → запрет обходится без падения. Фикс: **AST-разбор** (`@babel/parser`) статических/re-export/dynamic/require; тесты на конкатенацию, комментарий-после-литерала, второй аргумент `import()`.
- [x] **K1-R4-4 — число результатов только зрячему.** `resultCount` визуально + `aria-hidden`, `statusContent` необязателен → «12» показано, скринридеру не объявлено. Фикс: обязательный локализованный announcement при показе счётчика (sr-only aria-live), иначе визуального счётчика нет.
- [x] **K1-R4-5 (non-block) — двойной commit IME.** `compositionEnd` безусловно шлёт финал, затем браузер может прислать `change` с тем же значением → 2 commit → 2 запуска/отмены. Фикс: дедуп по (value,isComposing); тесты обоих браузерных сценариев.
- [x] **K1-R4-6 (non-block) — disabled минует onOpenChange.** `setOpenState(false)` при disabled закрывает DOM, но `onOpenChange(false)` не зовётся → внешний наблюдатель остаётся `open=true`. Фикс: уведомлять эффективный open (popupOpen) единым каналом.
- [x] **K1-R4-7 (non-block) — whitespace-only label.** `!!clearAriaLabel` пропускает строку из пробелов; то же для `chip.removeLabel`. Фикс: `hasText` (trim) fail-closed для обоих. **Приёмка K1 (Игорь): remove-кнопка чипа не была прикрыта `hasText` → дозакрыто перед K2** (SearchFilterChips: remove-button рендерится только при `hasText(chip.removeLabel)`; чип-лейбл виден, но без безымянной кнопки). +2 теста (whitespace / missing removeLabel). Кластер 114/114.

### K2 дефекты (ревью Игоря 15.07 — 4 P1 контракта; тесты закрепляли реализацию, не контракт; TDD-first, до фиксов)

Основа рабочая (K1-остаток закрыт, 8 режимов/группы верны), но некоторые тесты фиксируют реализацию вместо сквозного контракта (напр. `search-modes.test.js:39` утверждает «null → все режимы», называя это fail-closed — это fail-OPEN).

**✅ ИСПРАВЛЕНО 15.07 (K2-corr), TDD-first.** K2-P1-A..D закрыты; тесты переписаны против контракта (реальные `SEARCH_PROFILES`, fail-CLOSED). Кластер `components/Search` + `search-modes` **144 pass**, scoped lint **0 errors** (source-warnings 0).

- [x] **K2-P1-A — Registry и capabilities не один контракт.** `search-modes.js` НЕ импортирует `PREFIX_REGISTRY` — вручную дублирует `canonical/labelKey/requiresProvider` (дрейф: `mol` labelKey `molecules` vs registry `molecule`). `capabilityHas(available==null)→true` = **fail-OPEN**. `primer/project` требуют вымышленные capabilities; реальные `SEARCH_PROFILES.providers` = metadata/sequence/protein/enzymeCatalog/restrictionSites; kinds — в `entityScope`. Фикс: модели ВЫВОДИТЬ из PREFIX_REGISTRY; capability-контракт `{providers, entityScope}` раздельно; отсутствие/неверная форма → только `lib/mol`; тест против реального `SEARCH_PROFILES.globalLibrary`, не синтетики.
- [x] **K2-P1-B — «Проект» не text-фильтр.** `SEARCH_FILTER_DEFS` project = `inputType:'text'` → два одноимённых проекта не различить. Фикс: тип `entity` (`entityKind:'project'`), опции `{projectId,label}`, callback передаёт устойчивый `projectId` (спека §617: id→имя, уник-совпадение = projectId+label, дубли = ошибка-с-выбором).
- [x] **K2-P1-C — Selector смешивает ARIA-паттерны.** Trigger — обычная `<button>`, но с `aria-activedescendant` (SearchModeSelector:69). Фикс: ОДИН паттерн — select-only `combobox` (`role="combobox"` на trigger, фокус+activedescendant на нём, popup role=listbox) [W3C].
- [x] **K2-P1-D — SearchFilterMenu не закрывает автомат состояний.** Матрица `type-list/text/enum/project`: `open→disabled` немедленно закрыть + блок `pickDef/commit`; re-enable без воскрешения стадии; Enter/Escape в IME-composition не добавляют/не закрывают; фокус → trigger после close/Escape/select; `role=menu` → перемещение фокуса + Arrow/Home/End; текст-input → `role=dialog`/popover (не input внутри `role=menu`); пустые trigger/def/option-имена и пустые enum-values → fail-closed.

### K2-corr2 (ревью Игоря 15.07 — 4 контрпримера внутри A/D; pairwise-пересечения состояний, не новые требования)

C (combobox) и B (projectId) приняты. Осталось: тесты проверяли отдельные примеры, не ПЕРЕСЕЧЕНИЯ состояний.

**✅ ИСПРАВЛЕНО 15.07 (K2-corr2), TDD-first + pairwise-набор.** K2-corr2-A..D3 закрыты; `components/Search`+`search-modes` **149 pass**, scoped lint **0 errors** (source-warnings 0). + адверсариальный intersection-probe (4 скептика) прогнан → нашёл ещё один класс (D4, ниже), CONFIRMED, дозакрыт.

- [x] **K2-corr2-D4 — drain-after-open (гард только на момент open)** (найдено адверсариальным probe, CONFIRMED). Если actionable-множество опустошается ПОСЛЕ открытия (parent rerender `filterDefs=[]`, или опции активного enum/entity стали пустыми при показанной стадии) → рендерится пустой `role=menu` без menuitem'ов, фокус падает на `body`, Escape не сработает (фокус вне ul) — клавиатурный тупик. + roving `tabIndex` из UNCLAMPED `menuIndex` (после End→shrink ни один item не 0, focused=-1). + blur без compositionEnd оставляет IME-флаг залипшим (нет blur-clear). Фикс: render-time drain-close (setOpen false + reset, без ref) когда текущая стадия пуста; refocus trigger через effect при потере фокуса на body; clamp menuIndex для tabIndex+focus; onBlur input чистит composingRef.

- [x] **K2-corr2-A — частично-повреждённый capability fail-open** (`search-modes.js:115`). Две оси валидируются независимо: `{providers:['sequence'], entityScope:'oops'}` открывает ДНК; `{providers:'oops', entityScope:['primer']}` открывает Праймеры. Замороженный контракт: неверная форма ОБЪЕКТА целиком → только lib/mol. Фикс: pre-validate ОБЕ оси до расчёта; любая present-but-malformed ось → fail-closed целиком. +2 симметричных теста.
- [x] **K2-corr2-D1 — IME-флаг переживает закрытие** (`SearchFilterMenu.jsx:70`). `resetStages()` не чистит `composingRef` → IME-start → disable/close → re-open text → Enter/Escape навсегда игнорируются. Фикс: `composingRef.current=false` на reset/close/disable. Комбинированный тест (IME×close×reopen), не раздельные.
- [x] **K2-corr2-D2 — видимый trigger → безымянное меню.** Guard пускает `triggerLabel` при пустом `ariaLabel`, но popup получает пустой aria-label. Фикс: единый `accessibleLabel = hasText(ariaLabel)?ariaLabel.trim():triggerLabel.trim()` для trigger+menu+dialog. Тест: нормальный triggerLabel + отсутствующий/whitespace ariaLabel.
- [x] **K2-corr2-D3 — можно открыть заведомо пустую стадию.** `visibleDefs` проверяет только label → показывает def с неизвестным `inputType` (после выбора popup исчезает при aria-expanded=true) и enum/entity без единой валидной опции (пустое меню). Фикс: до рендера пускать только `text|enum|entity` с ≥1 валидной опцией для enum/entity; нет валидных defs → не открывать trigger/menu. Тесты: unknown-inputType; enum со всеми-плохими опциями.

### K2-corr3 (ревью Игоря 15.07 — D4 недозакрыт: три перехода при обновлении данных под уже ОТКРЫТЫМ меню; A+D1–D3 приняты)

Прошлый D4-гард ловил drain-на-момент-open (enum-опции/type-list опустели). Осталось три пересечения, где стадия «переживает» изменение входных пропов — источник ложного «ничего не найдено».

**✅ ИСПРАВЛЕНО 15.07 (K2-corr3), TDD-first (RED → GREEN).** `components/Search`+`search-modes` **159 pass** (SearchFilterMenu 27), scoped lint **0 errors / 0 warnings**, `SearchFilterMenu.jsx` 13.6 KiB (≪ soft 30). Полная сюита **7000 pass / 0 fail / 18 skipped** (чистый прогон), `npx vite build` OK. Общий корень: захваченное по объекту/индексу состояние переживало смену входных пропов → continuous re-resolve по стабильной identity (def по `id`, roving по `key`), потеря имени = drain, и **единый инвариант: любой popup open→closed чистит IME (corr3-1b)**. **K2 ПРИНЯТ Игорем** (4 гейта: RED→GREEN, целевой прогон, полная suite, build) → переход к K3 без широкого ревью.

- [x] **K2-corr3-1 — активный фильтр удалён из `filterDefs`** (`SearchFilterMenu.jsx:48,87`). `activeDef` хранился КАК ЗАХВАЧЕННЫЙ ОБЪЕКТ в state; parent убирает его из `filterDefs` → `stageHasContent` для text = `true` → окно text-стадии остаётся, `commitText` вызывает `onAddFilter(removedDef,…)` с уже недоступным фильтром. Фикс: хранить `activeDefId`, на КАЖДОМ рендере резолвить `activeDef` заново по стабильному `id` среди текущих `actionableDefs`; `activeDefLost` (id есть, def нет) → полный drain (закрыть меню + очистить стадию/текст/IME), commit-гарды `!activeDef`. Тесты: text-def удалён → dialog+menu закрыты, focus→trigger, callback не вызван; enum-def удалён → закрыт.
- [x] **K2-corr3-2 — фокус после изменения списка** (`SearchFilterMenu.jsx:114,145`). `activeMenuIndex` клампился к новому размеру, но Arrow-математика считала от СТАРОГО `menuIndex` (`(i+1)%count`), а focus-effect зависел от числового индекса → при перестановке/сокращении списка DOM-фокус и `tabIndex=0` расходились (фокус мог остаться на элементе с `tabIndex=-1`). Фикс: активность по КЛЮЧУ элемента (`activeKey`→`effectiveActiveKey`), навигация `moveActive` от эффективного ключа, `tabIndex` по ключу, focus-effect ищет узел по `data-menukey` (зависит от ключа, не индекса). Тест: фокус на 'b' → reorder ('b'→фронт) → DOM-фокус и `tabIndex=0` на 'b', ArrowDown → 'c'.
- [x] **K2-corr3-1b — IME-флаг переживает `activeDefLost`-drain** (ревью Игоря 15.07, точный остаток corr3-1). `activeDefLost`-drain (render-time) сбрасывал `open/activeDefId/text/activeKey`, но `composingRef` чистился ТОЛЬКО в effect по `[disabled, accessibleLabel]` — не по потере активного def. Удаление активного text-фильтра ПРЯМО ВО ВРЕМЯ IME-композиции → после reopen Enter/Escape заблокированы. Инвариант (надёжный): при ЛЮБОМ переходе popup `open → closed` (Escape/select/disable/drain/`activeDefLost`/потеря имени) чистить `composingRef`. Фикс: перенёс clear в effect `[popupOpen]` (он уже ловит именно этот переход — там же drain-refocus), убрал теперь-избыточный effect `[disabled, accessibleLabel]`. Тест: composition → удалить active def → вернуть → reopen → ввод+Enter → callback вызван.
- [x] **K2-corr3-3 — исчезновение доступного имени → воскрешение сессии** (`SearchFilterMenu.jsx:138`). При пустых `ariaLabel`+`triggerLabel` компонент делал `return null`, НЕ сбрасывая `open/activeDef/textValue/IME` → после возврата подписи старая стадия и текст «воскресали». Фикс: `accessibleLabel` вычисляется ПЕРВЫМ; `sessionDead = disabled || !accessibleLabel` входит в continuous drain до `return null`; IME-флаг чистится в effect по `[disabled, accessibleLabel]`. Тесты: IME+текст → убрать обе подписи → вернуть → reopen с пустым input; Enter коммитит (IME не залип).

---

### K3-corr (ревью Игоря 15.07 — NO-GO: `classify(raw) ≢ classify(build(sync(raw)))`, sync/build теряют семантику)

Граница верна (чистый `lib/`), но плоский `draft` + `split(/\s+/)` в сериализаторе теряет структуру. Контрпримеры через настоящий classifyQuery: `aa:HHHH pUC`→белок «HHHH pUC» (д.б. белок HHHH + текст pUC); `aa:HHHH seq:ATGC`→конфликт исчезает; `"Alpha Beta"`→два битых терма; `mol:"Alpha Beta"`→`mol:Alpha`+свободный Beta. Целевые 32/32 не ловили это — не сверяли эквивалентность плана.

**✅ ИСПРАВЛЕНО 15.07 (K3-corr), TDD-first (RED 18 → GREEN 33), oracle-first.** Переписан `lib/search-query-sync.js` (15.0 KiB) на **сегментную модель**: residual хранится как ordered segments (modeValue / text / rawPrefix / incomplete / pending), build re-emits из сегментов (не split). Первый mode lift'ится, последующие → verbatim → classify(build) пере-детектит конфликты. Oracle `project(classify(raw)) ≡ project(classify(build(sync(raw))))` зелёный на всех 12 entry. Source lint 0/0; test 0 warnings (Cyrillic alias берётся из registry, без литерала); полная сюита 7033 pass / 0 fail; build OK.

- [x] **K3-corr-A — потеря происхождения токенов.** Одного `draft` мало → хранить структурные сегменты: значение mode/provider ОТДЕЛЬНО от free text, quoted/raw, незавершённые, блокирующие конфликты. Serializer НЕ `split(/\s+/)` — собирать из сегментов (mode-value re-prefix, text bare, rawPrefix/incomplete verbatim). Несколько mode/scope в одном paste — НЕ last-wins: первый lift, остальные verbatim → classify(build) пере-детектит multiple-provider-intents / multiple-scope-presets.
- [x] **K3-corr-B — завершение unquoted-фильтра.** Посимвольный `tag:cloning` уже на `tag:c` делал chip. Контракт: quoted завершается кавычкой; unquoted tail — по пробелу/Enter/blur/paste (commit-сигнал); keystroke внутри последнего токена chip НЕ создаёт. Чистый параметр `commitTail`/transactionKind (монтирует K4).
- [x] **K3-corr-C — addFilter не fail-closed + typed `in:` резолв.** Принимал `aa`/`evil`/неверный inputType. Фикс: только через `FILTER_BY_CANONICAL`, брать РЕАЛЬНЫЙ def из registry (не доверять объекту), отклонять unknown/mode canonical. Typed `in:Gla` НЕ `{projectId:'Gla'}` — оставлять unresolved; уникальное совпадение (инжектируемый resolver) → `{projectId,label}`, неоднозначное → blocking diagnostic.
- [x] **K3-corr-D — потеря mode при пустом draft.** `mode=primer + tag:x + draft=''` сериализовался как `tag:x` → UI «Праймеры», движок ищет default library. Фикс: либо scope в исполняемом плане, либо явный non-runnable (`isRunnable=false`) — но НЕ запускать другой scope.
- [x] **K3-corr-E — oracle-матрица.** Параметризованный тест `project(classify(raw)) ≡ project(classify(build(sync(raw))))` (сравнивать provider/scope/bio-queries/textTerms/filters/blocking-codes; игнор raw/spans/clause-id). Матрица: `aa:HHHH`, `aa:HHHH pUC`, `aa:HHHH type:circular`, `aa:HHHH seq:ATGC`, `mol:pUC project:Alpha`, `"Alpha Beta"`, `mol:"Alpha Beta"`, `foo:"bar baz"`, `aa:"HH`, `re:EcoRI`, `рестриктаза:BsaI`, `tag:a tag:b` + отдельно: посимвольный `tag:cloning`; unknown/non-filter addFilter; unique/ambiguous typed `in:`; mode+filters+пустой draft.

- [x] **K3-corr2 — конфликт исчезает при следующем вводе (state-transition в frozen A)** (`search-query-sync.js:111`). ✅ ИСПРАВЛЕНО 15.07. Oracle проверял только `sync(DEFAULT, raw)`; реальный UI = `sync(prevState, editedDraft)`. `modeLifted` начинал `false` КАЖДЫЙ вызов → уже поднятый `aa:` скрыт из draft, оставшийся `seq:` на следующем keystroke ошибочно становился «первым» режимом → `aa:HHHH seq:ATGC` + « pUC» = режим aa→seq, `multiple-provider-intents` исчезал; аналогично `mol:pUC project:Alpha`. Фикс (одна строка): `modeLifted = base.mode !== 'lib'` — режим уже активен → последующие provider/scope-префиксы verbatim до явного разрешения (селектор/clear). +3 lifecycle-теста на `sync(prevState, editedDraft)` (оба конфликта сохраняются; mode не auto-switch'ится, переключение только через clearMode/селектор). 36/36, source lint 0, полная сюита 7036/0, build OK.

(K4-гейт, НЕ блокер: `name`/`feature`/`in` в registry = parse-only → до монтажа сделать исполняемыми или не показывать рабочими фильтрами.)

### K4.1-corr (ревью Игоря 15.07 — 2 P1 в фундаменте контроллера `useSearchQueryState`, до монтажа K4.2)

**✅ ИСПРАВЛЕНО 15.07, TDD-first (RED 4 → GREEN 12/12), lint 0/0, hook 4.9 KiB.** P1-1: `runnable = isRunnable(state) && !hasBlockingError && !hasUnresolvedEntity`. P1-2: `onDraftChange(value, {isComposing, inputType})` + отдельный видимый `buffer`/`composing` (`draftValue`), IME не парсится, `insertFromPaste` коммитит в том же change. Полная сюита 7048/0 (первый прогон 1 flake ECONNREFUSED :3000 — network, не воспроизвёлся). **K3-corr2 принят → GO на K4.2 после этих фиксов.**

- [x] **K4.1-P1-1 — `runnable` не учитывает блокировки.** Неоднозначный `in:X` даёт `canonicalQuery=in:X`, `ambiguous-project` diagnostic, но `runnable=true`; то же при provider-конфликте → K4.2 запустит битый поиск. Инвариант: `runnable = isRunnable(state) AND нет severity:error AND нет unresolved entity-фильтров`. Тесты: unresolved `in:Gla`→false; unique→`in:p7`,true; ambiguous/missing→false; provider conflict→false.
- [x] **K4.1-P1-2 — контроллер теряет метаданные события ввода.** `SearchField` шлёт `{isComposing}`, но hook берёт только `value` → промежуточный кириллический IME разбирается парсером; «paste коммитит хвост» недоказан (browser `paste` до `change` → `onCommitDraft()` из `onPaste` коммитит СТАРОЕ значение). Контракт `onDraftChange(value, {isComposing, inputType})`: `isComposing:true`→только видимый буфер (mode/chips/canonical/поиск не трогать); composition end→разобрать финал один раз; `inputType==='insertFromPaste'`→коммит вставленного в том же change. Тесты: mid-IME не парсится; paste коммитит немедленно.

### K4.2b∪K5-preflight (Игорь 15.07 — 3 узких P1 ДО первого изменения живых компонентов; TDD-first, RED→GREEN)

Перед dedicated assembly pass (i18n → монтаж `LibraryWorkspace+LibraryTopBar` → дерево на `runTreeSearch`) закрываем три контракта в ЧИСТОМ ядре (`lib/search-query-sync.js` + контроллер `useSearchQueryState`), пока живой код не тронут — дешевле и безопаснее. Все три — латентные (K3/K4 нигде не смонтированы), но каждый позже открыл бы НЕ ТОТ объект / молча исказил запрос.

**✅ ИСПРАВЛЕНО 15.07 (K4.2b∪K5-preflight), TDD-first (RED 7 → GREEN).** Три P1 закрыты в чистом ядре, живые компоненты не тронуты. Кластер `components/Search` + `lib` search (query-sync/modes/classify/registry/tree-controller) + `useSearchQueryState` **313/313 pass**. Дальше — DOM-монтаж.

- [x] **PRE-1 — удаление неоднозначного `in:`-чипа не пересчитывает diagnostics.** `removeFilter` (`search-query-sync.js:173`) сохранял `base.diagnostics` дословно → стале `ambiguous-project`/`unknown-project` от удалённого чипа переживал удаление → `hasBlockingError` держал оставшийся валидный запрос (напр. `tag:cloning`) заблокированным (`runnable=false`). Фикс (чистый, без резолвера): `removeFilter` отбрасывает entity-диагностику, у которой больше нет соответствующего present unresolved-фильтра (`ENTITY_DIAG_CODES` + `unresolvedTokens`). Тесты: pure (per-chip диагностика дропается, чужая по другому unresolved-чипу остаётся) + контроллер (после удаления `in:X` `runnable` восстанавливается на `tag:cloning`).
- [x] **PRE-2 — во время IME-композиции нет сигнала «погасить popup».** Контроллер прокидывал `draftValue = composing ? buffer : draft`, но НЕ возвращал `composing` → смонтированный combobox не знал, что во время композиции popup со старой выдачей надо закрыть/запретить выбор. Фикс: контроллер возвращает `composing`; контракт (в комментарии) — mount гейтит `open` и выбор (мышь+клавиатура) на `!composing`. Тест: `composing=true` mid-IME (+ committed-state не меняется), `false` после коммита. (UI-поведение popup — в браузерной матрице.)
- [x] **PRE-3 — `tag:a tag:a` молча схлопывается в один фильтр.** `makeChip`/`addChipDedup` (`:79-84`) ключевали по `${canonical}:${valueKey}` → повтор одного клауза терялся; это ЖЕ ломало oracle для повторов (build отдавал один `tag:a`, classify(raw) видит два). Фикс: **parser-owned occurrence identity** — `makeChip(canonical,value,occurrence)` + `nextOccurrence`/`appendParsedChip` дают каждому клаузу отдельный `canonical:value#N` id (без дедупа); меню `addFilter` остаётся идемпотентным (дедуп по canonical+valueKey, повторный клик = no-op); `resolveEntityFilters` держит id стабильным при резолве (`{...f, value}`, без коллизии `in:X in:X`). `addChipDedup` удалён. Тесты: oracle-матрица (+`tag:a tag:a`) + 2 чипа с разными id, build round-trip, удаление одного оставляет второй, меню идемпотентно.

### K4.2b∪K5 assembly — DOM-монтаж + split дерева (Игорь 15.07; браузер-проверено целиком)

**✅ РЕАЛИЗОВАНО 15.07, TDD + build + full suite + браузер.** Единый атомарный шаг: i18n → монтаж `LibraryWorkspace + LibraryTopBar` на контроллер → дерево на `runTreeSearch`. Полная сюита **7072 pass / 0 fail** (761 файл), `npx vite build` OK. Кластеры: query-sync/modes/controller/tree-controller/`components/Search` **313**, topbar **34**, workspace **15**, tree **119**. Файлы (size budget): LibraryTopBar 32.8 KiB (soft>30, hard<40 — WATCH, вырос ~13 KiB от SearchField/selector/chips/menu-композиции), LibraryWorkspace 39.4 KiB (у hard 40 — WATCH), search-query-sync 17.7 KiB (OK), tree-controller 3.7 KiB (OK).

- [x] **i18n** — `search.*` flat-ключи в `i18n.js` (en+ru): modeGroup/mode/field/filter/placeholder/enum/error/diag. Селектор/чипы/меню/плейсхолдеры резолвятся через `t()`.
- [x] **ch1 монтаж + split** — `LibraryWorkspace` разделил SEARCH-UNIFY общий `query` на `treeQuery` (дерево) + `globalSearch = useSearchQueryState(globalLibrary)` (топ-бар); эскалация «Полный поиск» = `seedGlobalQuery(treeQuery)` (replace, не merge); enzyme-card «Найти сайты» → `seedGlobalQuery` (в глобальный, не в дерево). `LibraryTopBar`: bare `<input>`→`SearchField` + `SearchModeSelector` + `SearchFilterMenu` (только исполнимые tag/type/status) + `SearchFilterChips`; фасад бежит на `search.canonicalQuery` при `runnable`; блок-запрос → notice из `search.diagnostics` (fail-closed, не «ничего не найдено»); popup гейтится `!composing` (PRE-2); per-mode placeholder. Тест «две строки независимы» переписан (был «shared query»).
- [x] **ch2 дерево** — `LibraryTreeRoot` считает ОДНУ `runTreeSearch(query, collectEntryDocuments(...))`-сессию; выводит `matchEntry` (active→matchingEntryIds, empty/эскалация→show-all) + `requiresFullSearch`; прокинул в `ProjectZone`/`LooseZone` (убрал 3 независимых `makeEntryMatcher`). seq:/aa:/cut: в дереве → не фильтрует (показывает всё) + баннер «Полный поиск» — **чинит false-empty** (makeEntryMatcher fail-closed → пусто).
- [x] **Браузерная матрица (live, 142 записи, 0 console/server errors):** глобальная строка «puc»→дропдаун «Поиск·1» pUC, дерево НЕ отфильтровано (независимость); `seq:GAATTC` в дереве→баннер эскалации + дерево полное (не пусто, не match-all — ch2); селектор открывает группы МОИ ДАННЫЕ/БИОПОИСК/СПРАВОЧНИКИ с ru-лейблами; per-mode placeholder (seq→«Нуклеотиды…»); clear→сброс на «Вся библиотека»; очистка глобальной строки не тронула seq:-эскалацию дерева (независимость ×2). Блок-notice при ручном наборре конфликта не воспроизвёлся из-за race быстрого ввода в controlled-input (browser-automation артефакт) — логика покрыта юнитом (seed-path `enz:Bsa seq:GAATTC`→blocked notice, dna-search.test).

## REV#2 Stage 3 — K4.2b∪K5 corrective (review 16.07.2026)

Найдены при независимой приёмке смонтированного поиска. Исправляются одной ограниченной corrective-итерацией, без расширения Stage 3. Каждый пункт требует RED regression-теста до production-правки.

- [x] **REV#2-S3-CORR-P1-1 (✅ ИСПРАВЛЕНО 16.07) — live TopBar обходит K1 combobox-контракт.** `ArrowUp/ArrowDown` не двигают активный результат, `Enter` не выбирает его, `aria-activedescendant` отсутствует; мышь — единственный рабочий путь.
- [x] **REV#2-S3-CORR-P1-2 (✅ ИСПРАВЛЕНО 16.07) — дерево теряет project/primer kinds.** Live-путь собирает только `collectEntryDocuments`; `primer:`, `type:primer`, primer-status и `project:` могут дать false-empty при существующей сущности.
- [x] **REV#2-S3-CORR-P1-3 (✅ ИСПРАВЛЕНО 16.07) — заявленная единая tree-session повторно вычисляется в зонах и строках.** `ProjectZone`/`LooseZone` создают matcher заново, `TreeItemRow` вызывает `describeEntryMatch -> runSearch` per-row.
- [x] **REV#2-S3-CORR-P1-4 (✅ ИСПРАВЛЕНО 16.07) — blocked/error QueryPlan дерева выглядит как обычный пустой результат; parse-only escalation ведёт в глобальный тупик.** Нужны blocked diagnostics и только исполнимое recovery-действие.
- [x] **REV#2-S3-CORR-P1-5 (✅ ИСПРАВЛЕНО 16.07) — query-state diagnostics устаревают после UI-переходов.** После удаления конфликтующего chip canonical-query уже валиден, но stale `incompatible-scope-*` может оставить `runnable=false`; quoted whitespace и controlled-clear dedup также нарушают lifecycle.
- [x] **REV#2-S3-CORR-P1-6 (✅ ИСПРАВЛЕНО 16.07) — metadata partial показывается как финальное «Ничего не найдено».** До worker-final нужен честный loading/partial state.
- [x] **REV#2-S3-CORR-P1-7 (✅ ИСПРАВЛЕНО 16.07) — worker-unavailable молча включает тяжёлый inline sequence scan в UI-потоке.** Browser runtime должен деградировать в metadata-only `incomplete`, а inline разрешён только как явный test/core режим.
- [x] **REV#2-S3-CORR-P1-8 (✅ ИСПРАВЛЕНО 16.07) — выбор результата не закрывает live-popup.** В браузере `ArrowDown → Enter` открывает правильную молекулу, но listbox остаётся поверх инспектора с `aria-expanded=true`; исправлено query-owned closed-state, которое не допускает позднего focus-воскрешения и снимается только новым действием пользователя.
- [x] **REV#2-S3-CORR-P2-1 (✅ ИСПРАВЛЕНО 16.07) — visual enum и i18n неполны.** Нет `type:primer` и primer lifecycle statuses; часть search/tree UI остаётся hardcoded RU.
- [x] **REV#2-S3-CORR-P2-2 (✅ ИСПРАВЛЕНО 16.07) — tree quick-search снимает видимый focus.** Инлайн `outline:none` оставляет клавиатурного пользователя без фокус-индикатора; нужен системный `:focus-visible`, а не безусловное подавление.
- [x] **REV#2-S3-CORR-PROC-1 (✅ ИСПРАВЛЕНО 16.07) — LibraryTopBar пересёк soft-size с большой дельтой.** Поиск вынесен в `LibrarySmartSearchBar`; `LibraryTopBar.jsx` уменьшен до 10.62 KiB.

### S3-CLOSE K1 corrective (Игорь 16.07 — узкий NO-GO: строгая AND ещё не инвариант системы; TDD-first, RED→GREEN)

Основной алгоритм работает когда matcher передан, но строгая AND зависела от наличия engine → четыре обхода. Записаны ДО исправления (Игорь разрешил BUGS.md для corrective-дефектов).

**✅ ИСПРАВЛЕНО 16.07 (S3-CLOSE K1 corrective), TDD-first (RED 7 движок/фасад/VM + RED 2 UI → GREEN).** Строгая AND стала инвариантом системы: ни absent/unknown policy, ни внешний ctx, ни быстрый клик не превращают кандидата в подтверждённое совпадение. Фокус-прогон **320/320** (23 файла: engine/facade/VM/worker/TopBar/tree/`components/Search`), полная сюита **7156 pass / 1 fail / 18 skipped** (единственный fail — `MarkdownView.test.jsx`, изолированно 5/5 = FLAKE-TIMEOUT-LOAD, вне поиска), build OK, scoped lint 0 errors. Mutation-proof: снятие форсированного `required` из final → P1-2 тест RED; разрешение выбора pending → 2 UI-теста RED; обе мутации удалены. Браузер (live, 0 console errors): `mol:pUC seq:GAATTC` (пример Игоря) → «Поиск по ДНК · 0», not blocked, «Ничего не найдено»; `mol:Bluescript seq:GAATTC` → pBluescript остаётся, `aria-disabled=null`, 100% идентичность; `pUC` → pUC19 выбираем. Размер `library-search.js` 24 443 → 24 683 B (+240, hard 25 600 не пересечён).

- [x] **S3CK1-P1-1 — движок не fail-closed без matcher.** `runSearch(..., {})` и даже неизвестный `providerPolicy` возвращают `pUC` без проверки мотива (`ids:["puc"], providerPending:true`), потому что строгий drop привязан к `providerEngine[d]` (engine-presence). Требование: при `required` (absent/unknown/`required`) отсутствие любой обязательной dimension ВСЕГДА удаляет документ; только точное `deferred` разрешает `providerPending`-кандидата. Metadata-поверхности (facade partial, `tree-search-controller`, `describeEntryMatch`) обязаны передавать `deferred` ЯВНО, а не полагаться на неявное отсутствие matcher.
- [x] **S3CK1-P1-2 — final ослабляется внешним ctx.** `search-facade.js` resolve делает `{ ...ctx, seqMatch, ... }` → пользовательский `providerPolicy:'deferred'` протекает в финал → `facade.search(mixed-negative, {providerPolicy:'deferred'})` оставляет name-кандидата в завершённой сессии без `incomplete`. Фикс: successful final принудительно `providerPolicy:'required'` ПОСЛЕ spread; failure-path сам явно `deferred + incomplete:true` (контракт #168).
- [x] **S3CK1-P1-3 — partial-кандидат можно выбрать.** `providerPending` теряется в `search-result-vm.js` (нет в VM), а `LibrarySmartSearchBar` `pickRow` позволяет открыть pending-строку мышью/Enter до биопроверки. Фикс: перенести `providerPending` в VM; pending-строка — проверяемая, не подтверждённая; мышь+Enter no-op; `aria-disabled`; после положительного final выбираема, после отрицательного исчезает; локализованный loading-текст, без хардкода; plain metadata не блокировать.
- [x] **S3CK1-P2 — modern `cutQuery` не запускается.** Обязательность читает `cutQuery`, но фактический запуск (`matchDocument`, `empty`, `pureFilter`, facade `reMatch`) — только transitional `reQuery`. Единый источник `plan.cutQuery || plan.reQuery` везде.
- [x] **S3CK1-P2 — partial теряет пользовательские настройки.** `runMetadata(plan, documents)` не принимает `ctx` → partial игнорирует `opts.limit` и пр. Фикс: `runMetadata(plan, documents, ctx)` → `{ ...ctx, providerPolicy:'deferred' }`.

### S3-CLOSE K2 (Игорь 16.07 — единая обработка сбоев биологических провайдеров; записаны ДО исправления, TDD-first)

Три вычислительные проверки (`seq:`→sequence, `aa:`→protein, `cut:`/`re:`→enzyme) обрабатывают сбой по-разному, и ни одна не доводит до пользователя различие «проверка завершилась, совпадений нет» vs «проверка не выполнена, отсутствие не доказано».

**✅ ИСПРАВЛЕНО 16.07 (S3-CLOSE K2), TDD-first (RED 47 failed/32 passed + 2 Unhandled Rejection → GREEN).** Сбой любого из трёх провайдеров теперь одинаково доходит до сессии как именованная причина (`TIMEOUT`/`WORKER_FAILURE`/`PROVIDER_ERROR`) и до UI как предупреждение с именем упавшей проверки; «Ничего не найдено» показывается только после успешно завершённой проверки. Failure-логика вынесена в новый `lib/search-provider-failures.js` (6 281 B) + `search-facade.js`; **`library-search.js` не изменён — SHA-256 совпадает с baseline** (`f386874f…`, 24 683 B). Фокус-прогон 665/665 (49 файлов), полная сюита 7235 pass/18 skipped (первый прогон дал 1 waitFor-таймаут = FLAKE-TIMEOUT-LOAD, повторный полностью зелёный), build OK, scoped lint 0 errors / 3 warnings (все пред-существующие). Mutation-proof 6 мутаций: `byId:null`→RED(2), снятие guard'ов провайдеров→RED(17), `required` после сбоя→RED(8), `CANCELLED` как сбой→RED(1, только unit — путь отмены двойно защищён), `showResultCount`→RED(1), потеря `incompleteDims`→RED(5); все откачены. Браузер (live, 0 console/server errors): завершённый DNA-miss → «ПОИСК ПО ДНК · 0» + «Ничего не найдено» без предупреждения; DNA-hit → pBluescript «100% идентичность · 6/6 нт», выбираем; `cut:EcoRI` hit/miss; `aa:HHHHHH pET` → KanR «100% совпадение белка · 6 aa». Fault-path — test-covered детерминированным harness, не live (production fault-toggle не добавлялся).

- [x] **S3CK2-1 — inline-исключение провайдера роняет поиск в вечный partial.** `search-facade.js:71,74` конструирует `makeProteinMatch`/`makeReMatch` и передаёт их в `runSearch` без какой-либо защиты; `library-search.js` вызывает matcher напрямую. Исключение при конструировании ИЛИ при вызове (`aa:`/`cut:`) пробрасывается из `resolve`, а `search-service.js:60` имеет только `.then(...)` без `.catch` → promise фасада остаётся rejected, final-колбэк не приходит, UI навсегда залипает в `phase:'partial'` (спиннер + невыбираемые pending-кандидаты), плюс unhandled rejection в консоли. Только sequence-провайдер обёрнут try/catch (`:53`).
- [x] **S3CK2-2 — повреждённый ответ воркера = «честный пустой результат».** `search-worker-client.js:116` делает `resolve(new Map(Object.entries(byId || {})))`. При совпавшем `id`, но `byId: null` / отсутствующем / массиве / строке / объекте с не-массивами в значениях клиент возвращает пустую (или мусорную) Map, фасад считает это успехом, `incomplete` не ставится → пользователь видит «Ничего не найдено» как доказанное отсутствие мотива. Протокол не валидируется вообще.
- [x] **S3CK2-3 — UI теряет измерение сбоя и смешивает «0 совпадений» с «не проверено».** `LibrarySmartSearchBar.jsx:160,168,208` хранит только булев `incomplete` — `incompleteDims` из сессии выбрасывается, поэтому предупреждение жёстко DNA-текстовое (`search.results.incomplete` = «Поиск по последовательности не выполнен…») даже когда упал белок или скан сайтов рестрикции. Счётчик в шапке (`:394`) и `resultCount` (`:331`) при сбое показывают «· 0» рядом с предупреждением — «ноль совпадений» и «проверка не выполнена» визуально сливаются. `finalEmpty` (`:171`) не смотрит на `incomplete`: сегодня текст «Ничего не найдено» всё же подавляется гейтом `!diagnostic` в `SearchResultsListbox.jsx:51`, то есть дефект **латентный** — но единственная защита лежит в чужом компоненте и держится на том, что предупреждение непусто. Локальный `seqIncomplete` (`:168,291,293`) назван DNA-only, хотя поверхность общая для трёх провайдеров. Pending-строка ничем не помечена: пользователь видит невыбираемую строку без объяснения, почему.

### S3-CLOSE K2 corrective (Игорь 16.07 — узкий NO-GO: два fail-open остатка; TDD-first, RED→GREEN)

Архитектура K2 принята, но оба остатка нарушали главное обещание K2 — не выдавать технический сбой за биологический результат. Записаны ДО исправления.

**✅ ИСПРАВЛЕНО 16.07 (S3-CLOSE K2 corrective), TDD-first (RED 39 → GREEN 102/102).** Валидатор ответа стал структурным и общим для worker+inline; счётчик стал единой проекцией состояния проверки для визуального и доступного UI. Кластер 804/804 (55 файлов), полная сюита **7289 pass / 0 fail / 18 skipped — целиком зелёная** (без флейка), build OK, scoped lint 0 errors / 3 warnings (все пред-существующие). Mutation-proof: A — proto-гейт → старая рыхлая проверка → RED(12); B — inline мимо гейта → RED(22); C — счётчик игнорирует состояние проверки → RED(5); все откачены. **`library-search.js` не изменён — SHA-256 `f386874f…` совпадает.** Браузер (live, 0 console/server errors): завершённый miss → «Результатов: 0» + «ПОИСК ПО ДНК · 0»; blocked → шапка «ПОИСК» БЕЗ числа, счётчика нет; hit → «Результатов: 1», pBluescript «100% идентичность · 6/6 нт».

- [x] **S3CK2-C-P1-1 — протокол воркера fail-open.** `isValidByIdPayload` (`search-worker-client.js:64`) проверял лишь «объект + значения-массивы». Следствия: `Map`/`Date` проходят `typeof === 'object'` и через `Object.entries()` дают пустой результат → ложное «мотива нет»; `[null]` / `[{}]` — truthy-массив → ложный DNA-hit без координат; `{'entry:a': []}` принимается как miss, хотя настоящий воркер пустых ключей не создаёт (`if (occ.length)`); inline-путь (`:91`) валидатор обходил полностью. Фикс: один общий `validateByIdPayload(byId, allowedKeys)` для worker и inline — plain record (proto `Object.prototype`/`null`), ключи из отправленного набора документов, непустой массив occurrences, непустые `location.segments`, конечные целые `start/end` с `end > start`. Внешний мусор → `WORKER_FAILURE` + новый воркер; inline-мусор → `PROVIDER_ERROR` (плоский `Error`, не `SearchAbortError`), не complete miss. `{}` остаётся валидным honest miss.
- [x] **S3CK2-C-P1-2 — счётчик не учитывает состояние проверки.** `resultCountLabel` (`LibrarySmartSearchBar.jsx:379`) объявлял скринридеру «Результатов: N» и для неподтверждённых кандидатов; шапка (`:442`) показывала `· 0` пока воркер ещё работает, при заблокированном запросе и рядом с «проверка не выполнена». Фикс: единая проекция `countKind` на визуальный и доступный UI — complete final → «Результатов: N» (честный ноль включительно); unconfirmed (`pendingList || providerIncomplete`) и N>0 → «Предварительных кандидатов: N»; unconfirmed с нулём → числа нет; blocked → числа нет. Новый ключ `search.results.candidatesCount` в обоих языковых блоках.

### S3-CLOSE K3 (спека Chat 16.07 — закрывающий контракт поиска)

**✅ ИСПРАВЛЕНО 16.07 (S3-CLOSE K3), TDD-first по каждой группе.** Подтверждённый hit теперь означает одновременно: валидная сущность, валидные координаты ВНУТРИ её последовательности, успешно завершённая проверка и единая трактовка состояния визуальным UI и скринридером. Новые чистые модули `lib/search-provider-contract.js` (6 265 B) и `lib/search-session-presentation.js` (5 959 B); **`library-search.js` не изменён — SHA-256 `f386874f…`, 0 B дельты.** Focused 949/949 (58 файлов), полная сюита **7434 pass / 0 fail / 18 skipped** (два подряд зелёных прогона), build OK, scoped lint 0 errors / 1 warning (пред-существующая). Mutation-proof: все 8 обязательных мутаций → RED (bounds 4, targetRef 2, inline-array 2, empty>incomplete 3, selection 2, role=alert 3, old-id 11, typedef 2), все откачены. Браузер (live, 0 console/server errors): hit → «Результатов: 1» + 1 live-region/0 alert/0 вложенных; complete miss → «Результатов: 0» + «Ничего не найдено» без warning; blocked → «ПОИСК» без числа, без empty.

- [x] **S3CK3-P1-1 — provider occurrence проверяется не до биологически безопасной структуры.** Общий K2-валидатор проверяет plain `byId`, непустые массивы и целые `start/end` с `end > start`, но не требует `0 <= start < end <= sequenceLength`, plain occurrence/location/segment, допустимый strand/`wrapsOrigin` и отсутствие provider-owned `targetRef`. Inline protein/cut guard проверяет только `Array.isArray`. Следствие: повреждённый провайдер способен опубликовать ложный подтверждённый биологический hit или перенаправить его на другую сущность. Нужен общий `search-provider-contract.js` для worker и inline; `library-search.js` не менять.
- [x] **S3CK3-P1-2 — состояние выдачи выводится из нескольких независимых булевых и маскируется listbox.** `complete/finalEmpty/unconfirmed/countKind` принадлежат бару, а `SearchResultsListbox` повторно решает приоритет через `!loading && !diagnostic`. Поэтому мутация empty-гарда в K2 осталась GREEN. Нужна одна чистая проекция session→presentation и один mutually-exclusive status.
- [x] **S3CK3-P1-3 — вложенные live-region дублируют или переупорядочивают сообщение.** `SearchNotice role="alert"` рендерится внутри `SearchResultsListbox role="status" aria-live="polite"`. Владелец состояния выдачи должен быть один; визуальный notice — без live-role. Счётчик `SearchField` остаётся отдельным sibling-live-region, но питается той же проекцией.
- [x] **S3CK3-P2-1 — DNA-only test id у общего provider warning.** `search-seq-incomplete-warning` используется и для DNA, и для protein/cut. Переименовать во всём репо в `search-provider-incomplete-warning` без alias; старое имя после K3 не встречается.

### SEARCH-COMMIT-AUDIT K1 (Chat 17.07 — аудит состава search-коммита)

- [x] **S3PRE-P2-1 — СНЯТО 17.07, не баг: mojibake в комментариях search-контрактов не подтвердился.** Hex/grep-проверка показала корректный UTF-8 на диске (`C2 AB` для `«`, `E2 86 92` для `→`); строки вида `В«`/`вЂ` возникали из-за чтения UTF-8 как CP1251 в выводе инструмента. Cleanup source-файлов ничего бы не исправил и разошёлся бы с существующим стилем репозитория. Если нужно лечить это системно — отдельная инфраструктурная задача `.editorconfig charset = utf-8`, не search-runtime.
- [ ] **S3COM-P1-1 — search-пакет нельзя безопасно коммитить по путям без hunk-аудита.** Worktree содержит крупный mixed dirty backlog: часть файлов несёт одновременно search-правки и чужой поток работы (`i18n.js`, `App.jsx`, `uiSlice.js`, `import-annotations.js` и др.). `git add .` или широкий path-add может затянуть незавершённые чужие изменения в search-коммит. Нужен точный commit manifest: include/exclude по файлам, mixed-файлы — только после hunk-классификации и явного решения Игоря.
- [ ] **S3COM-P1-2 — в рабочем дереве есть мусорные 0-байтные shell-артефакты.** `gui/designer/console.log(Object.keys(m)))` и `gui/designer/console.log(m.searchAllSequences('AAA'` — следы сорвавшейся shell-команды. В search-коммит не должны попасть ни при каком сценарии; удалять только отдельным явным действием/с разрешения, не через широкую чистку.

## FEATURE REQUESTS

### Feature/annotation gap-research (29.06.2026, 3-агентный воркфлоу SnapGene/Benchling/Geneious + GenBank-модель, 17 кандидатов → ранжировано; см. [[project_feature_annotation_polish]])

Все VERIFIED по коду. Топ-quick-win сделан; остальные — приоритизированный бэклог для Игоря/Chat (многие — модель-уровень, трогают import/export → лучше через спеку).

- [x] **FEAT-TOOLTIP (render, high/small) — ✅ СДЕЛАНО 29.06.** Hover-`<title>` на фиче в СИКВЕНС-треке (раньше qualifier-инфо была только на hover-card карт). AnnotationTrack не имел `<title>`; enriched-поля (description/identity% из enrichWithCommonFeatures) не видны там, где биолог правит. Фикс: чистый `buildAnnotationTitle(region)` (annotation-title.js: имя·тип·1-based span·strand·identity%·description[HTML strip]) + `<title>` первым ребёнком feature-`<g>` (покрывает exon-split и plain ветки). TDD: annotation-title +7, annotation-track +1. Якоря: SequenceView/tracks/annotation-title.js + AnnotationTrack.jsx.
- [ ] **FEAT-TRANSL-TABLE (model, high/medium) — КОРРЕКТНОСТЬ.** `codons.js` имеет ОДНУ жёстко зашитую CODON_TABLE (NCBI 1); грибная мито-CDS (table 4, UGA=Trp) показывает ложный внутренний стоп в AA-треке и экспортит неверный белок. Фикс: `GENETIC_CODES={1,3,4,12}` + translateDNA/walkCodons(table=1) + хранить/парсить/экспортить `transl_table` на region + `<select>` в AnnotationEditor. Самый грибо-специфичный gap.
- [x] **FEAT-QUALIFIERS (model, high/medium) — round-trip ✅ СДЕЛАНО 29.06; editing-UI отложено.** Был: import дропал /gene //product //note → потеря провенанса. **Фикс:** `pickQualifiers(q)` whitelist (note/product/gene/gene_synonym/locus_tag/old_locus_tag/EC_number/db_xref/function/standard_name/protein_id/pseudo; исключает уже-маппленные label/ApEinfo/exons/primer_seq) → `ann.qualifiers` в importFeatures (region + detail pass); export-genbank emit `/key="value"` (массивы→построчно, pseudo=флаг, кавычки удвоены). Round-trip: import→ann.qualifiers→export→re-parse сохраняет product. **Бонус:** FEAT-TOOLTIP уже читает qualifiers.note/product → импортнутые ноты сразу видны в hover. TDD: import-annotations +2, export-genbank +1. **Отложено (follow-up):** key/value секция редактирования в FeatureEditorModal (показ есть через tooltip; правка — отдельно). Якоря: import-annotations.js pickQualifiers, export-genbank.js. `import-annotations.js` парсит ВСЕ qualifiers в feat.qualifiers, но строит аннотацию только из name/type/coords/strand → `/gene /product /EC_number /note /db_xref` теряются на import; export пишет только /label+/bodgegene_*. Round-trip теряет провенанс. Фикс: `qualifiers:{}` на аннотации + whitelist-копия в importFeatures + key/value секция в FeatureEditorModal + emit в entryToGenbank.
- [x] **FEAT-CF-SHARE (export, high/small) — ✅ СДЕЛАНО 29.06.** User common-features DB (Dexie) нельзя было экспортнуть/импортнуть → лаба не делится курированным набором + теряется на v0.6 wipe. Фикс: чистый `common-features-io.js` (serializeCommonFeatures(overlay)→JSON `{format,version,userFeatures[],overrides[]}` детерминированно; parseCommonFeatures лениент — bare-array ok, дропает невалид, чужой format→error) + слайс `importCommonFeatures({userFeatures,overrides})` (reuse promoteFeature дедуп + overrideCommonFeature, минтит свежие id) + 2 кнопки ↓Экспорт/↑Импорт + hidden file-input в CommonFeaturesPanel header (downloadBlob из file-system.js). TDD: common-features-io +8 (serialize/parse/round-trip/lenient), common-features-share +3 (кнопки + export→blob-text + import→store). Якоря: CommonFeaturesPanel/common-features-io.js + index.jsx, commonFeaturesSlice.js importCommonFeatures.
- [ ] **FEAT-DETECT-SLIDER (search, high/medium).** feature-match-core УЖЕ считает identity/coverage + принимает threshold, но детекция на фикс-cutoff без UI и без pre-apply списка хитов. Фикс: проброс minIdentity + панель хитов (имя/тип/span/identity% + чекбоксы + слайдер 80-100%).
- [x] **FEAT-EXTRACT (export, high/medium) — ✅ ПОЛНОСТЬЮ 29.06 (движок + UI).** UI: контекст-меню «Извлечь в библиотеку» на выделении, совпавшем с регионом (рельса matchedRegion как promote-common) → хост (LibrarySingleInspector) делает `extractFeatureSequences` → `buildLibraryEntry({sequence:cdna, topology:'linear', description})` → `addLibraryEntry` (новый loose-entry «{имя} cDNA»). Consumer-gated проп `onExtractFeature` прокинут build-selection-menu-items → SequenceView → SequenceTab → LibrarySingleInspector (Assembly/PCR/Annotator-preview не задеты). TDD: build-selection-menu-items +3 (item при matchedRegion+проп; onClick→callback; нет без regionа/пропа); инспектор+edit-ops 173 зелёных. Якоря ниже. ↓исходный движок: Чистый `lib/feature-extract.js::extractFeatureSequences(fullSeq, region, annotations, opts)` = `spliceRegion` (убирает интроны, strand-aware) → mature cDNA + `translateDNA`→protein (только coding-типы CDS/gene/mRNA/marker/reporter/orf, trailing-stop срезан) + имена `${name} cDNA/protein` + `intronsRemoved`. TDD feature-extract +6 (интрон-сплайс убирает преждевременный TGA-стоп; reverse→RC; promoter→без белка; opts.translate force on/off; null-guard). **UI-обвязка (next, тривиально — оба хелпера готовы):** пункт контекст-меню на выделении-покрывающем-регион (reuse findRegionForSelection как у 'e'-edit) → `buildLibraryEntry({sequence:r.cdna, topology:'linear', description}, r.cdnaName)` → `addLibraryEntry`. Белок-точность для грибной мито зависит от FEAT-TRANSL-TABLE (cDNA независим, корректен сейчас). Якорь: lib/feature-extract.js.
- [x] **FEAT-COLORS (render, medium/small) — ✅ СДЕЛАНО 29.06.** Фунгальные detail-типы (intron/signal_peptide/transit_peptide/propeptide/mat_peptide/domain/motif/active_site/binding/disulfide_bond/core_promoter/poly_a/regulatory/stem_loop) падали в misc (ivory) → мульти-интрон/мульти-домен ген плоский. Добавил 14 dedicated hex в FEATURE_COLORS_V2 (direct-key hit; имена = canonical из DETAIL_TYPE_MAP). TDD feature-palette +3 (все non-misc; intron/signal/domain/active_site взаимно различны; shaded works) + обновлён count-контракт 16→30. Якорь: feature-palette.js FEATURE_COLORS_V2.
- [ ] **Прочее (medium/low):** /codon_start phase (small), флэт-список фич с фильтром по типу + jump-to (medium), annotate-from-reference по гомологии (large), GenBank header ORGANISM/SOURCE round-trip (low/small), same-name-diff-type coexist (low/small). Multi-segment join() — PARTLY REAL но уже покрыто intron-detail моделью (export re-emit join работает) → defer, проверить реальный failing fixture сначала.

(Исторически: фичи живут в `docs/ARCHITECTURE_v2.md` §7 Roadmap до конкретного дизайн-вопроса.)
